# ⚡ سند طراحی — سیستم «انرژی و حال روحی» (Energy & Mood) — صفحه‌ی جدید

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_ENERGY.md`.
> رنگِ هویت: صورتی `#EC4899` (همان رنگِ کاشیِ هاب).

---

## ۱) وضعیت فعلی — موتورِ قوی، صفحه‌ی صفر

- ✅ `EnergyAnalyticsEngine` فوق‌العاده پخته است: **اوجِ کارایی** (بازه‌ی ۳ساعته)، **روزِ پربارِ هفته**، **بازه‌ی خستگی**، و **انرژیِ پویا (٪)** با زنجیره‌ی توضیحِ علّی (پایه + خواب + ساعتِ زیستی + فعالیت + زوالِ زمانی). این‌ها مصرفِ RIE می‌شوند.
- ⚠️ ثبت فقط از `morning_checkin_sheet` و **یک‌بار در روز**: کاربر یک «حال» (خسته/عادی/خوب/عالی) می‌زند که صرفاً **پراکسیِ تنظیمِ انرژی** است → `daily_checkins` + `energy_logs` + `default_energy_level`.
- ❌ صفحه‌ی اختصاصی ندارد؛ کاشیِ هاب «به‌زودی» است. تحلیل‌های غنی فقط در insights دفن‌اند.
- ❌ «حال» یک محورِ مستقل نیست (فقط دیالِ انرژی)؛ بدونِ واژگانِ حسیِ غنی، بدونِ روند، بدونِ همبستگی.
- ❌ انرژی فقط صبح ثبت می‌شود (موتور از چند ثبتِ روزانه پشتیبانی می‌کند ولی استفاده نمی‌شود) → منحنیِ روز معنا ندارد.

**نتیجه:** موتور آماده است؛ کاری که مانده **صفحه + ثبتِ غنی‌تر + مستقل‌کردنِ حال + رونمایی از تحلیل‌ها** است.

---

## ۲) سه تصمیمِ قطعی (تأییدشده)

1. **دو محورِ مستقل + همبستگی.** انرژی (کم↔زیاد = برانگیختگی/arousal) و حال (بد↔خوب = خوشایندی/valence) دو محورِ جدا با واژگانِ حسیِ غنی (آرام، مضطرب، غمگین، عصبانی، شاد، هیجان‌زده...). مدلِ مفهومی = **دایره‌ی احساسات (انرژی × حال)**. تحلیلِ همبستگیِ این دو.
2. **ثبتِ سریعِ هر‌زمان.** دکمه‌ی «الان چطورم؟» که هر وقت از روز انرژی + حال را ثبت می‌کند (موتورِ زوالِ زمانی از قبل آماده است). منحنیِ انرژیِ روز معنادار می‌شود.
3. **داشبوردِ تحلیلیِ کامل + دستیار.** همه‌ی خروجی‌های موتور (اوج/خستگی/روزِ پربار) + نمودارِ روند + همبستگی + دستیارِ AI که الگوها را **غیرمستقیم** توضیح می‌دهد.

---

## ۳) قیدِ محرمانگیِ چرخه (سخت)

چرخه‌ی بدن می‌تواند با رضایتِ کاربر (`cycle_consent_energy`) انرژی را تغذیه کند. در صفحه‌ی انرژی:
- **هیچ اشاره‌ی مستقیمی به قاعدگی/چرخه/پریود نشود** — نه در توضیحِ موتور، نه در دستیار.
- اگر تعدیلی از چرخه می‌آید، فقط از مسیرِ `CycleConsentBridge` و با لحنِ غیرمستقیم («بر اساس ریتمِ بدنی‌ات این روزها انرژی کمی پایین‌تره»).
- برای کاربرِ مرد یا وقتی رضایت خاموش است، **هیچ ردّی** دیده نشود.

---

## ۴) معماریِ داده

- **`energy_logs`** (موجود: id, energyLevel[HIGH/MEDIUM/LOW], source, note, loggedAt) — **دست‌نخورده** (موتور و RIE به آن وابسته‌اند). انرژی این‌جا می‌ماند.
- **`mood_logs`** (جدید) — حال به‌عنوان محورِ مستقل:
  `id, mood TEXT, valence INTEGER(1..5), note, loggedAt`.
  `mood` = کلیدِ احساس (CALM/HAPPY/ANXIOUS/SAD/ANGRY/TIRED/EXCITED/...)، `valence` = خوشایندیِ ۱..۵.
- **ثبتِ سریع** هر دو را می‌نویسد (انرژی→`energy_logs`، حال→`mood_logs`)؛ کاربر می‌تواند فقط یکی را هم ثبت کند.
- **`daily_checkins`/`daily_reflections`** دست‌نخورده (چک‌اینِ صبح و بازتابِ عصر سرِ جایشان).
- **تنظیمات:** `module_energy_enabled` (جدید، پیش‌فرض false — فقط صفحه را گیت می‌کند؛ موتور و چک‌اینِ صبح مستقل کار می‌کنند).

---

## ۵) موتورها

- **`EnergyAnalyticsEngine`** (موجود) — بدونِ تغییر؛ منبعِ انرژیِ پویا + اوج/خستگی/روزِ پربار.
- **`mood_engine.dart`** (جدید، `CachedEngine`) — روندِ حال، احساسِ غالب، حالِ هر بازه/روز، و **همبستگیِ حال↔انرژی** (جفت‌کردنِ لاگ‌ها بر اساس مجاورتِ زمانی، همبستگیِ ساده و صادق، بدونِ ادعای اضافه).

---

## ۶) ساختار صفحه (EnergyMoodScreen)

**هیرو:** انرژیِ پویای فعلی (٪ بزرگ) + چیپ‌های «چرا این عدد؟» (توضیحِ موتور) + حالِ فعلی. دکمه‌ی شناورِ **«الان چطورم؟»**.

بخش‌ها/تب‌ها:
1. **امروز** — منحنیِ انرژیِ درون‌روزی (از لاگ‌ها) + لاگ‌های حالِ امروز + وضعیتِ کنونی با توضیح.
2. **الگوها** — اوجِ کارایی، روزِ پربار، بازه‌ی خستگی (از موتور، بالاخره به کاربر نشان داده می‌شوند) با لحنِ تشویقی.
3. **روند** — انرژی و حال در چند هفته (خطی/میله‌ای) + احساسِ غالب + بینشِ همبستگی.
4. **دستیار AI** (آخر) — توضیحِ غیرمستقیمِ الگوها + پیشنهادِ ملایم («بازه‌ی خستگی‌ت معمولاً عصره؛ کارهای سبک‌تر اون موقع بذار»). قیدِ چرخه رعایت شود.

**ثبتِ سریع:** انتخابِ سطحِ انرژی (۳ سطح یا اسلایدر) + احساس (شبکه‌ی ایموجی/واژه) → نوشتن در دو جدول + به‌روزرسانیِ `default_energy_level`.

---

## ۷) فعال‌سازی (هاب)

کاشیِ «انرژی و حالت روحی» از «به‌زودی» به الگوی استانداردِ ماژول (مثل کنکور) → `module_energy_enabled`. موتور و چک‌اینِ صبح مستقل‌اند، پس فعال/غیرفعال‌بودنِ صفحه به RIE آسیب نمی‌زند.

---

## ۸) لحن و اصول

- تشویقی و بدونِ قضاوت؛ حالِ بد هرگز «اشتباه» نیست.
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی.
- توضیحِ علّی همیشه شفاف («چرا این عدد؟») — جعبه‌ی سیاه نباشد.
- قیدِ چرخه (بخش ۳) هرگز نقض نشود.

---

## ۹) ترتیبِ ساخت (خلاصه)

E1 مهاجرت (`mood_logs` + `module_energy_enabled`) → E2 مدل‌ها → E3 موتورِ حال + همبستگی → E4 ثبتِ سریع → E5 هیرو → E6 امروز (منحنی) → E7 الگوها → E8 روند+همبستگی → E9 مونتاژ + هاب → E10 دستیار AI → E11 پرداخت + قیدِ چرخه + تست.
