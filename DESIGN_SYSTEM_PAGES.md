# 🏛 سند طراحی صفحات اختصاصی «سیستم‌های من» (System Pages Master Design)

> نویسنده: Claude (معمار). هدف: طراحی کاملِ تک‌تکِ صفحات سیستم‌ها بر اساس داده‌ی واقعی موجود در دیتابیس.
> این سند مرجعِ مادر است؛ برای هر صفحه بعداً یک پرامپت Gemini جدا نوشته می‌شود.
> **اصل مشترک:** ورزش/عبادت/دوره/کنکور/دارو/خواب/اهداف فقط از صفحه‌ی اختصاصی خودشان مدیریت می‌شوند، نه از فلوی «ثبت جدید».

---

## 📊 وضعیت فعلی صفحات (حسابرسی)
| سیستم | وضعیت | فایل |
|-------|-------|------|
| چرخه بدن | ⚠️ ساخته ولی مشکل‌دار | `cycle_harmony_screen.dart` |
| عبادت | ❌ فقط شیت فصول، ناقص | `worship_seasons_sheet.dart` |
| کنکور | ⚠️ ساخته ولی ناقص | `konkur_dashboard_screen.dart` |
| دستیار | ✅ کامل | `assistant_chat_screen.dart` |
| **درمان/دارو** | ❌ stub «به‌زودی» | — |
| **خواب و بیداری** | ❌ stub | — |
| **اهداف و برنامه‌ها** | ❌ stub | — |
| **دوره‌های آموزشی** | ❌ stub | — |
| **انرژی و حال روحی** | ❌ stub | — |

## 🧱 الگوی صفحه‌ی استاندارد سیستم (مشترک همه)
هر صفحه‌ی سیستم این اسکلت را دارد (برای انسجام بصری):
1. **هدر بزرگ** با عنوان سیستم + دکمه‌ی بازگشت + (اختیاری) دکمه‌ی تنظیمات سیستم.
2. **کارت خلاصه‌ی وضعیت (Hero)** — مهم‌ترین عدد/حالت آن سیستم.
3. **بخش اقدام سریع** — دکمه‌(های) افزودن مخصوص همان سیستم.
4. **لیست/محتوای اصلی** — رکوردهای آن سیستم.
5. **بخش تنظیمات/پیشرفته** (تاشو).
> توکن‌های بصری از `RitmoTheme`. RTL، فارسی، ارقام فارسی، حداقل لمسی ۴۸dp، اصل «بدون سرزنش» (قرمز فقط برای ایمنی دارو).

---

# 1) 💊 صفحه‌ی «درمان» (Medicine) — نام پیشنهادی به‌جای «دارو»
**نام:** «درمان» یا «سلامت دارویی». (مالک تأیید کند.)
**داده‌ی موجود:** `routines` با دسته‌ی `MEDICAL` · `prn_logs (routineId, takenAt, dosage)` · موتور `medical_engine.dart` (هشدار اتمام/تداخل).
**اجزای صفحه:**
- **Hero:** «داروهای امروز» — چند دوز مانده، دوز بعدی کِی.
- **لیست داروها:** هر دارو با نام، دوز، زمان‌بندی، **موجودی باقی‌مانده** (نوار پیشرفت)، و وضعیت (مصرف‌شده/مانده).
- **دکمه‌ی افزودن دارو:** فرم مخصوص: نام، دوز، نوع زمان‌بندی (ثابت/PRN در صورت نیاز)، **حداقل فاصله‌ی PRN**، **سقف روزانه**، **موجودی اولیه**، آستانه‌ی هشدار اتمام.
- **ثبت مصرف:** دکمه‌ی «مصرف کردم» → ثبت در `prn_logs` + کاهش موجودی. برای PRN، اگر فاصله رعایت نشده هشدار.
- **هشدارهای بحرانی:** داروی رو به اتمام / تداخل (از `medical_engine`). **تنها جایی که قرمز مجاز است.**
- **تاریخچه‌ی مصرف:** نمودار/لیست از `prn_logs`.
**شکاف فنی:** ستون موجودی/سقف/آستانه ممکن است روی `routines` نباشد → بررسی و در صورت نبود، مهاجرت.

# 2) 🕌 صفحه‌ی «عبادت» (Worship) — بازطراحی کامل
**داده‌ی موجود:** `prayer_times_cache` (fajr/sunrise/dhuhr/asr/maghrib/isha/midnightShari + ihtiyat) · `iran_cities` (مختصات) · `worship_debts` · `worship_seasons` · `prayer_time_provider.dart`.
**اجزای صفحه (طبق خواسته‌ی مالک):**
- **انتخاب شهر:** انتخابگر شهر از `iran_cities` (با جست‌وجو)، ذخیره در `app_settings`. اوقات بر همین مبنا محاسبه می‌شود.
- **کارت اوقات شرعی امروز:** نمایش فجر، طلوع، ظهر، عصر، مغرب، عشا، نیمه‌شب شرعی + شمارش معکوس تا نماز بعدی.
- **بخش نمازهای واجب:** فهرست (صبح، ظهر، عصر، مغرب، عشا) با **تیک یادآوری** — کاربر هر نماز را تیک بزند تا یادآورش (مبتنی بر وقت شرعی + آفست/احتیاط) فعال شود.
- **بخش مستحبات:** نماز شب، نوافل، تعقیبات و... — قابل فعال‌سازی با یادآوری دلخواه.
- **بخش قرآن و ذکر:** هدف روزانه‌ی قرآن (مثلاً X آیه/صفحه)، اذکار روزانه (تسبیحات، صلوات) با شمارنده و یادآوری.
- **بدهی‌های عبادی:** از `worship_debts` (قضا نماز/روزه) با هدف روزانه و پیشرفت.
- **فصول عبادی:** `worship_seasons` (رمضان، اعتکاف) — همان شیت موجود، یکپارچه در این صفحه.
**معماری:** یادآوری‌ها از طریق سرویس آلارم موجود + اوقات شرعی. وزن فصول روی اولویت روتین‌ها (RIE) از قبل هست.
**شکاف:** جدول مخصوص اهداف قرآن/ذکر و تیک نمازها لازم است (احتمالاً `worship_practices` جدید). در پرامپت لحاظ شود.

# 3) 🌙 صفحه‌ی «خواب و بیداری» (Sleep)
**داده‌ی موجود:** `bedtime_diagnostics (date, reason, note)` · مرتبط با موتور پیشروی (WS-1، حالت `TIME_SHIFT`).
**اجزای صفحه:**
- **Hero:** هدف خواب فعلی (ساعت هدف) + ساعت فعلی خواب + پیشرفت شیفت تدریجی.
- **تنظیم هدف خواب:** ساعت خواب فعلی → ساعت هدف، گام شیفت (۱۵دق)، هر چند روز یک‌بار. (همان `TIME_SHIFT` موتور پیشروی.)
- **ثبت خواب دیشب:** ساعت خواب/بیداری واقعی.
- **«چرا دیر خوابیدم؟»:** ثبت در `bedtime_diagnostics` (علت + یادداشت).
- **روند:** نمودار هفتگی ساعت خواب و نزدیک‌شدن به هدف.
**وابستگی:** به موتور پیشروی (`AGENT_PROMPT_VISION.md` WS-1) وصل است. ترتیب: اول پیشروی، بعد این صفحه.

# 4) 🎯 صفحه‌ی «اهداف و برنامه‌ها» (Goals)
**داده‌ی موجود:** `goals (parentGoalId, goalType, targetDate, status)` · `goal_steps (scheduledDate, linkedRoutineId)` — سلسله‌مراتب آماده.
**اجزای صفحه:**
- **Hero:** هدف فعال اصلی + درصد پیشرفت.
- **نمای درختی:** سالانه → ماهانه → هفتگی → روزانه (باز/بسته‌شونده).
- **افزودن هدف:** انتخاب سطح (`ANNUAL/MONTHLY/WEEKLY/DAILY`) + والد.
- **خرد کردن با AI:** دکمه → پیشنهاد سلسله‌مراتب → پیش‌نمایش ویرایش‌پذیر → ذخیره (WS-3).
- **قدم امروز:** قدم‌های `scheduledDate==today` (که روی داشبورد هم بازتاب دارد).
- **اتصال به روال:** هر قدم می‌تواند به یک روال (`linkedRoutineId`) وصل شود.
**وابستگی:** WS-2/WS-3 از `AGENT_PROMPT_VISION.md`. این صفحه عملاً «خانه‌ی» آن کارهاست.

# 5) 📚 صفحه‌ی «دوره‌های آموزشی» (Courses)
**داده‌ی موجود:** `courses (totalSessions, sessionDurationMinutes, activityType, energyRule)` · `course_sessions (sessionNumber, plannedDate, completionStatus, actualDurationMinutes)`.
**اجزای صفحه:**
- **Hero:** دوره‌ی فعال + جلسات انجام‌شده/کل + جلسه‌ی بعدی.
- **لیست دوره‌ها:** هر دوره با نوار پیشرفت جلسات.
- **افزودن دوره:** نام، تعداد کل جلسات، مدت هر جلسه، نوع فعالیت، قانون انرژی.
- **مدیریت جلسات:** علامت‌گذاری جلسه‌ی انجام‌شده + مدت واقعی + یادداشت.
- **زمان‌بندی:** برنامه‌ریزی جلسات روی تاریخ‌ها.

# 6) ⚡ صفحه‌ی «انرژی و حال روحی» (Energy & Mood)
**داده‌ی موجود:** `energy_logs (energyLevel, source, note, loggedAt)` · موتور `energy_analytics_engine.dart`.
**اجزای صفحه:**
- **Hero:** انرژی فعلی + روند امروز.
- **ثبت سریع انرژی/حال:** انتخابگر سطح (بصری) + یادداشت.
- **تحلیل‌ها:** پنجره‌ی اوج عملکرد، خسته‌کننده‌ترین بازه، پربازده‌ترین روز هفته (از موتور موجود، از طریق EngineBus).
- **روند:** نمودار انرژی هفتگی/ماهانه.

---

# 7) 🌸 بازطراحی «چرخه بدن» (Cycle) — رفع مشکلات
**وضعیت:** ساخته شده ولی مالک می‌گوید «خوب نیست و مشکل دارد».
**داده:** `cycle_logs (cycleStartDate, phase, flowLevel, symptomsJson, suppressedPrayer, fastDebtCreated)`.
**اقدام:** نیازمند **حسابرسی مشکلات فعلی** است قبل از بازطراحی. (در پرامپت مخصوص، اول لیست باگ‌ها استخراج شود.) محورها: صحت پیش‌بینی فاز، یکپارچگی با عبادت (`suppressedPrayer`/`fastDebtCreated`)، ثبت/ویرایش تاریخ‌ها، تله‌متری.

# 8) 📝 بازطراحی «کنکور» (Konkur) — تکمیل
**وضعیت:** ساخته ولی «درست ساخته نشده».
**داده:** `konkur_subjects` · `konkur_topics` (درختی با `parentTopicId`، `studyTargetMinutes`) · `konkur_mock_exams` · `konkur_mock_exam_results (percentage, correct/wrong/empty)`.
**اقدام:** نیازمند حسابرسی. محورها: مدیریت درختی مباحث، ثبت آزمون آزمایشی و درصدها، میانگین درصد per-subject، روند آزمون‌ها، اتصال زمان مطالعه به مباحث.

---

## 🗺 اولویت‌بندی پیشنهادی (توالی ساخت)
چون بعضی صفحات به موتورهای دیگر وابسته‌اند:

| ترتیب | صفحه | دلیل |
|------|------|------|
| ۱ | **درمان/دارو** | مستقل، داده آماده، موتور آماده، برد سریع |
| ۲ | **عبادت** | داده‌ی اوقات شرعی کامل آماده؛ خواسته‌ی صریح مالک |
| ۳ | **انرژی و حال** | مستقل، موتور آماده، کوچک |
| ۴ | **دوره‌ها** | مستقل، داده آماده |
| ۵ | **اهداف** | بعد از WS-2/WS-3 موتور پیشروی |
| ۶ | **خواب** | بعد از WS-1 (موتور پیشروی TIME_SHIFT) |
| ۷ | **چرخه بدن** | بازطراحی، نیازمند حسابرسی باگ |
| ۸ | **کنکور** | تکمیل، نیازمند حسابرسی |

## ❓ نیازمند تصمیم مالک قبل از پرامپت‌نویسی
1. نام نهایی صفحه‌ی دارو: «درمان»، «سلامت دارویی»، یا چیز دیگر؟
2. صفحه‌ی عبادت: آیا اهداف قرآن/ذکر و تیک نمازها را در یک جدول جدید (`worship_practices`) نگه داریم؟ (پیشنهاد معمار: بله)
3. از کدام صفحه شروع کنیم؟ (پیشنهاد: درمان → عبادت)
4. آیا صفحات وابسته (خواب/اهداف) را بعد از تکمیل موتور پیشروی بسازیم یا موازی؟
