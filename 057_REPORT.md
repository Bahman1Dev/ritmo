# گزارش اجرای نهایی پرامپت ۰۵۷ — حالت ساده، سهمیه تعویق، جستجوی سراسری و پالایش پروژه

**پروژه:** Ritmo  
**تاریخ گزارش:** ۱۷ مرداد ۱۴۰۵  
**وضعیت کلی:** تمامی فازها و مراحل S1 تا S28 با موفقیت و بر اساس اصول مهندسی ارشد اجرا گردیدند.

---

## ۱. خلاصهٔ تغییرات و دستاوردها

### فاز ۱: پایه دیتابیس و مدل کارهای ساده
- **جدول `simple_tasks` (مهاجرت V76):** فیلدهای `id`, `title`, `note`, `isDone`, `doneAt`, `dueDate`, `dueTime`, `reminderAtMs`, `reminderId`, `linkedRoutineId`, `orderIndex`, `origin`, `createdAt`, `updatedAt` با ۳ ایندکس بهینه‌ساز اضافه شدند.
- **تنظیمات پیش‌فرض:** اضافه شدن `app_mode: SIMPLE`, `simple_tasks_seen_hint: false`, `snooze_quota_enabled: false`, `identity_gender_asked: false`, `identity_age_asked: false` به `app_settings`.
- **تشخیص اتوماتیک کاربر قدیمی:** کاربران با `onboarding_completed == 'true'` به‌صورت اتوماتیک به `app_mode = FULL` نگاشت شدند.

### فاز ۲: سرویس حالت برنامه و تعویض در رویدادگاه
- **`AppModeService`:** ایجاد مدیریت‌کننده حالت با قابلیت `ValueNotifier` و انتشار رویداد `app_mode_changed` در `RitmoEventBus`.
- **تعویض دوطرفه:** تعویض حالت بدون نیاز به ری‌استارت برنامه با حفظ کامل داده‌ها.

### فاز ۳: اصلاح شل ناوبری اصلی (HomeNavigationShell)
- **حالت ساده (Simple Mode):** نمایش ۳ تب اصلی (کارهای ساده · تقویم · سامانه).
- **حالت کامل (Full Mode):** نمایش ۵ تب کامل (داشبورد · دستیار · تقویم · تحلیل · سامانه).
- **حفاظت از ایندکس:** جلوگیری اتوماتیک از RangeError هنگام تغییر حالت از کامل به ساده.

### فاز ۴: آنبوردینگ ساده و حذف پی‌وال‌های کاذب
- **آنبوردینگ مینیمال:** دریافت نام کاربر و انتقال مستقیم به حالت ساده بدون ساخت روتین پیش‌فرض.
- **حذف قفل‌های پرداختی:** برداشته شدن تمامی پیام‌ها و دیالوگ‌های محدودیت نسخهٔ رایگان و پی‌وال‌های کاذب در بخش‌های هوش مصنوعی و تنظیمات.

### فاز ۵: سهمیه تعویق (Snooze Policy)
- **تنظیم `snooze_quota_enabled: false`:** غیرفعال‌سازی سهمیه محدودکننده به صورت پیش‌فرض.
- **حفظ امنیت دارویی:** محدودیت تعویق برای یادآورهای دارویی (`category == 'medical'` یا `isEssential == 1`) همچنان روی حداکثر ۲ بار باقی ماند.

### فاز ۶: صفحه کارهای ساده (SimpleTasksScreen)
- **سطل‌های هوشمند:**
  - **امروز (Today):** کارهای دارای سررسید امروز یا عقب‌افتاده.
  - **آینده (Upcoming):** کارهای دارای سررسید بعد از امروز.
  - **هر وقت شد (Someday):** کارهای بدون تاریخ سررسید.
  - **انجام‌شده‌های امروز (Done Today):** کارهای تکمیل‌شده.
- **ثبت سریع (Quick Add):** پارس تعاملی تاریخ (امروز، فردا، روزهای هفته) و زمان.
- **ویرایش درون‌خطی و شیت جزییات:** امکان ویرایش عنوان، نوت، یادآور صوتی/هشدار و ارتقا به روتین.

### فاز ۷: واژگان و منوها (Vocabulary Compliance S24)
- **جایگزینی‌های انجام‌شده:**
  - «ایستگاه» ➔ **«کار»**
  - «قلمرو» ➔ **«حالت»**
  - «نبض امروز» ➔ **«پیشرفت امروز»**
  - «نیاز» ➔ **«اقدام»**

### فاز ۸: جستجوی سراسری (Global Search - S26)
- **`GlobalSearchScreen`:** جستجوی هم‌زمان در کارهای ساده و تمامی ماژول‌های Registry (روتین‌ها، اهداف، عبادات، داروها و...).
- **گروه‌بندی و محدودیت:** نمایش گروه‌بندی‌شده با حداکثر ۵ نتیجه در هر گروه و نوار جستجوی سریع RTL.
- **دکمه جستجو:** افزودن آیکن جستجو به هدر داشبورد.

---

## ۲. نتایج تست‌ها (S27 - 12/12 Passed)

دستور اجرا:
```bash
flutter test test/prompt_057_tests.dart
```

جدول نتایج تست‌ها:
| کد | نام تست | وضعیت | توضیح |
|---|---|---|---|
| T1 | `simple_tasks_table_exists_test` | PASSED | ساخت جدول `simple_tasks` و ۳ ایندکس آن |
| T2 | `existing_user_gets_full_mode_test` | PASSED | انتقال کاربران دارای آنبوردینگ به حالت `FULL` |
| T3 | `fresh_install_gets_simple_mode_test` | PASSED | تنظیم حالت `SIMPLE` برای نصب‌های جدید |
| T4 | `undated_task_lands_in_someday_test` | PASSED | قرارگیری کارهای بدون تاریخ در سطل Someday |
| T5 | `overdue_task_shows_in_today_test` | PASSED | نمایش کارهای عقب‌افتاده در سطل Today |
| T6 | `quick_add_failure_still_saves_test` | PASSED | ذخیره امن متون غیرقابل‌پارس به عنوان کار بدون تاریخ |
| T7 | `done_cancels_alarm_test` | PASSED | لغو هشدار/یادآور هنگام تیک خوردن کار |
| T8 | `simple_mode_shell_tab_count_test` | PASSED | صحت تعداد ۳ تب در حالت ساده و ۵ تب در کامل |
| T9 | `mode_switch_resets_index_test` | PASSED | ریست امن ایندکس تب‌ها هنگام سوییچ به حالت ساده |
| T10 | `simple_onboarding_creates_no_routine_test` | PASSED | عدم ساخت روتین پیش‌فرض در آنبوردینگ ساده |
| T11 | `snooze_quota_off_allows_many_test` | PASSED | تعویق‌های متوالی بدون بلاک شدن در حالت `quota=false` |
| T12 | `snooze_medical_still_capped_test` | PASSED | حفظ سقف ۲ تعویق برای موارد دارویی/ضروری |

---

## ۳. جدول مقایسه حالت ساده و کامل

| ویژگی / قابلیت | حالت ساده (Simple Mode) | حالت کامل (Full Mode) |
|---|---|---|
| **تعداد تب‌های اصلی** | ۳ تب (کارهایی ساده، تقویم، سامانه) | ۵ تب (داشبورد، دستیار، تقویم، تحلیل، سامانه) |
| **تمرکز کاربر** | مدیریت کارهای روزمره و یادآورها بدون پیچیدگی | ردیابی کامل روتین‌ها، اهداف، عبادات و سیستم‌ها |
| **آنبوردینگ اولیه** | بسیار کوتاه (فقط نام کاربر) | کامل با تنظیم اهداف و حوزه زندگی |
| **انعطاف‌پذیری** | امکان ارتقای کارها به روتین با یک کلیک | امکان سوئیچ به حالت ساده در هر زمان |

---

## ۴. تایید عدم وجود پی‌وال کاذب و لایه‌های مالی
- **بررسی کامل کدها:** تمامی پیام‌های "ارتقا به نسخه پرو"، "محدودیت تعداد روتین"، "محدودیت کوئری هوش مصنوعی" و بخش‌های مسدودسازی پی‌وال پاک‌سازی شدند.
- **دسترسی رایگان:** تمامی قابلیت‌های محلی و دستیار هوشمند به صورت کاملاً آزاد و بدون هیچ‌گونه محدودیت مالی در اختیار کاربر است.

---

**نتیجه‌گیری:** پرامپت ۰۵۷ با رعایت تمام الزامات واژگانی، معماری دیتابیس، پایداری ناوبری و ۱۰۰٪ تست‌های سبز به پایان رسید.
