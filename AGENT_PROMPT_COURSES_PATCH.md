# 🔧 پرامپت اصلاحی — ادامه‌ی صفحه‌ی «دوره‌های آموزشی»

> این یک **به‌روزرسانی** روی `AGENT_PROMPT_COURSES.md` است. کارِ قبلی را دور نریز؛ فقط اصلاحات زیر را اعمال کن. این تصمیم‌ها **قطعی و غیرقابل‌نقض** هستند و بر هر چیزی که قبلاً نوشته شده اولویت دارند.

---

## ⛔️ ۵ اصلاح قطعی

### ۱) اتصال به Goals فقط یک‌طرفه است (Course → Goal)
- تکمیل یک جلسه یا حتی تکمیل **کل دوره** هرگز نباید `goal_steps.isCompleted` را خودکار `1` کند.
- تنها کار مجاز: **نمایش/تغذیه‌ی درصد پیشرفت دوره** در UI هدف.
- مثال خطری که باید جلوگیری شود: هدف «قبولی ارشد» / قدم «مطالعه فصل ۵» — اتمام یک جلسه نباید آن قدم را ببندد.
- ⚠️ اگر در C9 کدی برای complete کردن خودکار `goal_step` نوشته‌ای، **حذفش کن**.

### ۲) AI هرگز مستقیم در DB نمی‌نویسد
- مسیر اجباری برای هر خروجی AI:
  ```
  AI Suggestion → Preview → User Edit → Save (با تأیید صریح)
  ```
- «تقسیم دوره به واحد» باید یک **لیست قابل‌ویرایش** بدهد (کاربر هر عنوان را تغییر/حذف کند) — هرگز مستقیم در `sessionTitle` ذخیره نشود.
- «پیشنهاد ریتم» فقط فیلدهای فرم را **پیش‌پر** کند؛ scheduler را خودکار اجرا نکند.

### ۳) ستون `isAdaptive` در schema اضافه شود (فعلاً بلااستفاده)
در مهاجرت `courses` این ستون را هم اضافه کن:
```sql
ALTER TABLE courses ADD COLUMN isAdaptive INTEGER NOT NULL DEFAULT 0;
```
(در `_createDB` هم در تعریف اولیه‌ی `courses` بگذار.) فعلاً هیچ منطقی رویش سوار نکن — فقط schema آماده باشد.

### ۴) منبع Completion دوره‌ها = فقط `course_sessions.completionStatus`
- هرگز در `routine_completions` (FK به `routines`) یا `daily_rhythm` (تجمیع روتین) برای دوره‌ها رکورد نزن.
- آمار دوره‌ها (استریک، ساعت مطالعه، عقب‌افتادگی) کاملاً مستقل محاسبه می‌شود.

### ۵) ترتیب اجرا: AI آخر
ترتیب درست:
```
PASS 0 → C1 → C2 → C3 → C4 → C12(اسکلت) → C10 → C9 → C6 → C8 → C7 → C11 → C5 → C14 → C13(AI، آخر)
```
AI را از وسط بردار؛ اول قابلیت اصلی کامل و آزموده کار کند، بعد AI رویش سوار شود.

---

## 🔨 اصلاح فنی: گیر یادآوری دوره (`routineId NOT NULL`)

**تصمیم مالک محصول: راهکار (الف) — `routineId` را nullable کن.**

جدول `pending_reminders` از قبل ستون `courseSessionId` دارد، اما `routineId` با `NOT NULL` تعریف شده و مانع ثبت یادآوری برای جلسه‌ی دوره می‌شود (دوره روتین ندارد).

**اقدام (این را به مهاجرت C1 / نسخه ۱۳ اضافه کن):**
- چون SQLite دستور مستقیم برای حذف `NOT NULL` ندارد، جدول `pending_reminders` را با **الگوی بازسازی جدول** مهاجرت بده:
  1. `CREATE TABLE pending_reminders_new (...)` با همان ستون‌ها ولی `routineId TEXT` (بدون `NOT NULL`).
  2. `INSERT INTO pending_reminders_new SELECT ... FROM pending_reminders;`
  3. `DROP TABLE pending_reminders;`
  4. `ALTER TABLE pending_reminders_new RENAME TO pending_reminders;`
  5. ایندکس‌های قبلی (`index_pending_reminders_routineId`, `_scheduleId`, `_scheduledTime`) را دوباره بساز.
- همه‌ی FKها و ستون‌های دیگر دقیقاً مثل قبل بمانند (فقط `routineId` nullable می‌شود).
- در `_createDB` هم تعریف اولیه‌ی `pending_reminders` را به `routineId TEXT` (nullable) تغییر بده تا نصب تازه و ارتقا یکسان باشند.
- **قانون استفاده:** برای یادآوری جلسه‌ی دوره، `routineId = NULL` و `courseSessionId = <id>`. برای یادآوری روتین، برعکس. هر رکورد یادآوری دقیقاً یکی از این دو را پر می‌کند.

⚠️ این یک تغییر روی جدول مشترک و حساس است. بعد از مهاجرت، حتماً تست کن که یادآوری‌های روتینِ موجود سالم منتقل شده‌اند (تعداد رکوردها قبل/بعد برابر).

---

## 📋 خلاصه‌ی کاری که الان باید بکنی
1. مهاجرت C1 را به‌روز کن: افزودن `isAdaptive` + بازسازی `pending_reminders` با `routineId` nullable. (نسخه‌ی DB همان ۱۳.)
2. اگر C9 را قبلاً نوشته‌ای، complete خودکار `goal_step` را حذف کن.
3. C13 (AI) را تا انتهای کار به تعویق بینداز و الزام Preview→Edit→Save را رعایت کن.
4. بقیه‌ی تسک‌ها طبق `AGENT_PROMPT_COURSES.md` به‌روز شده.

**اگر هنوز روی C1 نرسیده‌ای:** فقط نسخه‌ی به‌روزِ مهاجرت را پیاده کن.
**اگر C1 را قبلاً پاس کرده‌ای:** یک مهاجرت اصلاحی (نسخه ۱۴) برای بازسازی `pending_reminders` بساز و گزارش بده — **اول توقف کن و بپرس**.

**بعد از مهاجرت، توقف کن و منتظر تأیید انسانی بمان.**
