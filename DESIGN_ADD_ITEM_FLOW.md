# 🧩 سند طراحی: بازطراحی بخش «افزودن» (Add Item Flow)

> نویسنده: Claude (معمار) · هدف: بازتعریف مفهومی و طراحی مجدد فلوی افزودن، بر اساس بازخورد مالک محصول.
> این سند **مبنای پرامپت Gemini** است؛ اول روی طراحی توافق کنیم، بعد پرامپت کدنویسی نوشته می‌شود.

---

## 🎯 بینش محوری (تغییر پارادایم)
بخش فعلی فقط «روتین» می‌سازد و یک **ویزارد ۵ مرحله‌ای** سنگین است. اما کاربر همیشه روتین نمی‌سازد؛ گاهی فقط یک **یادآوری**، یک **کار تک‌بار** (مثل Microsoft To Do / Todoist) یا **برنامه‌ریزی روز** می‌خواهد.

**تصمیم:** این بخش از «ساخت روتین» به یک **«نقطه‌ی ثبت عمومی» (Universal Add)** تبدیل می‌شود که چند **نوع آیتم** را پشتیبانی می‌کند.

### حذف از این بخش (مرزبندی روشن)
ورزش، عبادات، دوره‌ی آموزشی، کنکور، دارو و... **هرگز از اینجا اضافه نمی‌شوند**. هرکدام صفحه‌ی اختصاصی خود را در **«سیستم‌های من»** دارد با تنظیمات مخصوص (نسخه‌ی سبک/حداقلی، پیشروی، اوقات شرعی و...). این بخش فقط آیتم‌های **عمومی زندگی** را می‌سازد.

> پیامد: گزینه‌های «نسخه‌ی سبک/حداقلی» و دسته‌های تخصصی از فلوی عمومی **حذف** می‌شوند و به صفحات سیستم منتقل می‌گردند.

---

## 🏷 اصلاح نام‌گذاری (فارسی‌سازی)
کلمه‌ی «روتین» برای کل بخش نامناسب است. پیشنهادها:
| مفهوم | نام پیشنهادی فارسی |
|------|---------------------|
| کل بخش / تب افزودن | **«افزودن»** یا **«ثبت جدید»** |
| Routine | **«برنامه‌ی تکرارشونده»** / **«روال»** (به‌جای «روتین») |
| Reminder | **«یادآور»** |
| Task (تک‌بار) | **«کار»** / **«کار روز»** |
> نام نهایی را مالک محصول تأیید می‌کند؛ این سند از «روال / یادآور / کار» استفاده می‌کند.

---

## 📐 سه نوع آیتم (Item Types)
کاربر در ابتدا **نوع** را انتخاب می‌کند (یک تصمیم ساده)، سپس فرمِ متناسب همان نوع باز می‌شود:

### ۱) 🔁 روال (Routine) — کار تکرارشونده
- تکرار: روزانه / روزهای کاری / روزهای دلخواه / **هر N روز** / **هر N ساعت** ⭐ / ماهانه.
- دکمه‌های تعامل: فقط **«انجام»** و **«تعویق»** (مطابق خواسته‌ی مالک).
- پس از «انجام» → **مودال نیت/بازتاب** باز می‌شود (سؤال‌های پس از انجام؛ همان مکانیک موجود).

### ۲) 🔔 یادآور (Reminder) — فقط اعلان در زمان معین
- یک یا چند زمان مشخص؛ بدون مکانیک «انجام/تعویق» سنگین.
- ساده‌ترین نوع: نام + زمان + (اختیاری) تکرار.

### ۳) ✅ کار (Task) — تک‌بار، بدون تکرار
- مثل Microsoft To Do: نام + تاریخ/زمان (اختیاری) + اولویت. وقتی انجام شد، تمام.

---

## ⏱ شکاف فنی کشف‌شده: بازه‌ی ساعتی
`RecurrenceRule` فعلی فقط `intervalDays` دارد ([models.dart:198](lib/core/domain/models.dart#L198)). برای «هر ۸ ساعت / هر ۴۸ ساعت» باید فیلد **`intervalHours`** اضافه شود (و موتور `routine_occurrence_generator` آن را بفهمد). این یک پیش‌نیاز فنی برای خواسته‌ی شماست.

---

## 🎨 ساختار UI پیشنهادی (با Progressive Disclosure)

فلسفه: **تک‌صفحه + لایه‌ی پیشرفته‌ی اختیاری + افزودن سریع**. (نه ویزارد ۵ مرحله‌ای.)

```
┌─────────────────────────────────────────────┐
│ ← ثبت جدید                          [ذخیره]  │
│                                               │
│  ┌─ افزودن سریع (زبان طبیعی) ──────────────┐ │
│  │ 🪄 «ورزش هر روز ۶ عصر» ...              │ │  ← پارس خودکار
│  └──────────────────────────────────────────┘ │
│                                               │
│  نوع:  [ 🔁 روال ] [ 🔔 یادآور ] [ ✅ کار ] │  ← سگمنت
│  ─────────────────────────────────────────── │
│  عنوان: [_________________________]           │  لایه ۱
│  دسته:  [ 💼 کار ▾ ]                          │  (همیشه دیده)
│  زمان:  [ ⏰ ۰۸:۰۰ ]                          │
│  تکرار: ( روزانه ) ( هر N روز ) ( هر N ساعت ) │  ← فقط برای «روال»
│  ─────────────────────────────────────────── │
│  ▸ گزینه‌های بیشتر (تاشده)                    │  لایه ۲
│     ├ حیاتی؟ · اولویت · بازه‌ی ممنوع/ترجیحی   │
│     └ وابستگی به روال دیگر (habit stacking)   │  لایه ۳
└─────────────────────────────────────────────┘
```

**پیش‌فرض‌های هوشمند:** نوع=روال، تکرار=روزانه، دسته=آخرین انتخاب. کاربر می‌تواند فقط عنوان را بزند و «ذخیره».

---

## 🧠 افزودن سریع با زبان طبیعی
نوار بالای فرم: کاربر تایپ می‌کند «حموم هر ۴۸ ساعت» یا «دارو خوردن ساعت ۹ شب». سیستم:
- **فاز ۱ (بدون AI):** پارسر سبک محلی برای الگوهای پرتکرار (ساعت، «هر N روز/ساعت»، «هر روز»، روزهای هفته).
- **فاز ۲ (با AI):** متن مبهم → `AiGateway` → خروجی JSON ساختاریافته‌ی فیلدها → پیش‌پرکردن فرم برای تأیید کاربر.
> همیشه نتیجه در فرم نمایش داده می‌شود تا کاربر **قبل از ذخیره تأیید/اصلاح** کند (هرگز ذخیره‌ی کور).

---

## 🔄 مکانیک پس از «انجام»
وقتی دکمه‌ی **انجام** زده شد، یک **مودال** باز می‌شود (نه ثبت بی‌صدا) که موارد توافق‌شده‌ی قبلی را می‌پرسد (مدت واقعی، حس/بازتاب و...). این رفتار باید برای نوع «روال» فعال و برای «یادآور/کار» ساده‌تر باشد.

---

## ✅ تصمیم‌های قطعی (قفل‌شده)
1. **نام‌ها تأیید شد:** «روال / یادآور / کار». نام تب/بخش: «ثبت جدید».
2. **مدل داده = تک‌جدول.** سه نوع در همان جدول `routines` با ستون جدید `itemType` (`ROUTINE` / `REMINDER` / `TASK`) نگه‌داری می‌شوند.
   - **دلیل معماری:** کل زیرساخت موجود (موتور occurrence، `routine_completions`، آلارم، `RitmoEvents`) روی `routines` بناشده؛ جدول جدا = بازنویسی همه‌ی این‌ها + ریسک مهاجرت بالا. تک‌جدول، حداقل تغییر با حداکثر سازگاری است.
   - نوع `TASK` صرفاً `itemType='TASK'` با تکرار خالی (تک‌بار)؛ نوع `REMINDER` با `reminderTimes` پرشده و بدون مکانیک کامل انجام.
3. **افزودن سریع = هر دو، با اولویت AI.** اول `AiGateway` صدا زده می‌شود؛ اگر AI پاسخ نداد/خطا داد/JSON نامعتبر بود، **به‌صورت خودکار** به پارسر محلی fallback شود. نتیجه همیشه در فرم برای تأیید کاربر نمایش داده می‌شود.
4. **دکمه‌ی «انجام» برای «کار» هم مودال را باز می‌کند** (یکنواختی تجربه؛ سؤال‌های پس از انجام برای هر سه نوع).

> این تصمیم‌ها مبنای `AGENT_PROMPT_ADD_FLOW.md` هستند.

---

## 📊 وضعیت پیاده‌سازی (Implementation Status) - خرداد ۱۴۰۵

تمامی تصمیمات و ملزومات فوق به طور کامل پیاده‌سازی و آزمایش شده‌اند:
1. **مهاجرت دیتابیس (A1):** اسکیما به نسخه ۱۰ ارتقا یافت و ستون `itemType` به همراه پشتیبانی از فیلد ساعتی `intervalHours` در کلاس‌های `Routine` و `RecurrenceRule` با حفظ سازگاری عقب‌گرد پیاده‌سازی شد.
2. **پشتیبانی بازه ساعتی (A2):** موتور محاسباتی تکرار در کلاس `RoutineOccurrenceGenerator` بروزرسانی شد تا به درستی رخدادهای دوره‌ای ساعتی (مانند هر ۸ ساعت یا ۴۸ ساعت) را پشتیبانی کند.
3. **رابط کاربری تک‌صفحه‌ای (A3 & A4):** رابط کاربری `routine_create_flow.dart` کاملاً بازنویسی شد و به صورت یک فرم منعطفِ لایه‌ای همراه با شیشه‌ریختی (Glassmorphism) و گزینه‌های پنهان شونده (Progressive Disclosure) پیاده‌سازی شد.
4. **افزودن سریع هوشمند (A7):** افزونه پردازش زبان طبیعی فارسی با موفقیت پیاده‌سازی شد؛ به طوری که در گام اول درخواست به متد مجزای `parseQuickAdd` در `AIGateway` ارسال شده و در صورت نبود دسترسی یا خطای پردازش، به طور خودکار به موتور پردازشگر محلی `QuickAddParser.parse` منتقل (Fallback) می‌شود. ارقام فارسی و عربی نیز به درستی برای پردازش استاندارد تبدیل می‌شوند.
5. **یکپارچگی تجربه پس از انجام (A6):** عملکردهای اتمام برای هر سه نوع آیتم روال، کار و یادآور به مودال نیت و بازخورد (`NiyyahSheet`) متصل شده و رفتارهای خاموش قبلی حذف گردیدند.
6. **تست‌ها و بهینه‌سازی (A8 & A9):** تمامی ۱۰۸ تست قبلی به انضمام ۶ تستِ جدید برای پردازشگر افزودن سریع و موتور دوره‌ای ساعتی بازنویسی شده و هر ۱۱۴ تست با موفقیت پاس شدند. تحلیل ایستای Flutter نیز بدون هیچ خطایی تایید شد.
