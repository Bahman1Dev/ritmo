# 🤖 پرامپت اجرایی — ویژگی‌های تکمیلی «چرخه بدن» — برای Gemini

> فایلِ خودبسنده. سیستمِ چرخه بدن از قبل ساخته شده (v2 ارتقایافته)؛ این مرحله **افزودنِ ۴ ویژگیِ جدید** است. کلِ صفِ D1 تا D7 را **یک‌سره تا آخر** اجرا کن؛ توقفِ میان‌راهی لازم نیست. در پایان یک‌بار اعتبارسنجی و گزارشِ نهایی بده.
> هدف: افزودنِ **نمودارِ خُلق‌وخو** + **دکمه فوریتِ درد (SOS)** + **حالتِ بارداری** + **تکمیلِ نوتیفیکیشنِ واقعی** — با حفظِ کاملِ محرمانگی و ساختارِ موجود.
> سندِ طراحی: `DESIGN_SYSTEM_CYCLE.md`. فایل‌های اصلی موجود: `lib/features/cycle/presentation/cycle_screen.dart`، `cycle_lock_gate.dart`، `lib/core/domain/engines/cycle_engine.dart`، `lib/core/utils/cycle_consent_bridge.dart`، `cycle_privacy_guard.dart`، `widgets/cycle_trends_section.dart`، `widgets/cycle_correlation_section.dart`.

## ⛔️ قواعد (یک‌بار)
- فارسی/RTL، `Vazirmatn`، ارقامِ فارسی، تاریخِ **شمسی** (`shamsi_date`؛ ذخیره ISO/epoch، نمایش شمسی). l10n جدید به `app_fa.arb`/`app_en.arb`.
- رنگ/اندازه هاردکد نکن؛ `RitmoTheme`/`context.colors`. رنگِ پایه: صورتی `#EC4899`.
- لحنِ آرام و محترمانه و بدونِ قضاوت.
- داده‌ی دیتابیس تستی است؛ ستون‌های جدید با `DEFAULT`.
- فقط فایل‌های مرتبطِ هر تسک. ابهامِ واقعی → بپرس.
- معماریِ موجودِ حریم/قفل را **نشکن:** `CyclePrivacyGuard`, `CycleLockGate`, رضایت‌ها، `module_cycle_enabled`. فقط اضافه/عمیق کن.

## 🔒 قیدِ محرمانگی (هرگز نقض نشود — باگِ بحرانی)
- **بیرون از این صفحه هیچ واژه‌ی صریحِ پریود/قاعدگی/چرخه** نوشته نشود.
- **باروری پنهان (قیدِ سخت):** هیچ پنجره‌ی بارور و هیچ تخمک‌گذاری در UI.
- درونِ این صفحه (پشتِ PIN، vault) محتوای صریح مجاز است؛ بیرون نه.
- نوتیفیکیشن‌ها باید **محرمانه** باشند (بدون واژه‌ی صریح روی lock screen).

## 📁 محیط (تأییدشده از کد)
- DB SQLite، نسخه‌ی فعلی را از `database_helper.dart` بخوان؛ مهاجرت = **فعلی+۱**.
- `cycle_periods (id, startDate, endDate, flowIntensity, isPredicted, note, createdAt, updatedAt)`.
- `cycle_day_logs (id, logDate UNIQUE, flowLevel, symptomsJson, mood, energyTag, note, createdAt, updatedAt)`.
- `cycle_engine.dart` (`CachedEngine`): خروجی شامل `currentPhase`, `dayOfCycle`, `dayOfPeriod`, `nextPeriodWindowStart/End`, `pmsWindowStart/End`, `regularityScore`, `trendPoints`, `isIrregular`.
- `CycleConsentBridge` — تنها کانالِ مجازِ تأثیر بیرونی.
- `CycleLockGate` — قفلِ PIN/بیومتریک. حفظ شود.
- تنظیمات موجود: `cycle_consent_reminders`, `cycle_consent_worship/energy/dashboard/sleep`.
- الگوی موتور: `CachedEngine` + `RitmoEngineBus`.
- **ثبتِ علائم روزانه موجود** (خطوط ~1677-1910 از cycle_screen.dart): flowLevel, mood, energyTag, symptomsJson (cramps/headache/bloating/backache/mood_swings), note.
- **توصیه‌های فازی موجود** (خطوط ~1938-1970): تمرینات/تغذیه/ذهن بر اساس فاز. حفظ شوند.
- **تب‌های موجود** در cycle_screen: `0=امروز`, `1=روندها`, `2=تقویم`, `3=تنظیمات`. ممکن است تب‌های بیشتری باشد — ساختار را از کد بخوان.

---

# 🗂 صفِ تسک‌ها (پشتِ‌سرِ‌هم)

**D1 — نمودارِ تغییراتِ خُلق‌وخو در طولِ چرخه** (`widgets/cycle_mood_chart.dart` — ویجتِ جدید)

هدف: نمودار بصری به کاربر نشان دهد در طولِ چرخه (روز ۱ تا آخر)، کدام روزها بیشترین اضطراب/افسردگی/انرژی بالا/آرامش را تجربه کرده.

پیاده‌سازی:
- از `cycle_day_logs` تمامِ لاگ‌های موجود را بخوان. برای هر لاگ، `dayOfCycle` آن روز را از `cycle_engine` یا از فاصله با آخرین `cycle_periods.startDate` محاسبه کن.
- mood‌ها (`HAPPY`, `NEUTRAL`, `SAD`, `ANXIOUS`, `ANGRY`, `ENERGETIC` — یا هرچه در کد موجود است) را به یک مقیاس عددی map کن (مثلاً ENERGETIC=5, HAPPY=4, NEUTRAL=3, SAD=2, ANXIOUS=1, ANGRY=0).
- بر اساسِ `dayOfCycle` گروه‌بندی کن و میانگین بگیر.
- یک نمودارِ خطی با `CustomPainter` (بدونِ پکیجِ جدید) رسم کن: محور X = روز چرخه (۱ تا طولِ متوسط)، محور Y = میانگینِ mood.
- نقاطِ بحرانی (بیشترین اضطراب، بیشترین انرژی) را با رنگ و برچسبِ فارسی مشخص کن.
- اگر داده کم بود (کمتر از ۵ لاگ): پیامِ «هنوز در حالِ یادگیریِ الگوی احساست هستم 🌸 با ثبتِ روزانه‌ی احوال، نمودار دقیق‌تر می‌شود.»
- **این ویجت را در بخشِ «روندها» (تبِ ۱) زیرِ نمودار طول/مدت اضافه کن.**

---

**D2 — دکمه فوریتِ درد (SOS Button)** (`widgets/cycle_sos_section.dart` — ویجتِ جدید)

هدف: یک دکمه/بخشِ دم‌دست که وقتی درد شدید است، با یک کلیک مستقیماً راهکارهای سریعِ تسکین درد را نشان دهد.

پیاده‌سازی:
- **دکمه FAB یا دکمه برجسته** در تبِ «امروز» (تب ۰)، قابل مشاهده فقط وقتی کاربر در فازِ `menstrual` یا `luteal`/PMS است.
- با زدنِ دکمه، یک **bottom sheet یا صفحه** باز شود با بخش‌های:

  **۲.۱) پوزیشن‌های تسکین درد رحم:**
  - هر کدام یک کارت با عنوان فارسی + توضیح مختصر (بدون عکس، فقط آیکن اموجی + متن):
    - 🧒 **پوزیشن کودک (Child's Pose):** «روی زانو بنشینید، بالاتنه را جلو ببرید و پیشانی را روی زمین بگذارید. دست‌ها را جلو بکشید. ۱ تا ۳ دقیقه نگه دارید. فشار از عضلات شکم و کمر کم می‌شود.»
    - 🐱 **پوزیشن گربه-گاو (Cat-Cow):** «چهار دست و پا، سر بالا + کمر گود (گاو) → سر پایین + کمر قوس (گربه). ۱۰ بار تکرار. گردش خون لگن بهتر می‌شود.»
    - 🦋 **پوزیشن پروانه (Butterfly):** «نشسته، کف پاها را به هم بچسبانید، زانوها را آرام بالا و پایین ببرید. عضلات لگن شل می‌شوند.»
    - 🌙 **پوزیشن جنینی (Fetal Position):** «به پهلو بخوابید، زانوها را به سینه نزدیک کنید. عضلات شکم ریلکس می‌شوند.»

  **۲.۲) راهکارهای سریعِ خانگی:**
  - 🫖 **دمنوش:** «دمنوش بابونه یا زنجبیل. ۱ قاشق‌چایخوری در آب جوش، ۱۰ دقیقه دم بکشد. ضد اسپاسم و آرام‌بخش.»
  - 🔥 **کمپرس گرم:** «یک کیسه آب‌گرم یا حوله گرم روی شکم یا کمر بگذارید. ۱۵-۲۰ دقیقه. گرما عضلات منقبض را شل می‌کند.»
  - 🫧 **ماساژ شکم:** «با حرکات دایره‌ای آرام، شکم را در جهت عقربه‌های ساعت ماساژ دهید. ۵ دقیقه.»
  - 🧘 **تنفس عمیق ۴-۷-۸:** «۴ ثانیه نفس بکشید، ۷ ثانیه نگه دارید، ۸ ثانیه بازدم. ۴ بار تکرار. سیستم عصبی آرام می‌شود.»

- طراحی: کارت‌ها با رنگِ صورتیِ ملایم، آیکن‌های بزرگ، فونتِ خوانا. لحنِ آرام و حمایتی.
- دکمه SOS: یک دکمه بزرگ و واضح با آیکن `CupertinoIcons.heart` یا اموجی 🆘 و متنِ «کمک فوری تسکین درد».

---

**D3 — مهاجرتِ داده برای حالتِ بارداری.** نسخه فعلی+۱. تنظیماتِ جدید با `INSERT OR IGNORE`:
```
cycle_pregnancy_mode = 'false'
cycle_pregnancy_start_date = ''
cycle_pregnancy_due_date = ''
```
هم در `_createDB` هم در تابعِ مهاجرت.

---

**D4 — حالتِ بارداری (Pregnancy Mode)** (`widgets/cycle_pregnancy_mode.dart` — ویجتِ جدید)

هدف: اگر کاربر باردار شد، وضعیتِ اپلیکیشن تغییر کند: شمارشِ پریود متوقف شود و به‌جای آن هفته‌های بارداری، رشد جنین و نکات خودمراقبتی نمایش داده شود.

پیاده‌سازی:

**۴.۱) فعال‌سازی:**
- در **تنظیمات** (تبِ تنظیمات) یک دکمه اضافه کن: «🤰 فعال‌سازی حالت بارداری».
- با زدن، دیالوگ تأیید: «آیا باردار هستید؟ با فعال‌سازی این حالت، پیگیری چرخه قاعدگی متوقف می‌شود و اطلاعات دوران بارداری نمایش داده می‌شود.»
- کاربر تاریخِ آخرین شروعِ قاعدگی (LMP) را وارد کند (date picker شمسی). از آن:
  - `pregnancy_start_date` = LMP
  - `pregnancy_due_date` = LMP + 280 روز
- تنظیم `cycle_pregnancy_mode = 'true'` ذخیره شود.

**۴.۲) نمایشِ حالت بارداری:**
- وقتی `cycle_pregnancy_mode == 'true'`:
  - **کل UIِ چرخه (orb، تقویم، روندها) مخفی شود.**
  - به‌جای آن، ویجتِ بارداری نمایش داده شود:

  **الف) هفته فعلی + شمارش معکوس:**
  - از `pregnancy_start_date` تا الان = هفته X (روز Y).
  - شمارش معکوس: «Z روز تا تاریخ تخمینی زایمان 🎀».
  - یک progress bar دایره‌ای (0-40 هفته) با رنگِ صورتی.

  **ب) رشد جنین (هفتگی):**
  - یک Map ایستا (hardcoded) با اطلاعات هفته ۱ تا ۴۲:
  ```dart
  final Map<int, Map<String, String>> pregnancyWeekData = {
    4: {'size': 'به اندازه یک دانه خشخاش', 'development': 'لانه‌گزینی جنین در رحم', 'tip': 'مصرف اسید فولیک را شروع کنید'},
    5: {'size': 'به اندازه یک دانه کنجد', 'development': 'قلب شروع به تپیدن می‌کند', 'tip': 'از مصرف کافئین زیاد خودداری کنید'},
    6: {'size': 'به اندازه یک دانه عدس', 'development': 'صورت در حال شکل‌گیری است', 'tip': 'استراحت کافی داشته باشید'},
    8: {'size': 'به اندازه یک تمشک', 'development': 'انگشتان دست شکل گرفته‌اند', 'tip': 'غذاهای سرشار از آهن مصرف کنید'},
    10: {'size': 'به اندازه یک زیتون', 'development': 'اندام‌های حیاتی تشکیل شده‌اند', 'tip': 'پیاده‌روی روزانه را فراموش نکنید'},
    12: {'size': 'به اندازه یک آلو', 'development': 'ناخن‌ها رشد می‌کنند', 'tip': 'ویزیت دوم پزشک (غربالگری اول)'},
    14: {'size': 'به اندازه یک لیمو', 'development': 'جنین می‌تواند اخم کند', 'tip': 'حالت تهوع معمولاً کاهش می‌یابد'},
    16: {'size': 'به اندازه یک آووکادو', 'development': 'جنین حرکات خود را حس می‌کند', 'tip': 'شاید اولین لگد را حس کنید!'},
    18: {'size': 'به اندازه یک فلفل دلمه‌ای', 'development': 'گوش‌ها کامل شده‌اند', 'tip': 'با جنین حرف بزنید، صدایتان را می‌شنود'},
    20: {'size': 'به اندازه یک موز', 'development': 'نیمه راه! جنسیت قابل تشخیص', 'tip': 'سونوگرافی آنومالی (غربالگری دوم)'},
    22: {'size': 'به اندازه یک بلال ذرت', 'development': 'ابروها و مژه‌ها رشد کرده‌اند', 'tip': 'ورزش‌های کگل را شروع کنید'},
    24: {'size': 'به اندازه یک بادمجان', 'development': 'ریه‌ها در حال بلوغ هستند', 'tip': 'تست قند بارداری انجام دهید'},
    26: {'size': 'به اندازه یک کلم بروکلی', 'development': 'چشم‌ها باز می‌شوند', 'tip': 'خواب به پهلوی چپ را عادت کنید'},
    28: {'size': 'به اندازه یک کدو حلوایی کوچک', 'development': 'مغز به‌سرعت رشد می‌کند', 'tip': 'شمارش حرکات جنین را شروع کنید'},
    30: {'size': 'به اندازه یک کاهو', 'development': 'جنین وضعیت سر پایین می‌گیرد', 'tip': 'کلاس آمادگی زایمان را در نظر بگیرید'},
    32: {'size': 'به اندازه یک نارگیل', 'development': 'ناخن‌های پا رشد کرده‌اند', 'tip': 'ساک بیمارستان را آماده کنید'},
    34: {'size': 'به اندازه یک طالبی', 'development': 'سیستم ایمنی فعال شده', 'tip': 'استراحت بیشتر و بالش بارداری'},
    36: {'size': 'به اندازه یک هندوانه کوچک', 'development': 'جنین تقریباً کامل است', 'tip': 'ویزیت‌ها هفتگی می‌شوند'},
    38: {'size': 'به اندازه یک کدو تنبل', 'development': 'ریه‌ها بالغ شده‌اند', 'tip': 'علائم شروع زایمان را بشناسید'},
    40: {'size': 'به اندازه یک هندوانه', 'development': 'آماده تولد! 🎉', 'tip': 'صبور باشید، نوبتش می‌رسد'},
  };
  ```
  - برای هفته‌هایی که در Map نیستند، **نزدیک‌ترین هفته‌ی قبلی** را نشان بده (interpolation).
  - هر هفته یک کارت با: اندازه، پیشرفت، و نکته.

  **ج) نکات خودمراقبتی سه‌ماهه‌ای:**
  - سه‌ماهه اول (هفته ۱-۱۲): تغذیه + اسید فولیک + مدیریت تهوع.
  - سه‌ماهه دوم (هفته ۱۳-۲۶): ورزش ملایم + آهن + آزمایشات.
  - سه‌ماهه سوم (هفته ۲۷-۴۰): استراحت + آمادگی زایمان + شمارش حرکات.

**۴.۳) غیرفعال‌سازی:**
- در حالت بارداری، دکمه «بازگشت به حالت عادی» → دیالوگ تأیید → `cycle_pregnancy_mode = 'false'`.
- داده‌های چرخه حفظ شوند (حذف نشوند).

**۴.۴) تأثیر بر بقیه سیستم:**
- وقتی حالت بارداری فعال است، `CycleConsentBridge.isUserMenstruating()` باید `false` برگرداند.
- نوتیفیکیشن‌های پریود متوقف شوند.
- در نوار ناوبری، آیکن صفحه تغییر نکند (همان heart_fill).

---

**D5 — تکمیلِ نوتیفیکیشنِ واقعی** (`lib/core/services/cycle_notification_service.dart` — سرویسِ جدید)

هدف: ارسالِ نوتیفیکیشنِ واقعیِ push، ۲ و ۱ روز قبل از شروعِ پیش‌بینی‌شدهٔ عادت.

پیاده‌سازی:
- **از پکیجِ `flutter_local_notifications`** استفاده کن. اگر در `pubspec.yaml` نیست، اضافه کن.
- **اگر پکیج `flutter_local_notifications` قبلاً در پروژه وجود دارد** (حتی برای بخش‌های دیگر)، از همان instance و تنظیمات استفاده کن. فقط channel/category جدید اضافه کن.

- سرویس:
  ```dart
  class CycleNotificationService {
    /// Schedule period reminder notifications
    static Future<void> scheduleReminders({
      required DateTime nextPeriodWindowStart,
      required bool consentReminders,
      required bool pregnancyMode,
    }) async { ... }

    /// Cancel all cycle-related notifications
    static Future<void> cancelAll() async { ... }
  }
  ```

- وقتی `cycle_consent_reminders == 'true'` و `cycle_pregnancy_mode != 'true'`:
  - **۲ روز قبل از `nextPeriodWindowStart`:** نوتیفیکیشن با عنوانِ **محرمانه**: `«یادآوری ریتمو 🌸»` و بدنه: `«لوازم شخصی‌ات رو چک کن 💜»`. (**هیچ واژه‌ی صریحِ پریود/قاعدگی/خونریزی**.)
  - **۱ روز قبل:** `«ریتمو 🌸»` — `«فردا ممکنه شروع بشه، آماده باش 💪»`.
- وقتی `cycle_consent_reminders == 'false'` یا حالت بارداری: **همه نوتیفیکیشن‌های چرخه لغو شوند.**
- نوتیفیکیشن‌ها **هر بار بعد از ثبتِ شروع/پایان دوره یا تغییر تنظیمات** re-schedule شوند (فراخوانی در `_loadData` یا `_togglePeriodToday`).
- در اندروید، notification channel: `id='cycle_reminders'`, `name='یادآوری‌های شخصی'`, `importance=DEFAULT` (نه HIGH — محرمانه).

---

**D6 — یکپارچه‌سازی در cycle_screen.dart:**
- **تبِ امروز (0):** دکمه SOS (D2) را اضافه کن — فقط در فازِ menstrual/luteal قابل مشاهده.
- **تبِ روندها (1):** ویجتِ نمودار خُلق‌وخو (D1) را زیرِ نمودار طول/مدت اضافه کن.
- **حالت بارداری (D4):** در `_CycleScreenContentState.build()`، اگر `cycle_pregnancy_mode == 'true'`، به‌جای تب‌های عادی، ویجتِ بارداری (D4) را نمایش بده.
- **تنظیمات:** دکمه فعال/غیرفعال‌سازی حالت بارداری (D4.1/D4.3) و قابلیت خاموش/روشن نوتیفیکیشن (موجود، فقط re-schedule صدا زده شود).
- **بعد از هر _loadData:** `CycleNotificationService.scheduleReminders(...)` فراخوانی شود (D5).

---

**D7 — پایان.**
- کلِ مسیر را برای محرمانگی بازبینی کن: نوتیفیکیشن‌ها بدون واژه صریح، حالت بارداری پشت قفل، بیرون هیچ ردّی.
- `DESIGN_SYSTEM_CYCLE.md` را با ویژگی‌های جدید به‌روز کن.

---

## ✅ اعتبارسنجیِ نهایی (یک‌بار)
- `flutter analyze` بدونِ error/warningِ جدید.
- دستی:
  - ورود با PIN → تبِ امروز → دکمه SOS در فاز عادت → bottom sheet راهکارها.
  - تبِ روندها → نمودار خُلق‌وخو زیر نمودار طول.
  - تنظیمات → فعال‌سازی حالت بارداری → UI بارداری (هفته + رشد + نکات).
  - غیرفعال‌سازی بارداری → بازگشت به چرخه عادی.
  - `cycle_consent_reminders` روشن → نوتیفیکیشن schedule شود (log چک شود).
  - `cycle_consent_reminders` خاموش → نوتیفیکیشن‌ها cancel شوند.
  - بازبینیِ محرمانگی: متنِ نوتیفیکیشن‌ها بدون واژه صریح.

## 📤 گزارشِ نهایی
```
- فایل‌های ساخته/تغییر: ...
- خلاصه‌ی D1..D7: ...
- نسخه‌ی مهاجرت: ...
- flutter analyze: ...
- بازبینیِ محرمانگی: ...
- ابهامات: ...
```
