# 🩺 سند طراحی — سیستم «دارو و سلامت» (Health) — ارتقای صفحه

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_HEALTH.md`.
> رنگِ هویت: قرمز `#EF4444` (همان کاشیِ هاب). آیکن: `bandage_fill`.

---

## ۱) وضعیت فعلی — بالغ‌ترین سیستم، ولی «فقط ثبت/خواندن»

این سیستم برخلافِ بقیه **کم‌عمق نیست**؛ ۱۰ بخشِ کاری دارد:
- داروها (موجودی/ریفیل/PRN با شمارشِ معکوس)، نوبت‌های پزشک، قند خون، فشار خون، علائمِ حیاتی (وزن/دما/SpO2/دورِ کمر)، مدارکِ پزشکی (+تصویر)، واکسیناسیون، آلرژی، پروفایلِ پزشکی، و ردیابِ بارداری (با تشخیصِ زایمانِ فعالِ ۵-۱-۱).
- **هشدارهای بحرانیِ واقعی:** افتِ/اوجِ قند، بحرانِ فشار (>۱۸۰/۱۲۰)، کم‌بودنِ موجودیِ دارو، قاعده‌ی ۵-۱-۱.
- **دستیارِ AI واقعی:** چت + OCR (شبیه‌سازی‌شده) با تزریقِ بافتِ سلامت و حفاظِ سختِ پزشکی (تشخیص/تجویز ممنوع، تداخلِ دارو-آلرژی قرمز).

**اما** ضعفِ اصلی روشن است:
- ❌ **هیچ لایه‌ی تحلیلی/روند:** نه `HealthEngine`، نه نمودارِ تاریخیِ قند/فشار/وزن. اعداد ثبت می‌شوند ولی فقط «آخرین مقدار» دیده می‌شود؛ هیچ روند، میانگین، یا «درصدِ در محدوده».
- ❌ **پایبندیِ دارو سنجیده نمی‌شود:** داروها در `routines` (category='medical') با `medStockCount`/`medRefillThreshold`/`minIntervalHours`/`maxDosesPerDay` هستند. برای داروهای PRN جدولِ `prn_logs(routineId,takenAt,dosage)` فقط رویدادِ «خوردم» را دارد، اما برای **داروهای زمان‌بندی‌شده هیچ لاگِ «این دوزِ موعد را خوردم/نخوردم»** نیست. پس استریک، درصدِ پایبندی و الگوی فراموشی غیرممکن است.
- ❌ **هیچ همبستگی:** فشار/قند با انرژی/خواب پیوند نمی‌خورد.
- ❌ **آماده‌سازیِ ویزیت نیست:** برای هر نوبتِ پزشک باید دستی همه‌چیز را به‌خاطر آورد؛ خلاصه‌ی قابلِ‌اشتراک تولید نمی‌شود.

**نتیجه:** استخوان‌بندی عالی است؛ کاری که می‌کنیم افزودنِ **مغزِ تحلیلی** روی همان جداول است — بدونِ شکستنِ هشدارها، دستیار، ماژولِ بارداری، یا زمان‌بندیِ یادآورها.

---

## ۲) سه تصمیمِ قطعی (تأییدشده)

1. **مربیِ کاملِ سلامت.** روندِ نموداریِ قند/فشار/وزن/SpO2 + پایبندیِ دارو + همبستگی + خلاصه‌ی آماده برای پزشک + همان هشدارهای بحرانی. جای یک اپِ مدیریتِ بیماریِ مزمن را بگیرد.
2. **جدولِ جدیدِ `medication_logs`** برای داروهای **زمان‌بندی‌شده** (لاگِ هر دوزِ موعد: taken/skipped + زمان). داروهای **PRN** همچنان از جدولِ موجودِ `prn_logs` استفاده می‌کنند (تکراری نساز). موتورِ پایبندی از **هر دو** می‌خواند. داروها همچنان در `routines` تعریف می‌شوند؛ این جدول فقط رویدادِ مصرف را ثبت می‌کند.
3. **OCR دست‌نخورده (موکِ صادق).** اسکنِ مدارک در این ارتقا واقعی نمی‌شود؛ فقط برچسبش صادقانه «به‌زودی/آزمایشی» شود. تمرکزِ کرِدیت روی لایه‌ی تحلیلی.

---

## ۳) معماریِ داده

> گسترشِ حداقلی. هیچ جدولِ سلامتِ موجود تغییرِ شکل نمی‌دهد؛ فقط یک جدولِ لاگ اضافه می‌شود و تحلیلِ فقط‌خواندنی رویِ همه ساخته می‌شود.

**جدولِ جدید `medication_logs`** برای داروهای زمان‌بندی‌شده (مهاجرت = نسخه‌ی فعلی + ۱ = **۲۱**):

| ستون | نوع | معنی |
|---|---|---|
| `id` | TEXT PK | شناسه |
| `routineId` | TEXT NOT NULL | ارجاع به داروی موجود در `routines` |
| `scheduledTime` | INTEGER | زمانِ موعدِ دوز (epoch) |
| `takenTime` | INTEGER | زمانِ واقعیِ ثبت (epoch؛ برای SKIPPED می‌تواند null) |
| `status` | TEXT NOT NULL DEFAULT 'TAKEN' | `TAKEN` / `SKIPPED` |
| `note` | TEXT | اختیاری |
| `createdAt` | INTEGER NOT NULL | — |

ایندکس: `idx_medlog_routine (routineId)`، `idx_medlog_time (scheduledTime)`.

- **`prn_logs` (موجود) دست‌نخورده**؛ داروهای PRN همان‌جا ثبت می‌شوند و موتور برای پایبندیِ PRN از آن می‌خواند. `medication_logs` فقط برای داروهای زمان‌بندی‌شده است.
- جداولِ `blood_sugar_logs`/`blood_pressure_logs`/`vital_signs_logs`/`doctor_visits`/... **دست‌نخورده**؛ موتور فقط از آن‌ها می‌خواند.
- بدونِ ماژول‌فلگِ جدید: گیتِ موجود `module_medicine_enabled` بماند.
- **تنظیمات (seed با `INSERT OR IGNORE`):** `health_adherence_enabled='true'`، `health_trend_window_days='30'`.

---

## ۴) موتور (`health_engine.dart`, الگوی `CachedEngine`)

ورودی (فقط‌خواندنی): `blood_sugar_logs`, `blood_pressure_logs`, `vital_signs_logs`, `medication_logs`, `prn_logs`, `routines(category='medical')`, `energy_logs`, `bedtime_diagnostics`/خواب، `today`, `windowDays`.
خروجی:
- **روندِ علائم:** برای هر متریک (قند/فشارِ سیستول‌دیاستول/وزن/SpO2) سریِ نقاط + میانگین + جهتِ روند (↑/↓/پایدار) + **درصدِ در محدوده** (با آستانه‌های دیابت/فشار از تنظیماتِ موجود).
- **پایبندیِ دارو:** `adherenceRate` (دوزهای خورده/موعد در بازه؛ زمان‌بندی‌شده از `medication_logs`، PRN از `prn_logs`)، `currentStreak`/`longestStreak`، الگوی فراموشی (مثلاً «شب‌ها بیشتر فراموش می‌شود»).
- **همبستگی:** فشار/قند ↔ انرژی/خواب (در [-1..1]، اگر داده کم بود null) + `insight` صادق و غیرقطعیِ علّی.
- **هشدارهای روندی (نه فقط لحظه‌ای):** مثلاً «میانگینِ فشارِ ۷ روزِ اخیر بالاتر از ۲ هفته‌ی قبل».

منطقِ خالص (روند/پایبندی/همبستگی) در helperِ تست‌پذیر. **فقط‌خواندنی.**

---

## ۵) ساختار صفحه (HealthScreen — افزودن، نه بازنویسی)

ساختارِ ۱۰‌بخشیِ موجود می‌ماند. این‌ها اضافه می‌شوند:

- **تبِ/بخشِ «روند و تحلیل» (جدید):**
  - نمودارِ قند/فشار/وزن در بازه (CustomPainter، بدونِ پکیجِ جدید) + خطِ محدوده‌ی سالم + جهتِ روند.
  - کارتِ **پایبندیِ دارو:** درصد + استریک + الگوی فراموشی.
  - کارتِ **همبستگی** (صادق): «روزهایی که خوبتر می‌خوابی، فشارت معمولاً پایین‌تره».
- **ثبتِ دوز (جدید):** کنارِ هر داروی فعال دکمه‌ی «خوردم»/«رد کردم» → `medication_logs`. شمارشِ موجودی همان منطقِ فعلی (نشکن).
- **خلاصه‌ی آماده برای پزشک (جدید):** از هر کارتِ نوبتِ پزشک (یا دکمه‌ی مستقل)، یک **خلاصه‌ی متنیِ قابلِ‌اشتراک/کپی** تولید شود: داروهای فعال، آخرین قند/فشار/وزن، روندها، آلرژی‌ها، علائمِ اخیر — یک‌جا. (Share/Copy؛ بدونِ پکیجِ سنگین.)
- **دستیارِ AI:** همان شیتِ موجود؛ بافتش با خلاصه‌ی روند/پایبندی غنی‌تر شود. حفاظِ پزشکی و OCRِ موک دست‌نخورده.

---

## ۶) لحن و اصول

- **هرگز تشخیص/تجویز نده.** حفاظِ پزشکیِ موجود حفظ شود؛ هشدارها «به پزشک مراجعه کن» می‌گویند، نه نسخه.
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی؛ واحدها فارسی.
- شفافیتِ علّی در همبستگی‌ها، بدونِ ادعای قطعیتِ علّی.
- حریم: داده‌ی پزشکی خصوصی است؛ خلاصه فقط با اقدامِ صریحِ کاربر share می‌شود.
- **محرمانگیِ چرخه:** صفحه‌ی سلامت هیچ ارجاعِ صریح به پریود/قاعدگی/تخمک‌گذاری ندارد؛ هیچ همبستگیِ سلامت با چرخه اینجا ساخته نمی‌شود (آن فقط درونِ والتِ چرخه و از مسیرِ `cycle_consent_bridge`).
- AI هرگز مستقیم در DB نمی‌نویسد (Preview→Edit→Save).

---

## ۷) فعال‌سازی (هاب)

کاشیِ «دارو» با گیتِ موجود `module_medicine_enabled` و `HealthScreen` دست‌نخورده می‌ماند؛ فقط محتوای صفحه غنی‌تر می‌شود.

---

## ۸) ترتیبِ ساخت (خلاصه)

H1 مهاجرت (`medication_logs` + تنظیمات) → H2 مدل‌ها → H3 `health_engine.dart` (روند/پایبندی/همبستگی) → H4 ثبتِ دوز (taken/skipped) → H5 نمودارِ روندِ علائم → H6 کارتِ پایبندیِ دارو → H7 بخشِ همبستگی → H8 خلاصه‌ی آماده برای پزشک (Share/Copy) → H9 مونتاژ در HealthScreen + غنی‌سازیِ بافتِ دستیار → H10 اعتبارسنجی و پرداخت (هشدارها/بارداری/یادآورها/محرمانگیِ چرخه نشکنند).
