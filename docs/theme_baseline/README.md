# Visual Baseline Screenshots

This directory stores the visual baseline reference for the 6 reference pages in Light and Dark modes prior to Theme Modernization (Prompt 047).

Reference Pages:
1. Dashboard (`now_dashboard_screen.dart`)
2. Calendar (`calendar_tokens.dart` / Calendar Views)
3. Systems Hub (`systems_hub_screen.dart`)
4. Goals (`goals_screen.dart`)
5. Profile & Settings (`profile_screen.dart`)
6. Form Sheet (`create_goal_sheet.dart`)
