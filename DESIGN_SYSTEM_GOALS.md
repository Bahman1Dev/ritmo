# 🎯 سند طراحی — سیستم «اهداف و برنامه‌ها» (Goals & Plans) — ارتقا به صفحه

> این سند برای **مالکِ محصول** است. معماری، تصمیم‌ها و منطقِ UX را توضیح می‌دهد.
> سندِ اجراییِ Gemini: `AGENT_PROMPT_GOALS.md`.
> رنگِ هویتِ سیستم: کهربایی `#F59E0B` (همان رنگِ فعلیِ کاشیِ اهداف در هاب).

---

## ۱) وضعیت فعلی — قابلیت هست، ولی دفن شده

برخلافِ کنکور (که اسکلت بود)، اهداف **منطقِ پخته‌ای دارد که در دسترس نیست**:

- ✅ `goals_management_sheet.dart` از قبل دارد: اهدافِ چندسطحی (سالانه/ماهانه/هفتگی/روزانه)، درختِ والد-فرزند (`parentGoalId`)، گام‌های تاریخ‌دار (`scheduledDate`)، اتصالِ گام به روتین (`linkedRoutineId`)، و **خرد کردن با AI** با پیش‌نمایشِ قابلِ‌ویرایش.
- ✅ داشبوردِ «امروز» از قبل گام‌های `scheduledDate == today` را کوئری می‌کند (پشتِ `module_goals_enabled`).
- ❌ ولی کاشیِ هاب **«به‌زودی» (placeholder)** است و ماژول را هرگز فعال نمی‌کند → کلِ این قابلیت **غیرقابلِ‌دسترس** است.
- ❌ این یک **شیتِ ۰٫۷ ارتفاع** است، نه صفحه؛ جا برای تایم‌لاین/آمار/آرشیو ندارد.
- ❌ `goals.targetDate` (مهلت) **هرگز استفاده نمی‌شود** — نه روزشمار، نه عقب‌افتادگی.
- ❌ `goal_steps.linkedRoutineId` **ست می‌شود ولی مصرف نمی‌شود** — وصل‌کردنِ گام به روتین هیچ اثری ندارد (پلِ «برنامه ← اجرا» وجود ندارد).
- ❌ تاریخ‌ها میلادی (`showDatePicker` + `year/month/day`)، نه شمسی.

**نتیجه:** این بازسازی نیست؛ **ارتقا و اتصال** است — بیرون‌کشیدنِ منطقِ موجود به یک صفحه‌ی کامل + زنده‌کردنِ سه چیزِ مرده (فعال‌سازیِ ماژول، مهلت/روزشمار، پلِ روتین↔گام) + تایم‌لاینِ کل‌نگر.

---

## ۲) سه تصمیمِ قطعی (تأییدشده توسط مالک)

1. **«برنامه‌ها» = هابِ برنامه‌ی کل‌نگر.** صفحه علاوه بر اهداف، یک **تایم‌لاینِ اجراییِ یکپارچه** دارد که در یک نما نشان می‌دهد: گام‌های هدف + جلساتِ دوره‌ها (`course_sessions.plannedDate`) + آیتم‌های برنامه‌ی کنکور (`konkur_plan_items`). فقط-خواندنیِ تجمیعی؛ هر آیتم به صفحه‌ی مبدأ لینک می‌دهد.
2. **پلِ روتین↔گام = پیشرفت‌دهی، بدون تیکِ خودکار.** انجامِ روتینِ متصل، درصدِ پیشرفت/وضعیتِ گام را تغذیه و نمایش می‌دهد، ولی **هرگز `goal_steps.isCompleted` را خودکار ۱ نمی‌کند** — کاربر دستی تأیید می‌کند. (همان قانونِ ایمنِ دوره‌ها: یک گام ممکن است معنایی فراتر از یک‌بار انجامِ روتین داشته باشد.)
3. **مهلت = روزشمارِ آرام + گامِ عقب‌افتاده.** هر هدف `targetDate`ِ اختیاری دارد با روزشمارِ آرام (نه قرمزِ اضطراب). گام‌های عقب‌افتاده (تاریخ گذشته و تیک‌نخورده) با لحنِ تشویقی برجسته می‌شوند، بدون سرزنش.

---

## ۳) تمایز از داشبورد و تقویم (مهم)

سه نما به گام‌های تاریخ‌دار نگاه می‌کنند؛ نباید تکراری حس شوند:
- **داشبوردِ «امروز»:** فقط امروز، تمرکزِ اجرا (کارتِ «قدم امروز»). — دست‌نخورده می‌ماند.
- **تقویم:** نمای ماهانه/سراسری.
- **صفحه‌ی اهداف › تایم‌لاین:** **نمای افقِ برنامه‌ریزی** — «این هفته / هفته‌ی بعد + عقب‌افتاده‌ها»، گروه‌بندی‌شده بر اساس روز، تجمیعِ هر سه منبع. هدفش «جلوتر را دیدن» است، نه «همین حالا را انجام دادن».

---

## ۴) معماریِ داده

> جداول از قبل وجود دارند؛ **گسترشِ حداقلی** + بدونِ جدولِ جدید. داده‌ی فعلی تستی است.

- **`goals`** — همه‌ی ستون‌ها هست. `targetDate` را **زنده می‌کنیم** (روزشمار). + ستونِ جدید `progressCache REAL DEFAULT 0` (اختیاری، کشِ درصدِ مشتق برای نمایشِ سریع؛ منبعِ حقیقت همان گام‌ها/فرزندان است).
- **`goal_steps`** — `scheduledDate` و `linkedRoutineId` هست. **`linkedRoutineId` را زنده می‌کنیم.** + `targetDate`/مهلتِ گام لازم نیست (همان `scheduledDate`).
- **بدونِ جدولِ جدید.** تایم‌لاین فقط از سه جدولِ موجود (`goal_steps`, `course_sessions`, `konkur_plan_items`) می‌خواند.
- **تنظیمات:** `module_goals_enabled` (موجود — فقط باید قابلِ فعال‌سازی شود).

### درصدِ پیشرفتِ هدف (مشتق، نه دستی)
- هدفِ بی‌فرزند: `گام‌های تیک‌خورده / کلِ گام‌ها`.
- هدفِ والد: میانگینِ پیشرفتِ فرزندان (بازگشتی).
- گامِ متصل به روتین: نوارِ فرعی نشان می‌دهد روتین چند بار/چه استریکی انجام شده (تغذیه)، ولی تیکِ نهایی دستی است.

---

## ۵) موتور (`goals_engine.dart`, الگوی `CachedEngine`)

خروجی‌ها: `goalProgress: Map<goalId,double>` (بازگشتی)، `todaySteps`، `upcomingTimeline` (افقِ N روز، تجمیعِ سه منبع، مرتب با تاریخ)، `overdueSteps` (تاریخ گذشته + تیک‌نخورده)، `linkedRoutineStatus: Map<stepId, {doneCount, streak}>`، `activeGoalsCount`، `completedGoalsCount`. منطقِ خالصِ بازگشتیِ پیشرفت در یک helperِ تست‌پذیر.

---

## ۶) ساختار صفحه (GoalsScreen جدید، جایگزینِ شیت در نقشِ ماژول)

**هیرو:** «X هدفِ فعال · Y گامِ این هفته» + خلاصه‌ی نزدیک‌ترین مهلت.

بخش‌ها/تب‌ها:
1. **اهداف (درخت)** — همان نمای درختیِ موجود، ارتقایافته: روزشمارِ مهلت، نوارِ پیشرفتِ مشتق، badgeِ گامِ عقب‌افتاده، چیپِ روتینِ متصل (با وضعیت). [＋ هدف] + [🤖 خرد کردن با AI] (منطقِ موجود بازاستفاده شود).
2. **تایم‌لاین (برنامه‌ی کل‌نگر)** — افقِ «این هفته/بعد + عقب‌افتاده»، گروه‌بندیِ روزانه، آیتم‌ها از سه منبع با آیکنِ مبدأ، تپ → صفحه‌ی مبدأ.
3. **آرشیو/دستاوردها** — اهدافِ `COMPLETED` با 🏆 و تاریخِ تکمیل؛ بازگردانی/حذف.

**ساخت/ویرایشِ هدف:** فرمِ موجود، با تاریخِ **شمسی**، `targetDate`، و انتخابِ روتینِ متصل برای هر گام.

---

## ۷) جریانِ فعال‌سازی (هاب)

کاشیِ هاب از «به‌زودی» به الگوی استانداردِ ماژول تبدیل می‌شود (مثل دارو/کنکور): اگر `module_goals_enabled=true` → ورود به `GoalsScreen`؛ وگرنه `_showActivationSheet` با `settingKey: 'module_goals_enabled'`. (داشبورد از قبل به این کلید گوش می‌دهد، پس با فعال‌سازی، کارتِ «قدم امروز» هم زنده می‌شود.)

---

## ۸) اتصال‌ها و مرزها

- **روتین → گام:** هنگامِ تکمیلِ موفقِ روتین، اگر گامی `linkedRoutineId` دارد، آمارِ تغذیه به‌روز شود (نه تیکِ خودکار). از مسیرِ موجودِ تکمیلِ روتین قلاب می‌زنیم (همان‌جا که `ProgressionEngine.onCompletion` صدا زده می‌شود).
- **هرگز در `routine_completions`/`daily_rhythm` ننویس.** پیشرفتِ هدف مستقل و مشتق است.
- **چرخه‌ی بدن هرگز در تایم‌لاین/اهداف ظاهر نمی‌شود** — محرمانه و خارج از این سیستم.
- **AI فقط دستیار:** خرد کردنِ هدف از مسیرِ Preview→Edit→Save (از قبل این‌طور است؛ حفظ شود).

---

## ۹) لحن و اصول

- روزشمار و عقب‌افتادگی همیشه تشویقی («۲ روز از این گام عقبی، می‌رسی بهش 💪»).
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی همه‌جا.
- درصد همیشه مشتق از داده‌ی واقعی.

---

## ۱۰) ترتیبِ ساخت (خلاصه)

PASS 0 → G1 مهاجرت 🔴 (حداقلی: `progressCache`) → G2 مدل‌ها → G3 helperِ پیشرفت (خالص) → G4 موتور → G5 پلِ روتین↔گام (قلابِ تکمیل) → G6 صفحه‌ی اصلی + استخراجِ منطقِ شیت → G7 تبِ اهداف (درخت + مهلت + عقب‌افتاده + شمسی) → G8 تبِ تایم‌لاین → G9 آرشیو → G10 اتصال به هاب (حذفِ placeholder) → G11 پرداخت + تست + سند.

🔴 = حساس به مهاجرت → بعدش توقف برای تأییدِ انسانی. (G1 سبک است چون اسکیمای اهداف از قبل آماده است.)
