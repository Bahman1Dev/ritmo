# 🤖 پرامپت اجرایی برای Gemini 3.5 Flash — بازطراحی بخش «ثبت جدید» (Add Item Flow)

> فایل **خودبسنده**. کل آن را عیناً به Gemini بده. هدف: تبدیل «ساخت روتین» به یک «نقطه‌ی ثبت عمومی» با سه نوع آیتم، فرم تک‌صفحه‌ای و افزودن سریع.
> فایل اصلی فعلی (ویزارد ۵ مرحله‌ای که بازنویسی می‌شود): `lib/features/routines/presentation/routine_create_flow.dart`

---

## ⛔️ قوانین سخت (هرگز نقض نکن)
1. **هر بار فقط یک تسک.** تا تأیید نشدن تسک فعلی، سراغ بعدی نرو.
2. **بعد از هر تسک:** `flutter analyze` و `flutter test` را اجرا کن و گزارش بده. error یا تستِ شکست‌خورده‌ی جدید نباید اضافه شود؛ در غیر این صورت همان تسک را revert کن و گزارش بده.
3. **فقط فایل‌های نام‌برده‌ی هر تسک را تغییر بده.** ریفکتور کد نامرتبط ممنوع.
4. **متن‌ها فارسی و راست‌چین (RTL).** فونت `Vazirmatn`. ارقام فارسی در نمایش.
5. **رنگ/اندازه/رادیوس هاردکد نکن** — از `RitmoTheme` استفاده کن.
6. **معماری RIE دست‌نخورده:** قانون «انجام فقط با مودال نیت/بازتاب» حفظ شود.
7. **تسک‌های دیتابیس (A1) حساس‌اند:** بعد از A1 توقف کن و منتظر تأیید انسانی بمان.
8. اگر چیزی مبهم بود، حدس نزن — بپرس.

## 📁 محیط پروژه (واقعیت کد فعلی)
- Flutter، ریشه `ritmo/`. دیتابیس SQLite، **نسخه‌ی فعلی ۸** (یا اگر بالاتر رفته، یکی جلوتر برو)، فایل `lib/core/database/database_helper.dart`.
- همه‌چیز اکنون «روتین» است؛ مدل جدا برای تسک/یادآور **وجود ندارد**.
- `RecurrenceRule` در `lib/core/domain/models.dart` این فیلدها را دارد: `weekdays`, `intervalDays`, `monthDay`, `startDate`, `endDate`, `excludedDates`, `reminderTimes`. **فیلد بازه‌ی ساعتی ندارد.**
- موتور تکرار: `lib/core/domain/engines/routine_occurrence_generator.dart`.
- ماژول AI آماده: `lib/core/ai/ai_gateway.dart`, `ai_prompt_engine.dart`, `ai_fallback_engine.dart`.
- مودال نیت/بازتاب و مکانیک «انجام/تعویق» از قبل وجود دارد (در `lib/features/routines/`).

## 🔒 تصمیم‌های قطعی طراحی (تغییر نده)
1. **سه نوع آیتم:** `ROUTINE` (روال، تکرارشونده) · `REMINDER` (یادآور، فقط اعلان) · `TASK` (کار، تک‌بار).
2. **نام‌های فارسی UI:** «روال» / «یادآور» / «کار». عنوان بخش: «ثبت جدید». از کلمه‌ی «روتین» در UI جدید استفاده نکن.
3. **مدل داده = تک‌جدول.** هر سه نوع در جدول `routines` با ستون جدید `itemType` ذخیره می‌شوند. **جدول جدا نساز.**
4. **حذف از این فلو:** ورزش، عبادت، دوره‌ی آموزشی، کنکور، دارو، و گزینه‌های «نسخه‌ی سبک/حداقلی». این‌ها اینجا نباید باشند (به صفحات «سیستم‌های من» تعلق دارند). اگر در فلوی فعلی هستند، حذفشان کن.
5. **فرم تک‌صفحه‌ای با Progressive Disclosure** (نه ویزارد چندمرحله‌ای). لایه۱ همیشه دیده؛ بقیه پشت «گزینه‌های بیشتر».
6. **افزودن سریع = AI اول، پارسر محلی fallback.** اگر AI خطا/JSON نامعتبر داد، خودکار به پارسر محلی برو. نتیجه همیشه در فرم برای تأیید نشان داده شود (ذخیره‌ی کور ممنوع).
7. **دکمه‌ی «انجام» برای هر سه نوع، مودال پس از انجام را باز می‌کند.**

---

# 🗂 صف تسک‌ها (به ترتیب)

## ▣ تسک A1 — مهاجرت دیتابیس 🔴 حساس
**فایل:** `lib/core/database/database_helper.dart` + `lib/core/domain/models.dart`
**اقدام:**
1. `version` را یک واحد جلو ببر (۸ → ۹ یا هرچه فعلی است +۱)، در همه‌ی جای ظاهرشدن.
2. به جدول `routines` ستون اضافه کن (هم `onCreate` هم `onUpgrade` با `ALTER TABLE`):
   - `itemType TEXT NOT NULL DEFAULT 'ROUTINE'` (مقادیر: `ROUTINE`/`REMINDER`/`TASK`)
3. در `models.dart` به کلاس `RecurrenceRule` فیلد `final int? intervalHours;` اضافه کن و در `toMap`/`fromMap` لحاظش کن (برای «هر N ساعت»). nullable و backward-compatible باشد.
**تأیید:** `test/migration_test.dart` پاس؛ نصب تازه و ارتقا از نسخه‌ی قبل سالم. رکوردهای موجود `itemType='ROUTINE'` بگیرند. **سپس توقف کن و منتظر تأیید انسانی بمان.**

## ▣ تسک A2 — پشتیبانی بازه‌ی ساعتی در موتور تکرار
**فایل:** `lib/core/domain/engines/routine_occurrence_generator.dart`
**اقدام:** منطق تولید رخداد را طوری گسترش بده که اگر `intervalHours != null` بود، رخدادها بر اساس بازه‌ی ساعتی (مثلاً هر ۸ یا ۴۸ ساعت از `startDate`) ساخته شوند. اولویت: اگر `intervalHours` ست بود، آن مبناست؛ وگرنه منطق روزانه‌ی فعلی.
**تأیید:** تست واحد جدید در `test/` که «هر ۸ ساعت» و «هر ۴۸ ساعت» را درست تولید کند؛ منطق روزهای موجود نشکند.

## ▣ تسک A3 — انتخاب‌گر نوع + اسکلت فرم تک‌صفحه‌ای
**فایل:** `lib/features/routines/presentation/routine_create_flow.dart` (بازنویسی به فرم تک‌صفحه)
**اقدام:**
1. ساختار ۵ مرحله‌ای را با یک **فرم تک‌صفحه‌ی اسکرول‌شونده** جایگزین کن.
2. بالای فرم یک **سگمنت سه‌تایی**: «🔁 روال» / «🔔 یادآور» / «✅ کار». پیش‌فرض «روال».
3. **لایه۱ (همیشه دیده):** عنوان، دسته، زمان. برای «روال» علاوه‌بر این: انتخاب تکرار (روزانه / هر N روز / **هر N ساعت** / روزهای دلخواه / ماهانه).
4. فیلدهای نامربوط به هر نوع مخفی شوند (مثلاً «کار» تکرار ندارد).
5. هدر: «ثبت جدید» + دکمه‌ی «ذخیره».
**تأیید:** هر سه نوع قابل انتخاب؛ فرم در یک صفحه؛ analyze/test سبز. (ذخیره در A5 کامل می‌شود.)

## ▣ تسک A4 — «گزینه‌های بیشتر» (Progressive Disclosure)
**فایل:** همان `routine_create_flow.dart`
**اقدام:** یک بخش تاشوی «گزینه‌های بیشتر» زیر لایه۱:
- لایه۲: حیاتی؟ (`isEssential`)، اولویت، بازه‌ی ممنوع/ترجیحی.
- لایه۳: وابستگی به روال دیگر (habit stacking) با اعتبارسنجی چرخه (والد→فرزند→والد ممنوع).
- **گزینه‌های حذف‌شده** (نسخه‌ی سبک/حداقلی، دسته‌های تخصصی ورزش/عبادت/دوره/کنکور/دارو) را اینجا **نیاور**.
**تأیید:** بخش پیش‌فرض جمع است؛ باز می‌شود؛ analyze/test سبز.

## ▣ تسک A5 — ذخیره‌سازی بر اساس نوع
**فایل:** `routine_create_flow.dart` + هر جای لازم برای نوشتن در `routines`/`routine_schedules`
**اقدام:** هنگام ذخیره، `itemType` درست ست شود:
- `ROUTINE`: با `RecurrenceRule` کامل (شامل `intervalHours` اگر انتخاب شد).
- `REMINDER`: عمدتاً `reminderTimes`؛ مکانیک کامل انجام لازم نیست.
- `TASK`: تک‌بار، بدون تکرار (recurrence خالی).
پیش‌فرض‌های هوشمند: نوع=روال، تکرار=روزانه، دسته=آخرین انتخاب. کاربر بتواند فقط با واردکردن عنوان ذخیره کند.
**تأیید:** هر سه نوع ذخیره و در لیست/داشبورد درست ظاهر شوند؛ رویداد `RitmoEvents` برای رفرش صادر شود؛ analyze/test سبز.

## ▣ تسک A6 — مودال پس از «انجام» برای هر سه نوع
**فایل‌ها:** مسیر «انجام/تعویق» در `lib/features/routines/` و لیست/داشبورد
**اقدام:** مطمئن شو دکمه‌ی «انجام» برای `REMINDER` و `TASK` هم همان مودال نیت/بازتاب موجود را باز می‌کند (نه تیک بی‌صدا). «تعویق» مثل قبل کار کند.
**تأیید:** زدن «انجام» روی هر سه نوع، مودال را باز کند؛ analyze/test سبز.

## ▣ تسک A7 — افزودن سریع (AI + fallback محلی)
**فایل‌ها:** `routine_create_flow.dart` + فایل جدید `lib/features/routines/presentation/quick_add_parser.dart` + `lib/core/ai/ai_prompt_engine.dart`
**اقدام:**
1. نوار «افزودن سریع» بالای فرم (TextField با placeholder مثل «ورزش هر روز ۶ عصر»).
2. `quick_add_parser.dart`: پارسر محلی برای الگوهای رایج فارسی (ساعت «۶ عصر/۲۱:۰۰»، «هر روز»، «هر N روز»، «هر N ساعت»، روزهای هفته).
3. قالب prompt جدید در `ai_prompt_engine.dart` که متن آزاد را به JSON فیلدهای فرم تبدیل کند (`title, itemType, time, recurrence...`). خروجی **فقط JSON**.
4. فلو: اول `AiGateway` → اگر موفق، فرم را پیش‌پر کن؛ اگر خطا/JSON نامعتبر/تایم‌اوت، **خودکار** پارسر محلی را اجرا کن. اگر هیچ‌کدام نتیجه نداد، فقط عنوان را در فیلد بگذار.
5. نتیجه همیشه در فرم نمایش داده شود؛ کاربر قبل از ذخیره ویرایش/تأیید کند.
**تأیید:** جمله‌ی نمونه به فیلدهای درست تبدیل شود (هم مسیر AI هم fallback)؛ بدون کرش هنگام قطع AI؛ analyze/test سبز.

## ▣ تسک A8 — پرداخت بصری و RTL
**فایل:** `routine_create_flow.dart` و ویجت‌های مرتبط
**اقدام:** هماهنگی با `RitmoTheme`، فاصله‌گذاری یکنواخت، RTL کامل، ارقام فارسی، حداقل لمسی ۴۸dp، گذار ملایم. بدون رنگ هاردکد.
**تأیید:** فرم شیک و یکدست؛ `grep withOpacity` در فایل خالی؛ analyze/test سبز.

## ▣ تسک A9 — صحت‌سنجی نهایی + مستندات
**اقدام:** `flutter analyze` (بدون ایراد جدید) · `flutter test` (سبز) · `flutter run -d chrome` و تست دستی: ساخت هر سه نوع، «هر ۸ ساعت»، افزودن سریع (AI و حالت قطع AI)، مودال پس از انجام. سپس `DESIGN_ADD_ITEM_FLOW.md` را به‌روز کن.
**تأیید:** کل فلو بدون رگرسیون.

---

## 📤 قالب گزارش بعد از هر تسک
```
## تسک [A?]: [عنوان]
- فایل‌های تغییر/ساخته‌شده: ...
- خلاصه‌ی تغییر: ...
- flutter analyze: [قبل → بعد]
- flutter test: [N passed, M failed]
- وضعیت: ✅ / ⚠️ / ❌
- نکات/ابهامات: ...
```
**یادآوری: بعد از تسک A1 حتماً توقف کن و منتظر تأیید انسانی بمان.**
