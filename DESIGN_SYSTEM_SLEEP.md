# 🌙 سند طراحی — سیستم «خواب و بیداری» (Sleep) — صفحه‌ی جدید

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_SLEEP.md`.
> رنگِ هویت: بنفش `#8B5CF6` (همان رنگِ کاشیِ هاب). آیکن: ماه‌وستاره.

---

## ۱) وضعیت فعلی — یک تله، نه یک سیستم

- ⚠️ جدولِ `bedtime_diagnostics (date PK, reason, note, createdAt)` وجود دارد ولی **حداقلیِ حداقلی** است: نه ساعتِ خواب، نه بیداری، نه مدت، نه کیفیتِ ساختاریافته. فقط یک «دلیل» و «یادداشت».
- ⚠️ **`EnergyAnalyticsEngine` این جدول را مصرف می‌کند:** اگر در `reason`/`note` کلیدواژه‌ی خوابِ بد باشد (`ضعیف/کم/دیر/خستگی/بی‌خوابی/poor/late/...`) → **−۱۵٪ انرژیِ پویا**. این پلِ خواب→انرژی از قبل زنده است و **نباید بشکند**.
- ❌ **هیچ UIِ نوشتنی وجود ندارد** — نه ثبتِ خواب، نه سؤالِ خواب در چک‌اینِ صبح. جدول عملاً همیشه خالی است؛ پس آن −۱۵٪ هرگز فعال نمی‌شود.
- ❌ صفحه‌ی اختصاصی ندارد؛ کاشیِ هاب «به‌زودی» است.
- ❌ نه هدفِ خواب، نه روزشمار، نه بدهیِ خواب، نه ثبات، نه روند، نه همبستگی.

**نتیجه:** زیرساختِ کوچکی هست (یک جدول + یک پلِ انرژی)، ولی سیستم باید **از صفر روی همان جدول ساخته شود** — با گسترشِ ساختاریافته، بدونِ شکستنِ پلِ موجود.

---

## ۲) سه تصمیمِ قطعی (تأییدشده)

1. **مربیِ کاملِ خواب.** هدفِ خواب + **بدهیِ خواب** (sleep debt) + **ثبات/نظمِ خواب** (consistency) + کیفیت + روند + **همبستگی با انرژی و حال** + دستیار. کامل، جوری که جای یک اپِ مستقلِ خواب را بگیرد.
2. **هدف‌گذاری + ثبتِ صبحگاهی (برنامه در برابر واقعیت).** کاربر یک‌بار هدف می‌گذارد (ساعتِ خوابِ هدف، ساعتِ بیداریِ هدف، مدتِ هدف). هر صبح واقعیتِ دیشب را ثبت می‌کند؛ سیستم **برنامه را با واقعیت** می‌سنجد.
3. **یادآورِ آرامِ پیش‌خواب + روزشمارِ هدف.** نه ساعتِ زنگِ مضطرب؛ یک روزشمارِ آرام تا «زمانِ آماده‌شدن برای خواب» + یادآورِ ملایمِ wind-down (اگر کاربر روشن کند).

---

## ۳) محورِ سوم: خواب × انرژی × حال

انرژی (برانگیختگی) و حال (خوشایندی) را همین حالا ساختیم. **خواب علتِ بالادستیِ هر دو است:** خوابِ خوبِ دیشب → انرژی/حالِ بهترِ امروز. پس صفحه‌ی خواب:
- خوابِ هر شب را با **انرژی/حالِ فردای آن** جفت می‌کند و **همبستگیِ صادق** نشان می‌دهد («شب‌هایی که زودتر می‌خوابی، انرژیِ فردات معمولاً بالاتره»).
- این بستن از طریقِ خواندنِ `energy_logs`/`mood_logs` است؛ **چیزی در آن‌ها نوشته نمی‌شود.**

---

## ۴) معماریِ داده

> گسترشِ حداقلیِ جدولِ موجود؛ **بدونِ جدولِ جدید.** داده‌ی فعلی تستی است.

`bedtime_diagnostics` گسترش می‌یابد (ستون‌های جدید با `DEFAULT`):

| ستون | نوع | معنی |
|---|---|---|
| `date` | TEXT PK | تاریخِ شبِ خواب (ISO `YYYY-MM-DD`) — موجود |
| `reason` | TEXT | **پلِ موتور** — مشتق از کیفیت/مدت (کلیدواژه‌دار) — موجود |
| `note` | TEXT | یادداشتِ کاربر — موجود |
| `createdAt` | INTEGER | epoch — موجود |
| `bedtimeAt` | INTEGER | epochِ واقعیِ به‌خواب‌رفتن — جدید |
| `wakeAt` | INTEGER | epochِ واقعیِ بیداری — جدید |
| `durationMinutes` | INTEGER | مدتِ خواب (دقیقه) — جدید |
| `quality` | INTEGER DEFAULT 3 | کیفیت ۱..۵ — جدید |
| `awakenings` | INTEGER DEFAULT 0 | تعدادِ بیدارشدنِ شبانه — جدید |

**پلِ سازگاری (مهم):** هنگامِ ثبت، علاوه بر ستون‌های ساختاریافته، `reason`/`note` هم پر می‌شوند با کلیدواژه‌ی مشتق:
- کیفیتِ ضعیف (۱–۲) یا مدتِ خیلی‌کم → `reason` شاملِ «ضعیف»/«کم» (تا جریمه‌ی −۱۵٪ موتور فعال شود).
- کیفیتِ خوب → `reason` بدونِ کلیدواژه‌ی بد (بدونِ جریمه).
این یعنی `EnergyAnalyticsEngine` **دست‌نخورده** کار می‌کند و حالا واقعاً تغذیه می‌شود.

**تنظیمات (seed با `INSERT OR IGNORE`):**
- `module_sleep_enabled` = `false` (گیتِ صفحه)
- `sleep_target_bedtime` = `23:30` (HH:mm)
- `sleep_target_wake` = `07:00`
- `sleep_target_duration_minutes` = `450` (۷٫۵ ساعت)
- `sleep_winddown_reminder` = `false`
- `sleep_winddown_minutes` = `30`
- `sleep_setup_done` = `false`

---

## ۵) موتور (`sleep_engine.dart`, الگوی `CachedEngine`)

ورودی: `sleepLogs` (bedtime_diagnostics)، هدف‌ها (bedtime/wake/duration)، `energyLogs`، `moodLogs`، `today`، `horizonDays`.
خروجی:
- `lastNight` — خلاصه‌ی دیشب (مدت، کیفیت، انحراف از هدف).
- `avgDurationMinutes` و `avgQuality` (پنجره‌ی چرخان).
- `sleepDebtMinutes` — بدهیِ خواب = جمعِ کسریِ N شبِ اخیر نسبت به مدتِ هدف.
- `consistencyScore` (۰..۱۰۰) — از پراکندگیِ ساعتِ خواب/بیداری؛ نظمِ خواب مهم‌ترین سنجه‌ی علمیِ خواب است.
- `durationTrend` / `qualityTrend` — چند هفته.
- `bestBedtimeWindow` — ساعتِ خوابی که تاریخاً بهترین انرژی/حالِ فردا را داده.
- `sleepEnergyCorrelation` / `sleepMoodCorrelation` (در [-1..1]، اگر داده کم بود null) + `correlationInsight` (متنِ صادق و غیرمستقیم).

منطقِ خالص (بدهی/ثبات/همبستگی) در helperِ تست‌پذیر.

---

## ۶) ساختار صفحه (SleepScreen)

**هیرو:** خلاصه‌ی دیشب (مدتِ بزرگ + کیفیت + چیپِ «به هدف رسیدی/۴۰ دقیقه کم») + **روزشمارِ آرام تا زمانِ خوابِ هدف** («۳ ساعت تا زمانِ خوابت») + دکمه‌ی «ثبتِ خوابِ دیشب».

تب‌ها:
1. **دیشب** — جزئیاتِ دیشب + **برنامه در برابر واقعیت** (هدف vs واقعی) + هدفِ امشب + کلیدِ یادآورِ wind-down.
2. **الگوها** — نمره‌ی ثبات، میانگینِ مدت، **بدهیِ خواب**، بهترین ساعتِ خواب (با لحنِ تشویقی). اگر داده کم بود: «هنوز در حالِ یادگیریِ الگوی خوابتم 🌙».
3. **روند** — مدت/کیفیت در چند هفته + **همبستگیِ خواب↔انرژی/حال** (صادق، غیرمستقیم).

**دستیارِ AI (🤖):** مربیِ خواب — توضیحِ غیرمستقیمِ الگوها + پیشنهادِ ملایم (wind-down زودتر، ساعتِ بیداریِ ثابت). فقط دستیار: Preview→Edit→Save، بدونِ نوشتنِ کور.

**ثبتِ خوابِ دیشب (شیت):** ساعتِ خواب + ساعتِ بیداری (مدت خودکار) + کیفیت (۱..۵ ایموجی) + بیدارشدن‌ها + یادداشت. می‌نویسد در `bedtime_diagnostics` (ساختاریافته + پلِ `reason`/`note`). نگاشتِ کیفیت→انرژی را از `morning_checkin_sheet` وام بگیر؛ نشکن.

**هدف‌گذاری (اولین فعال‌سازی):** ساعتِ خوابِ هدف + بیداریِ هدف + مدتِ هدف + کلیدِ یادآور → تنظیمات.

---

## ۷) یادآورِ پیش‌خواب (wind-down)

- بخشِ همیشه‌کار: **روزشمارِ درون‌برنامه‌ای** تا «زمانِ خوابِ هدف منهای دقیقه‌ی wind-down».
- اگر زیرساختِ نوتیفیکیشنِ محلی در پروژه باشد، یک یادآورِ آرام زمان‌بندی شود؛ اگر نبود، فقط روزشمارِ درون‌برنامه‌ای (تنزلِ آرام، بدونِ افزودنِ پکیجِ سنگین مگر از قبل باشد).
- لحن: دعوتِ آرام («وقتشه آروم آروم آماده‌ی خواب بشی 🌙»)، نه اخطار.

---

## ۸) فعال‌سازی (هاب)

کاشیِ «خواب و بیداری» از «به‌زودی» به الگوی استانداردِ ماژول (مثل کنکور/اهداف) → `module_sleep_enabled`. اولین ورود = شیتِ هدف‌گذاری.
**نکته:** پلِ خواب→انرژی مستقل از این گیت کار می‌کند (موتور همیشه `bedtime_diagnostics` را می‌خواند)، پس فعال/غیرفعال‌بودنِ صفحه به RIE آسیب نمی‌زند.

---

## ۹) لحن و اصول

- تشویقی و بدونِ قضاوت؛ خوابِ بد هرگز «شکست» نیست. روزشمار آرام، نه مضطرب.
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی.
- توضیحِ علّی شفاف («چرا این عدد؟»).
- **هیچ ارجاعی به چرخه** در این صفحه دیده نمی‌شود (محرمانه و خارج از این سیستم).
- درصد/همبستگی همیشه مشتق از داده‌ی واقعی.

---

## ۱۰) ترتیبِ ساخت (خلاصه)

S1 مهاجرت (گسترشِ `bedtime_diagnostics` + تنظیمات) → S2 مدل‌ها → S3 موتورِ خواب (بدهی/ثبات/همبستگی) → S4 شیتِ ثبتِ دیشب (+پلِ موتور) → S5 شیتِ هدف‌گذاری → S6 هیرو + روزشمار → S7 تبِ دیشب (برنامه vs واقعیت) → S8 تبِ الگوها → S9 تبِ روند + همبستگی → S10 مونتاژ + هاب → S11 دستیارِ AI + یادآورِ wind-down + پرداخت.
