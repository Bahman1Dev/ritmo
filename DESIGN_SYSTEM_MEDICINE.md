# 🏥 طراحی جامع «پرونده سلامت» (Health Hub) — صفحه‌ی درمان ریتمو

> فاز: طراحی مو‌به‌مو · وضعیت: منتظر بازخورد مالک
> نام نهایی: **«سلامت»** (به‌جای «درمان» — چون حالا خیلی فراتر از دارو رفته. مالک تأیید کند.)

---

## 🧬 فلسفه: چرا این صفحه خاص است؟

این **تنها** جایی در ریتمو است که:
- رنگ **قرمز** مجاز است (هشدار ایمنی پزشکی)
- دقت و صحت داده‌ها **حیاتی** است (نه تشویقی-تفریحی مثل بقیه)
- داده‌ها **حساس و خصوصی** هستند (رمزگذاری، بدون اشتراک‌گذاری، بدون AI خودسر)
- کاربر ممکن است **جانش** به این داده‌ها وابسته باشد (دیابت، فشارخون، بارداری)

> **اصل طلایی:** این صفحه **همیار پزشک** است، نه **جایگزین پزشک**. هرگز تشخیص یا توصیه‌ی پزشکی نمی‌دهد. فقط ثبت، یادآوری، و روندنمایی می‌کند.

---

## 🗺 نقشه‌ی کامل صفحه (نمای کلی)

```
┌──────────────────────────────────────────────────┐
│ ← سلامت                     [🔔] [⚙️] [🤖 AI]  │ هدر
├──────────────────────────────────────────────────┤
│                                                    │
│  ⚠️ نوار هشدارهای بحرانی (شرطی، قرمز/کهربایی)    │ ← فقط در صورت نیاز
│                                                    │
│  ┌─ 💊 داروهای امروز ──────────────────────────┐ │ ← بخش ۱
│  │ ▸ ۳ از ۵ دارو مصرف شده · دوز بعدی ۱۴:۳۰       │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ 🩺 نوبت‌های پزشک ──────────────────────────┐ │ ← بخش ۲
│  │ ▸ ۵ مرداد · دکتر شریفی (قلب) · ساعت ۱۰:۰۰     │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ 🩸 پایش سلامت ─────────────────────────────┐ │ ← بخش ۳
│  │ ▸ قند خون: ۱۱۰ ● فشار: ۱۲/۸ ● وزن: ۷۲         │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ 🧪 آزمایش‌ها و مدارک پزشکی ────────────────┐ │ ← بخش ۴
│  │ ▸ ۳ آزمایش ثبت شده · ۲ مدرک                   │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ 🤰 بارداری (فقط کاربر زن) ──────────────────┐ │ ← بخش ۵
│  │ ▸ هفته ۲۴ · سه‌ماهه دوم · ویزیت بعدی: ۱۰ مرداد│ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ 💉 واکسن‌ها ────────────────────────────────┐ │ ← بخش ۶
│  │ ▸ کزاز: ۱۴۰۲/۰۶/۱۵ · یادآور: ۱۴۰۶/۰۶/۱۵      │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ ⚠️ آلرژی‌ها و حساسیت‌ها ───────────────────┐ │ ← بخش ۷
│  │ ▸ پنی‌سیلین · لاتکس · گربه                     │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  ┌─ 📋 پرونده پزشکی ────────────────────────────┐ │ ← بخش ۸
│  │ ▸ گروه خون: +O · بیماری‌ها: دیابت نوع ۲       │ │
│  └──────────────────────────────────────────────┘ │
│                                                    │
│  [➕] دکمه شناور — افزودن سریع (حساس به context)  │
└──────────────────────────────────────────────────┘
```

**ساختار:** یک `ListView` عمودی از **۸ بخش جمع‌شونده (Collapsible)**. هر بخش:
- **بسته:** یک ردیف خلاصه (آیکون + عنوان + آخرین مقدار/وضعیت + تعداد)
- **باز:** محتوای کامل (لیست، نمودار، دکمه‌های اقدام)
- بخش‌های خالی: جمع‌شده و با راهنمای «ثبت اولین...»
- بخش بارداری: کاملاً مخفی اگر `userGender != 'FEMALE'` یا `isPregnant == false`.

---

# بخش‌به‌بخش (مو به مو)

---

## بخش ۱ — 💊 داروها (Medications)

> داده: `routines` با `category='MEDICAL'` · `prn_logs` · `medical_engine`

### نمای بسته (خلاصه)
`«۳ از ۵ دارو مصرف شده · دوز بعدی: استامینوفن ۱۴:۳۰»`

### نمای باز
**تب داخلی ۱: امروز**
- کارت پیشرفت: حلقه + «۳/۵»
- لیست داروهای امروز (مرتب: انجام‌نشده اول)
- هر ردیف: نام، دوز، ساعت، نوار موجودی، دکمه‌ی «💊 مصرف کردم»
- دکمه‌ی مصرف:
  - چک `checkOverdoseStatus` قبل از ثبت
  - اگر `warningUnderInterval` یا `warningMaxLimitExceeded` → **دکمه غیرفعال** + هشدار قرمز
  - در غیر این صورت: دیالوگ تأیید → کاهش موجودی → ثبت `prn_log`

**تب داخلی ۲: همه‌ی داروها**
- لیست تمام داروها (فعال/آرشیو) با نوار موجودی بصری
- هر کارت: swipe ویرایش/آرشیو
- دکمه‌ی شناور «➕ داروی جدید»

**تب داخلی ۳: تاریخچه مصرف**
- لیست `prn_logs` گروه‌بندی بر اساس تاریخ
- نمودار مصرف هفتگی

### شیت افزودن/ویرایش دارو
```
نام دارو · دوز (متن آزاد) · نوع (ثابت/PRN)
— اگر ثابت: ساعت‌های مصرف (چندتایی)
— اگر PRN: حداقل فاصله (ساعت) · سقف روزانه
موجودی اولیه · آستانه هشدار · یادداشت
```

### هشدارهای بحرانی دارو (در نوار بالای صفحه)
| وضعیت | رنگ | متن |
|-------|-----|-----|
| `isRefillNeeded` | کهربایی | «موجودی [نام] رو به اتمام است ([N] عدد)» |
| `warningUnderInterval` | قرمز | «فاصله مصرف [نام] رعایت نشده ([X] ساعت مانده)» — دکمه غیرفعال |
| `warningMaxLimitExceeded` | قرمز | «سقف روزانه [نام] ([N] دوز) پر شده» — دکمه غیرفعال |

---

## بخش ۲ — 🩺 نوبت‌های پزشک (Doctor Visits)

> **جدول جدید:** `doctor_visits`

### اسکیما
```sql
CREATE TABLE doctor_visits (
    id TEXT PRIMARY KEY,
    doctorName TEXT NOT NULL,
    specialty TEXT,              -- تخصص: قلب، پوست، زنان...
    clinicName TEXT,             -- نام مطب/بیمارستان
    clinicAddress TEXT,          -- آدرس
    clinicPhone TEXT,            -- تلفن
    visitDateTime INTEGER NOT NULL,  -- تاریخ/ساعت ویزیت (epoch)
    visitType TEXT NOT NULL DEFAULT 'IN_PERSON',  -- IN_PERSON / ONLINE / PHONE
    status TEXT NOT NULL DEFAULT 'UPCOMING',  -- UPCOMING / DONE / CANCELLED
    reason TEXT,                 -- علت مراجعه
    doctorNotes TEXT,            -- notes from doctor
    userNotes TEXT,              -- یادداشت کاربر
    followUpDate INTEGER,        -- تاریخ مراجعه بعدی
    reminderBefore INTEGER NOT NULL DEFAULT 60,  -- چند دقیقه قبل یادآوری
    attachmentPath TEXT,         -- عکس نسخه
    createdAt INTEGER NOT NULL,
    updatedAt INTEGER NOT NULL
);
```

### نمای بسته
`«نوبت بعدی: ۵ مرداد · دکتر شریفی (قلب) · ۱۰:۰۰ صبح»`

اگر نوبت فعال نباشد: `«نوبتی ثبت نشده»`

### نمای باز
**بالا: کارت نوبت بعدی** (برجسته، با شمارش معکوس)
- نام دکتر + تخصص + آیکون
- تاریخ و ساعت
- آدرس مطب (قابل لمس → نقشه)
- دکمه‌ی «📞 تماس» (اگر شماره ثبت شده)
- دکمه‌ی «📝 یادداشت قبل از ویزیت»

**پایین: تاریخچه ویزیت‌ها**
- لیست ویزیت‌های گذشته (جدیدترین اول)
- هر ردیف: تاریخ، دکتر، تخصص، علت، نتیجه
- لمس → جزئیات کامل + یادداشت‌ها + عکس نسخه

### شیت افزودن نوبت
```
نام دکتر · تخصص · نام مطب/بیمارستان · آدرس · تلفن
تاریخ و ساعت ویزیت
نوع: حضوری / آنلاین / تلفنی
علت مراجعه
یادآوری: [۶۰] دقیقه قبل
عکس نسخه (اختیاری)
```

### یادآوری خودکار
- از `alarm_scheduler_service` موجود استفاده می‌کند.
- اعلان: «نوبت دکتر [نام] — [تخصص] — فردا ساعت [ساعت]» (هم شب قبل، هم روز ویزیت).

---

## بخش ۳ — 🩸 پایش سلامت (Health Monitoring)

> **جداول جدید:** `blood_sugar_logs` · `blood_pressure_logs` · `vital_signs_logs`

### ۳-الف: قند خون (Blood Sugar)

```sql
CREATE TABLE blood_sugar_logs (
    id TEXT PRIMARY KEY,
    value INTEGER NOT NULL,         -- mg/dL
    measurementType TEXT NOT NULL DEFAULT 'FASTING',
        -- 'FASTING', 'BEFORE_MEAL', 'AFTER_MEAL', 'BEDTIME', 'RANDOM'
    note TEXT,
    loggedAt INTEGER NOT NULL
);
```

**ویژگی‌ها:**
- فعال/غیرفعال‌سازی از طریق تنظیمات (`patient_has_diabetes`)
- ثبت سریع: عدد + نوع (ناشتا/قبل غذا/بعد غذا/خواب/تصادفی)
- **محدوده‌ی هدف قابل تنظیم** (مثلاً ۸۰–۱۳۰ ناشتا)
- نمایش: آخرین مقدار + برچسب محدوده (نرمال/بالا/پایین) با رنگ
  - سبز: در محدوده · کهربایی: نزدیک مرز · قرمز: خارج محدوده
- **نمودار:** هفتگی/ماهانه با خط محدوده

### ۳-ب: فشار خون (Blood Pressure)

```sql
CREATE TABLE blood_pressure_logs (
    id TEXT PRIMARY KEY,
    systolic INTEGER NOT NULL,      -- فشار سیستول
    diastolic INTEGER NOT NULL,     -- فشار دیاستول
    pulse INTEGER,                  -- ضربان قلب
    arm TEXT DEFAULT 'LEFT',        -- دست چپ/راست
    position TEXT DEFAULT 'SITTING', -- نشسته/خوابیده/ایستاده
    note TEXT,
    loggedAt INTEGER NOT NULL
);
```

**ویژگی‌ها:**
- ثبت: سیستول / دیاستول / ضربان (اختیاری)
- محدوده‌های استاندارد: نرمال (<۱۲۰/۸۰) · پیش‌فشار · فشار بالا مرحله ۱ · مرحله ۲ · بحران
- برچسب خودکار + رنگ
- **نمودار** با دو خط (سیستول + دیاستول)

### ۳-ج: علائم حیاتی و وزن (Vital Signs)

```sql
CREATE TABLE vital_signs_logs (
    id TEXT PRIMARY KEY,
    vitalType TEXT NOT NULL,
        -- 'WEIGHT', 'TEMPERATURE', 'OXYGEN', 'WAIST', 'BMI'
    value REAL NOT NULL,
    unit TEXT NOT NULL,             -- 'kg', '°C', '%', 'cm'
    note TEXT,
    loggedAt INTEGER NOT NULL
);
```

**ویژگی‌ها:**
- وزن (نمودار روند — کاهش/افزایش)
- دمای بدن
- سطح اکسیژن خون (پالس اکسیمتر)
- دور کمر
- BMI خودکار (اگر قد در پروفایل پزشکی ثبت شده باشد)

---

## بخش ۴ — 🧪 آزمایش‌ها و مدارک پزشکی (Lab Results & Documents)

> **جداول جدید:** `lab_results` · `lab_result_images`

```sql
CREATE TABLE lab_results (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,             -- e.g. «آزمایش خون کامل»
    labName TEXT,                    -- نام آزمایشگاه
    testDate INTEGER NOT NULL,       -- تاریخ آزمایش
    category TEXT,                   -- خون / ادرار / تصویربرداری / پاتولوژی / ...
    summary TEXT,                    -- خلاصه‌ی نتیجه
    doctorNotes TEXT,                -- تفسیر پزشک
    userNotes TEXT,                  -- یادداشت کاربر
    createdAt INTEGER NOT NULL,
    updatedAt INTEGER NOT NULL
);

CREATE TABLE lab_result_images (
    id TEXT PRIMARY KEY,
    labResultId TEXT NOT NULL,
    imagePath TEXT NOT NULL,         -- مسیر فایل تصویر
    pageNumber INTEGER DEFAULT 1,   -- شماره صفحه (آزمایش‌های چندصفحه‌ای)
    caption TEXT,                    -- توضیح عکس
    FOREIGN KEY(labResultId) REFERENCES lab_results(id) ON DELETE CASCADE
);
```

### نمای بسته
`«۳ آزمایش ثبت شده · آخرین: آزمایش خون (۱۲ خرداد)»`

### نمای باز
- **لیست آزمایش‌ها** (جدیدترین اول)، هر ردیف: عنوان + تاریخ + نام آزمایشگاه + دسته
- لمس هر آزمایش → **صفحه‌ی جزئیات:**
  - اطلاعات کامل + خلاصه + تفسیر پزشک
  - **گالری تصاویر** (قابل بزرگ‌نمایی/زوم، swipe بین صفحات)
  - دکمه‌ی «➕ افزودن تصویر» (دوربین/گالری)
- **آپلود هوشمند:** کاربر از صفحه‌ی آزمایش عکس بگیرد → crop → ذخیره
- دکمه‌ی «📤 اشتراک‌گذاری با پزشک» (PDF یا ZIP تصاویر + خلاصه)

### شیت افزودن آزمایش
```
عنوان آزمایش · آزمایشگاه · تاریخ
دسته: خون / ادرار / تصویربرداری / پاتولوژی / سایر
خلاصه نتیجه
یادداشت/تفسیر پزشک
[➕ افزودن تصویر]
```

### مدارک پزشکی (علاوه بر آزمایش)
- نسخه‌های پزشک (عکس)
- جواب سونوگرافی/ام‌آر‌آی/سی‌تی
- کارت بیمه
- گواهی‌های پزشکی
- دفترچه‌ی بیمه

> همه در همان `lab_results` با دسته‌بندی `category` مدیریت می‌شوند. نام جدول `lab_results` را به `medical_documents` تغییر دهیم؟ (سؤال برای مالک)

---

## بخش ۵ — 🤰 بارداری (Pregnancy) 🔒 فقط کاربر زن

> **جدول جدید:** `pregnancy_tracker` · **شرط نمایش:** `userGender == 'FEMALE'` و `isPregnant == true` در `app_settings`

```sql
CREATE TABLE pregnancy_tracker (
    id TEXT PRIMARY KEY,
    lmpDate TEXT NOT NULL,            -- اولین روز آخرین قاعدگی
    estimatedDueDate TEXT NOT NULL,   -- تاریخ زایمان تخمینی
    currentWeek INTEGER NOT NULL,     -- هفته فعلی (محاسبه خودکار)
    currentTrimester INTEGER NOT NULL, -- سه‌ماهه (محاسبه خودکار)
    isActive INTEGER NOT NULL DEFAULT 1,
    notes TEXT,
    createdAt INTEGER NOT NULL,
    updatedAt INTEGER NOT NULL
);

CREATE TABLE pregnancy_checkups (
    id TEXT PRIMARY KEY,
    pregnancyId TEXT NOT NULL,
    title TEXT NOT NULL,              -- e.g. «سونوگرافی آنومالی»
    scheduledDate INTEGER,            -- تاریخ پیشنهادی
    actualDate INTEGER,               -- تاریخ واقعی
    type TEXT NOT NULL,               -- ULTRASOUND / BLOOD_TEST / CHECKUP / VACCINE
    result TEXT,                       -- نتیجه
    notes TEXT,
    isCompleted INTEGER DEFAULT 0,
    createdAt INTEGER NOT NULL,
    FOREIGN KEY(pregnancyId) REFERENCES pregnancy_tracker(id) ON DELETE CASCADE
);

CREATE TABLE pregnancy_symptoms (
    id TEXT PRIMARY KEY,
    pregnancyId TEXT NOT NULL,
    date TEXT NOT NULL,
    symptom TEXT NOT NULL,            -- تهوع، سردرد، کمر درد، ...
    severity TEXT DEFAULT 'MILD',     -- MILD / MODERATE / SEVERE
    note TEXT,
    FOREIGN KEY(pregnancyId) REFERENCES pregnancy_tracker(id) ON DELETE CASCADE
);

CREATE TABLE kick_counts (
    id TEXT PRIMARY KEY,
    pregnancyId TEXT NOT NULL,
    startTime INTEGER NOT NULL,       -- زمان شروع شمارش
    endTime INTEGER,                  -- زمان رسیدن به ۱۰ حرکت
    kickCount INTEGER,                -- تعداد حرکات
    loggedAt INTEGER NOT NULL,
    FOREIGN KEY(pregnancyId) REFERENCES pregnancy_tracker(id) ON DELETE CASCADE
);

CREATE TABLE contraction_timer (
    id TEXT PRIMARY KEY,
    pregnancyId TEXT NOT NULL,
    startTime INTEGER NOT NULL,
    endTime INTEGER,
    durationSeconds INTEGER,          -- طول انقباض
    intervalFromPrevious INTEGER,     -- فاصله از انقباض قبلی
    loggedAt INTEGER NOT NULL,
    FOREIGN KEY(pregnancyId) REFERENCES pregnancy_tracker(id) ON DELETE CASCADE
);
```

### نمای بسته (خلاصه)
`«🤰 هفته ۲۴ · سه‌ماهه دوم · ۱۱۲ روز تا زایمان»`

وقتی `isPregnant` false باشد، این بخش کاملاً مخفی است.

### نمای باز
**کارت Hero بارداری:**
- تصویر هفته به هفته (جنین در هفته جاری) — آیکون یا تصویر محلی
- «هفته ۲۴ از ۴۰» — نوار پیشرفت
- تاریخ زایمان تخمینی + شمارش معکوس
- سه‌ماهه: اول / دوم / سوم (با رنگ متفاوت)

**زیرمجموعه‌ها:**

**۱. جدول زمانی معاینات بارداری (Pregnancy Checkups):**
- لیست از پیش‌پر شده بر اساس دستورالعمل کشوری:
  - هفته ۶–۸: اولین ویزیت + سونوگرافی
  - هفته ۱۱–۱۳: سونوگرافی NT + غربالگری
  - هفته ۱۵–۲۰: سونوگرافی آنومالی
  - هفته ۲۴–۲۸: تست قند بارداری (GCT/GTT)
  - هفته ۲۸–۳۲: سونوگرافی رشد + واکسن کزاز
  - هفته ۳۲–۳۶: معاینه + NST
  - هفته ۳۶–۴۰: معاینه هفتگی + NST
- هر ردیف: انجام‌شده/نشده + تاریخ واقعی
- یادآوری خودکار نزدیک تاریخ

**۲. شمارشگر حرکت جنین (Kick Counter):**
- دکمه‌ی «▶️ شروع شمارش»
- تایمر: «حرکت ۱ از ۱۰ · ۴:۳۲ گذشته»
- ثبت خودکار زمانِ ۱۰ حرکت
- تاریخچه‌ی شمارش‌ها

**۳. تایمر انقباض (Contraction Timer):**
- دکمه‌ی «⏱ شروع انقباض» / «⏱ پایان انقباض»
- نمایش: طول، فاصله از قبلی، تعداد در ۱۰ دقیقه
- هشدار: «الگوی ۵-۱-۱» (انقباض هر ۵ دقیقه، طول ۱ دقیقه، ۱ ساعت مداوم) → «زمان مراجعه به بیمارستان فرا رسیده»

**۴. علائم روزانه:**
- ثبت سریع: تهوع، سردرد، کمر درد، ورم، ...
- شدت: خفیف/متوسط/شدید
- روند هفتگی

**۵. افزایش وزن بارداری:**
- نمودار وزن در طول بارداری
- خط راهنما (محدوده‌ی نرمال)

**۶. یادآورهای مکمل:**
- آهن، اسیدفولیک، کلسیم، ویتامین D
- (از بخش داروها — لینک داده میشود)

---

## بخش ۶ — 💉 واکسن‌ها (Vaccinations)

> **جدول جدید:** `vaccinations`

```sql
CREATE TABLE vaccinations (
    id TEXT PRIMARY KEY,
    vaccineName TEXT NOT NULL,         -- نام واکسن
    diseaseTarget TEXT,                -- بیماری هدف
    doseNumber INTEGER DEFAULT 1,      -- دوز چندم
    totalDoses INTEGER,                -- تعداد کل دوز
    dateAdministered INTEGER,          -- تاریخ تزریق
    nextDoseDue INTEGER,               -- تاریخ دوز بعدی
    batchNumber TEXT,                  -- شماره بچ
    clinicName TEXT,                   -- محل تزریق
    notes TEXT,
    createdAt INTEGER NOT NULL,
    updatedAt INTEGER NOT NULL
);
```

### نمای بسته
`«آخرین: کزاز (۱۴۰۲/۰۶/۱۵) · یادآور: ۱۴۰۶/۰۶/۱۵»`

### نمای باز
- **واکسن‌های تزریق‌شده:** لیست با تاریخ + شماره بچ
- **واکسن‌های در نوبت:** تاریخ یادآور + شمارش معکوس
- **جدول واکسن کشوری:** قابل فعال‌سازی (کزاز هر ۱۰ سال، آنفولانزا سالانه، ...)

---

## بخش ۷ — ⚠️ آلرژی‌ها و حساسیت‌ها (Allergies)

> **جدول جدید:** `allergies`

```sql
CREATE TABLE allergies (
    id TEXT PRIMARY KEY,
    allergen TEXT NOT NULL,            -- آلرژن
    category TEXT NOT NULL,            -- دارو / غذا / محیطی / حیوان / ...
    reaction TEXT,                     -- واکنش (راش، آنافیلاکسی، ...)
    severity TEXT NOT NULL DEFAULT 'MODERATE',  -- MILD / MODERATE / SEVERE / LIFE_THREATENING
    diagnosedDate INTEGER,            -- تاریخ تشخیص
    notes TEXT,
    createdAt INTEGER NOT NULL
);
```

### نمای بسته
`«۳ آلرژی ثبت شده · شدید: پنی‌سیلین»`

### نمای باز
- **هشدار بالای صفحه** (همیشه): «در صورت مراجعه به پزشک، این لیست را نشان دهید»
- **لیست آلرژی‌ها** با برچسب شدت:
  - 🟢 خفیف · 🟡 متوسط · 🟠 شدید · 🔴 تهدیدکننده‌ی حیات
- **اتصال به داروها:** اگر کاربر دارویی با آلرژن مشابه اضافه کند → **هشدار قرمز** (فقط تطبیق متنی ساده، نه تشخیص پزشکی)
- **کارت اشتراک‌گذاری:** «نمایش به پزشک» — یک کارت خلاصه با فونت درشت

---

## بخش ۸ — 📋 پرونده پزشکی (Medical Profile)

> **جدول جدید:** `medical_profile` · یا استفاده از `app_settings` برای موارد ساده

```sql
CREATE TABLE medical_profile (
    id TEXT PRIMARY KEY,
    profileKey TEXT NOT NULL UNIQUE,   -- 'blood_type', 'chronic_conditions', ...
    profileValue TEXT NOT NULL,        -- مقدار (متن یا JSON)
    updatedAt INTEGER NOT NULL
);

-- مقادیر پیشنهادی:
-- blood_type: 'O+', 'A-', 'B+', 'AB+', ...
-- chronic_conditions: 'دیابت نوع ۲, فشار خون'
-- current_medications_summary: متن آزاد (خلاصه برای پزشک)
-- emergency_contact_name
-- emergency_contact_phone
-- emergency_contact_relation
-- primary_physician_name
-- primary_physician_phone
-- insurance_name
-- insurance_id
-- height_cm
-- organ_donor: 'true'/'false'
-- medical_notes: متن آزاد
```

### نمای باز
**لیست فیلدها (قابل ویرایش):**
- گروه خون
- قد (سانتی‌متر)
- بیماری‌های مزمن
- پزشک معالج + تلفن
- مخاطب اضطراری + نسبت + تلفن
- بیمه (نام + شماره)
- اهدای عضو
- یادداشت پزشکی

> این بخش عمدتاً **تنظیمات** است — یک‌بار پر می‌شود و به‌ندرت تغییر می‌کند.

---

## 🤖 نقش AI در سلامت

AI به صفحه‌ی سلامت **دسترسی محدود و کنترل‌شده** دارد:

| مجاز ✅ | ممنوع ❌ |
|--------|---------|
| یادآوری زمان دارو بر اساس برنامه | تغییر خودکار دوز یا فاصله |
| پیشنهاد زمان‌بندی ویزیت (خالی بودن تقویم) | تشخیص پزشکی از روی علائم |
| خلاصه‌سازی گزارش سلامت برای پزشک | تفسیر نتیجه‌ی آزمایش |
| تبدیل عکس آزمایش به متن (OCR) | توصیه‌ی داروی جدید |
| **یادآوری منفعل:** «امروز فشار خونت رو ثبت نکردی» | **تصمیم‌گیری فعال:** «فشارت بالاست، برو بیمارستان» |

> **حریم خصوصی:** هیچ داده‌ی سلامت هرگز به سرور خارجی ارسال نمی‌شود. OCR و خلاصه‌سازی روی دستگاه انجام می‌شود. بارداری کاملاً آفلاین و خصوصی است.

---

## 🔗 اتصالات به سایر سیستم‌ها

| سیستم | نوع اتصال |
|-------|-----------|
| **چرخه بدن** | تاریخ LMP برای محاسبه بارداری ← از `cycle_logs` خوانده می‌شود. اگر کاربر باردار است، `suppressedPrayer` در چرخه (به دلیل قطع قاعدگی) غیرفعال شود. |
| **عبادت** | بارداری: روزه‌داری ممنوع ← وزن فصل عبادت کاهش یابد. قاعدگی: `suppressedPrayer` ← اوقات شرعی یادآوری نکند. |
| **خواب** | داروهای خواب‌آور ← اتصال به bedtime. (فاز ۲) |
| **تقویم** | ویزیت‌ها و یادآور واکسن روی تقویم نمایش داده شوند. |
| **ثبت جدید** | داروها هرگز از فلوی عمومی اضافه نشوند؛ فقط از این صفحه. (از قبل قفل شده) |
| **دستیار AI** | ✅ دسترسی مشورتی با محدودیت‌های بالا. |

---

## 🎨 طراحی بصری اختصاصی سلامت

| المان | مقدار |
|-------|-------|
| رنگ اصلی | آبی پزشکی `#0EA5E9` |
| رنگ هشدار | قرمز `#E5736B` + کهربایی `#F5B95B` |
| بک‌گراند کارت | `#FFFFFF` (روشن) / `#1E293B` (تاریک) — کمی جدی‌تر از بقیه |
| نوار موجودی دارو | گرادیان `#34D399 → #F5B95B → #E5736B` |
| فونت اعداد | واضح، درشت، با فاصله (خوانایی سریع در اورژانس) |
| لحن کلی | حرفه‌ای، تمیز، بدون تزئینات اضافه. «اطلاعات، نه سرگرمی.» |

---

## 📊 جداول جدید — جمع‌بندی (۷ جدول برای مهاجرت)

| # | نام جدول | کاربرد | شرطی؟ |
|---|---------|--------|-------|
| ۱ | `doctor_visits` | نوبت و تاریخچه پزشک | ❌ |
| ۲ | `blood_sugar_logs` | ثبت قند خون | ❌ (همه می‌توانند استفاده کنند) |
| ۳ | `blood_pressure_logs` | ثبت فشار خون | ❌ |
| ۴ | `vital_signs_logs` | وزن، دما، اکسیژن | ❌ |
| ۵ | `medical_documents` | آزمایش‌ها و مدارک پزشکی | ❌ |
| ۶ | `vaccinations` | واکسن‌ها | ❌ |
| ۷ | `allergies` | آلرژی‌ها | ❌ |
| ۸ | `medical_profile` | پرونده پزشکی (گروه خون و...) | ❌ |
| ۹ | `pregnancy_tracker` | بارداری | ✅ فقط زن + باردار |
| ۱۰ | `pregnancy_checkups` | معاینات بارداری | ✅ |
| ۱۱ | `pregnancy_symptoms` | علائم بارداری | ✅ |
| ۱۲ | `kick_counts` | شمارش حرکت جنین | ✅ |
| ۱۳ | `contraction_timer` | تایمر انقباض | ✅ |

---

## ✅ تصمیم‌های قطعی (قفل‌شده)

1. **نام نهایی:** «سلامت» (در هدر، تب و همه‌جا).
2. **جدول مدارک:** `medical_documents` — شامل آزمایش، نسخه، سونوگرافی، MRI، کارت بیمه و هر مدرک پزشکی دیگر.
3. **رفتار PRN = سخت‌گیرانه (Hard Block):** وقتی `warningUnderInterval` یا `warningMaxLimitExceeded`، دکمه‌ی «مصرف کردم» **کاملاً غیرفعال** و با متن شمارش معکوس جایگزین می‌شود: «۳ ساعت و ۱۵ دقیقه مانده تا دوز بعدی». (این تنها جای اپ است که ایمنی بر راحتی اولویت دارد.)
4. **بارداری فاز ۱ = کامل:** همه‌ی ۵ زیربخش (هفته‌شمار، معاینات، حرکت جنین، تایمر انقباض، علائم) یکجا ساخته می‌شوند.
5. **یادآورهای بهداشت شخصی (مسواک/لنز/نواربهداشتی):** ساخته نمی‌شوند (زیاده‌روی). همین ۸ بخش کافی است.
6. **حریم خصوصی:** هیچ داده‌ی سلامت به سرور خارجی ارسال نمی‌شود. OCR و AI روی دستگاه. بارداری کاملاً آفلاین.

> این تصمیم‌ها مبنای `AGENT_PROMPT_HEALTH.md` هستند.
