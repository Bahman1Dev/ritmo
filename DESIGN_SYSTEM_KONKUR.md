# 🎓 سند طراحی — سیستم «کنکور» (Konkur) — بازطراحی کامل

> این سند برای **مالکِ محصول** است (نه برای Gemini). معماری، تصمیم‌ها و منطقِ UX را توضیح می‌دهد.
> سند اجراییِ Gemini: `AGENT_PROMPT_KONKUR.md`.
> رنگِ هویتِ سیستم: بنفش `#8B5CF6` (همان رنگِ فعلیِ کاشیِ کنکور در هاب).

---

## ۱) چرا بازطراحی؟ — وضعیت فعلی

صفحه‌ی فعلی (`konkur_dashboard_screen.dart`، ۱۰۵۹ خط) یک **CRUD ساده** است، نه یک «سیستم کنکور». سه تب دارد (خلاصه آمار / درخت مباحث / کارنامه آزمون) ولی:

| کمبود | توضیح |
|---|---|
| ❌ روزشمار کنکور | نمادین‌ترین قابلیتِ هر اپ کنکور — اصلاً وجود ندارد. |
| ❌ بودجه‌بندی | کاشیِ هاب «بودجه‌بندی» را وعده می‌دهد ولی صفحه ندارد. «این مبحث چند تست در کنکور دارد» نیست. |
| ❌ درصدِ دستی | درصدِ پیشرفت را کاربر **با دست تایپ می‌کند** — از مطالعه یا تست استخراج نمی‌شود. |
| ❌ تست‌زنی واقعی | صحیح/غلط/نزده و درصدِ منفی‌دار (نمره‌ی منفی) محاسبه نمی‌شود. |
| ❌ تایمر مطالعه | دقیقه‌ها دستی تایپ می‌شوند (دوره‌ها تایمر واقعی دارد). |
| ❌ برنامه‌ریزی | عنوانِ صفحه «برنامه‌ریزی» است ولی هیچ برنامه‌ای ساخته نمی‌شود. |
| ❌ تاریخ شمسی | تاریخ‌ها میلادیِ ISO هستند. |
| ❌ نمودار روند | روندِ درصد هر درس بین آزمون‌ها — قلبِ تحلیلِ کنکور — نیست. |
| ❌ پریست رشته | کاربر باید همه‌ی دروس را دستی بسازد؛ رشته/ضرایب از پیش نیست. |
| ❌ اتصال | هیچ اتصالی به انرژی/داشبورد/AI مثل بقیه‌ی سیستم‌ها نیست. |

**نتیجه:** برای «کامل شدن تا جای اپ‌های کنکور را بگیرد»، تقریباً بازسازیِ کامل روی همان ۴ جدولِ موجود لازم است.

---

## ۲) چهار تصمیمِ قطعی (تأییدشده توسط مالک)

1. **تراز/رتبه → فقط درصد + روند.** هیچ تخمینِ رتبه/ترازِ کشوری زده نمی‌شود (بدون دیتای مرجعِ سنجش گمراه‌کننده است). کارنامه = درصدِ هر درس + نمودارِ روندِ درصد بین آزمون‌ها. صادق و دقیق.
2. **برنامه‌ریزی → برنامه‌سازِ کامل + اتصال به «امروز».** موتوری که مباحثِ باقی‌مانده را تا تاریخِ کنکور (وزن‌دار با ضریب × بودجه × سطحِ تسلط) روی روزها پخش می‌کند و آیتم‌های مطالعه‌ی امروز را در داشبوردِ «امروز» نشان می‌دهد.
3. **مستقلِ کامل از «دوره‌ها».** کنکور تایمر و موتورِ خودش را دارد؛ هیچ وابستگی‌ای به سیستمِ دوره‌ها ندارد. (تمیزتر؛ منطقِ تایمر در کنکور بازنوشته می‌شود.)
4. **هر ۵ رشته + ضرایب.** ریاضی، تجربی، انسانی، هنر، زبان — با لیستِ دروسِ اختصاصی (+عمومیِ اختیاری) و ضرایب از پیش بارگذاری شوند. کاربر رشته‌اش را انتخاب می‌کند و همه‌چیز آماده است.

---

## ۳) مدلِ ذهنیِ سیستم

سه لایه روی هم:

```
┌─ بودجه‌بندی (چه چیزی مهم است) ──────────────┐
│   رشته → دروس (ضریب) → مباحث (تعداد تست در کنکور)
│   سطحِ تسلطِ هر مبحث: نخوانده/در حال خواندن/مسلط/نیاز به مرور
└──────────────────────────────────────────────┘
            ↓ تغذیه می‌کند
┌─ برنامه‌ریزی (کِی بخوانم) ───────────────────┐
│   موتورِ پخش: مباحثِ مهم‌تر و عقب‌مانده‌تر زودتر،
│   تا تاریخِ کنکور، با سقفِ دقیقه‌ی روزانه
│   → آیتم‌های امروز → داشبوردِ «امروز»
└──────────────────────────────────────────────┘
            ↓ ثبت می‌شود
┌─ ثبت و تحلیل (چقدر جلو رفتم) ────────────────┐
│   جلسه‌ی مطالعه/تست (تایمر + صحیح/غلط/نزده)
│   کارنامه‌ی آزمون → درصد منفی‌دار → نمودارِ روند
│   روزشمار + آمادگیِ کلی + استریک + درسِ ضعیف
└──────────────────────────────────────────────┘
```

نکته‌ی کلیدیِ طراحی: **درصدِ پیشرفت دیگر دستی تایپ نمی‌شود.** از دو منبعِ واقعی می‌آید: (الف) سطحِ تسلطِ مباحث (۴-حالته) و (ب) جلساتِ ثبت‌شده. درصدِ آمادگیِ هر درس = میانگینِ وزن‌دارِ تسلطِ مباحثش بر اساسِ بودجه‌بندی (مبحثی که تستِ بیشتری در کنکور دارد وزنِ بیشتری می‌گیرد).

---

## ۴) معماریِ داده

> همه‌ی جداولِ کنکوری از قبل وجود دارند؛ **گسترش می‌دهیم** (ALTER ADD COLUMN با مقدار پیش‌فرض) + ۲ جدولِ جدید. داده‌ی فعلی تستی است؛ حفظش مهم نیست، ولی چون ستون‌ها را با پیش‌فرض اضافه می‌کنیم رکوردهای قدیمی هم سالم می‌مانند.

### جداولِ موجود (گسترش‌یافته)
- **`konkur_subjects`** + `subjectGroup TEXT` (GENERAL/SPECIALIZED), `examQuestionCount INTEGER` (تعداد تستِ کلِ درس در کنکور)، `orderIndex INTEGER`, `colorHex TEXT`, `isPreset INTEGER`. (ضریب = همان `importanceFactor` موجود.)
- **`konkur_topics`** + `examQuestionCount INTEGER` (بودجه‌بندیِ مبحث)، `masteryLevel TEXT` (NOT_STARTED/LEARNING/MASTERED/NEEDS_REVIEW)، `lastStudiedAt INTEGER`، `nextReviewDate TEXT`، `plannedDate TEXT`، `orderIndex INTEGER`. (`progressPercentage` می‌ماند ولی از تسلط مشتق می‌شود.)
- **`konkur_mock_exams`** + `provider TEXT` (قلم‌چی/گاج/سنجش/…)، `note TEXT`. (`examDate` به ISO `YYYY-MM-DD` نرمال؛ نمایش شمسی.)
- **`konkur_mock_exam_results`** — همین حالا `correctAnswers/wrongAnswers/emptyAnswers/percentage` دارد. درصد از **فرمولِ منفی‌دار** خودکار محاسبه می‌شود (پایین). + `totalQuestions INTEGER`.

### جداولِ جدید
- **`konkur_study_sessions`** — ثبتِ واقعیِ مطالعه/تست (مستقل از دوره‌ها):
  `id, topicId, subjectId, dateIso, durationMinutes, mode (STUDY/TEST/REVIEW), testsTotal, testsCorrect, testsWrong, testsBlank, note, createdAt`.
  منبعِ یکتای «ساعتِ مطالعه»، «روند»، «استریک».
- **`konkur_plan_items`** — خروجیِ برنامه‌ساز (آیتم‌های روزانه):
  `id, dateIso, subjectId, topicId, plannedMinutes, status (PENDING/DONE/SKIPPED), createdAt`.
  منبعِ بخشِ «امروز» و قلابِ داشبورد.

### تنظیمات (`app_settings`)
`konkur_field` (UNSET/RIYAZI/TAJROBI/ENSANI/HONAR/ZABAN)، `konkur_exam_date` (ISO)، `konkur_setup_done` ('false')، `konkur_daily_target_minutes` ('180')، `konkur_show_in_dashboard` ('true' — قابل خاموش‌کردن)، `module_konkur_enabled` (موجود).

### فرمولِ درصدِ منفی‌دار (مهم)
```
درصد = ((۳ × صحیح) − غلط) / (۳ × کلِ سؤال) × ۱۰۰
```
کارنامه فقط صحیح/غلط/نزده + کلِ سؤال می‌گیرد؛ درصد خودکار حساب و ذخیره می‌شود (هم نمایش، هم برای روند). می‌تواند منفی شود.

---

## ۵) موتورها (مستقل، الگوی `CachedEngine`)

- **`konkur_planner.dart`** (منطقِ خالص، تست‌پذیر، بدون I/O): مباحثِ `masteryLevel != MASTERED` را تا تاریخِ کنکور روی روزها پخش می‌کند. اولویت = `examQuestionCount × ضریبِ درس`، و مباحثِ عقب‌مانده/ضعیف زودتر. سقف = `daily_target_minutes`. مرورِ مباحثِ MASTERED با فاصله (لایتنرِ ساده) در `nextReviewDate`.
- **`konkur_engine.dart`** (`CachedEngine` + `RitmoEngineBus`): خروجی‌ها — `daysUntilExam` (روزشمار)، `overallReadiness` (۰..۱، وزن‌دار با ضریب×بودجه×تسلط)، `studyMinutesTotal/thisWeek`، `studyStreakDays`، `perSubjectReadiness`، `perSubjectTrend` (درصد در آزمون‌های متوالی)، `budgetCoverage` (چند درصدِ بودجه‌بندی پوشش داده شده)، `weakestSubjects`، `todayPlanItems`.

---

## ۶) ساختار صفحه (KonkurScreen جدید، جایگزینِ صفحه‌ی فعلی)

**هیروی بالای صفحه:** روزشمارِ بزرگِ کنکور + رینگِ آمادگیِ کلی + «این هفته X ساعت مطالعه» + 🔥 استریک.

چهار بخش/تب:
1. **امروز / برنامه** — آیتم‌های مطالعه‌ی امروز [▶ شروع تایمر][✓ انجام]، نمای کلیِ برنامه تا کنکور، دکمه‌ی «بازچینش برنامه». حالتِ خالی: «امروز برنامه‌ای نیست — یه مبحث انتخاب کن 🌿».
2. **بودجه‌بندی (دروس و مباحث)** — درختواره: درس (ضریب، % آمادگی) ← مباحث با عددِ بودجه‌بندی (تعداد تست) + چیپِ سطحِ تسلطِ ۴-حالته + [مطالعه/تست]. نوارِ پوششِ بودجه.
3. **آزمون‌ها (کارنامه)** — لیستِ آزمون‌ها، ثبتِ کارنامه (صحیح/غلط/نزده → درصدِ منفی‌دارِ خودکار)، **نمودارِ روندِ درصد هر درس بین آزمون‌ها**.
4. **آمار** — مطالعه‌ی کل، روندِ کلی، درسِ ضعیف، تفکیکِ آمادگی.

**دستیار AI (آخرین لایه):** کمکِ برنامه‌ریزی، «الان کدوم مبحث رو بخونم؟» (آگاه از بودجه/عقب‌افتادگی)، تحلیلِ کارنامه. **همیشه Preview→Edit→Save؛ هرگز مستقیم در DB نمی‌نویسد.**

---

## ۷) جریانِ راه‌اندازیِ اولیه (Setup)

اولین ورود (`konkur_setup_done=false`):
1. انتخابِ **رشته** (۵ پریست).
2. seedِ خودکارِ دروس + مباحث + ضرایب + بودجه‌بندی از `konkur_presets.dart`.
3. انتخابِ **تاریخِ کنکور** (روزشمار).
4. (اختیاری) سقفِ دقیقه‌ی مطالعه‌ی روزانه.
5. `konkur_setup_done=true`.

کاربر بعداً می‌تواند درس/مبحث اضافه/حذف کند و ضرایب را ویرایش کند (پریست‌ها فقط نقطه‌ی شروع‌اند).

---

## ۸) اتصال‌ها و کنترلِ کاربر

- **داشبوردِ امروز:** بخشِ «📚 مطالعه‌ی امروز (کنکور)» آیتم‌های `konkur_plan_items(today, PENDING)` را نشان می‌دهد. پشتِ سوییچِ `konkur_show_in_dashboard` (پیش‌فرض روشن، قابل خاموش‌کردن در تنظیماتِ صفحه).
- **انرژی (سبک):** کارتِ آیتمِ امروز می‌تواند با انرژیِ پایین پیامِ ملایم بدهد («انرژی‌ت پایینه، یه مبحثِ سبک‌تر چطوره؟») — اختیاری، بدونِ اجبار.
- **بدونِ نوشتن در روتین‌ها/daily_rhythm.** آمار و تکمیلِ کنکور مستقل محاسبه می‌شود (مثل قانونِ دوره‌ها).

---

## ۹) لحن و اصول UX

- لحنِ تشویقی؛ برای عقب‌افتادگی هرگز سرزنش («۳ روز عقبی، جبران می‌کنی 💪»).
- روزشمار انگیزه بدهد، نه اضطراب (رنگِ آرام، نه قرمزِ هشدار).
- ارقام فارسی، RTL، Vazirmatn، تاریخِ شمسی همه‌جا.
- درصد همیشه از دادهٔ واقعی؛ هیچ عددِ ساختگی/تایپی نمایش داده نشود.

---

## ۱۰) ترتیبِ ساخت (خلاصه)

PASS 0 (ممیزی) → K1 مهاجرت 🔴 → K2 مدل‌ها → K3 پریست رشته‌ها → K4 برنامه‌ساز → K5 موتور → K6 هیرو/روزشمار → K7 راه‌اندازی → K8 امروز/برنامه → K9 بودجه‌بندی → K10 ثبتِ مطالعه/تست (تایمر) → K11 کارنامه + نمودار → K12 آمار → K13 مونتاژ + هاب → K14 قلابِ داشبورد 🔴 → K15 AI (آخر) → K16 پرداخت + تست.

🔴 = حساس به مهاجرت → بعدش توقف برای تأییدِ انسانی.
