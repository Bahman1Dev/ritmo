# 📓 سند طراحی — سیستم «خودارزیابی و بازتاب» (Reflection) — ارتقای صفحه

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_REFLECTION.md`.
> رنگِ هویت: فیروزه‌ای `#06B6D4` (همان رنگِ کاشیِ هاب). آیکن: `square_list_fill`.

---

## ۱) وضعیت فعلی — کم‌عمق‌ترین سیستم: یک شیتِ فقط‌نوشتنی

- ⚠️ کاشیِ هاب فقط یک `DailyReflectionSheet` باز می‌کند — **صفحه‌ی اختصاصی ندارد**، وضعیتش هاردکدِ «فعال» است، `onSaved` خالی، بدونِ ماژول‌فلگ.
- ⚠️ شیتِ تأمل تک‌مرحله‌ای: `mood_score`(۱..۵) + «بازتابِ روزانه» + «یادگیری‌ها» → جدولِ `daily_reflections` (یک رکورد در روز، `date UNIQUE`).
- ⚠️ شیتِ جدای **چک‌اینِ صبح** (`morning_checkin_sheet`): ۴ حالِ از‌پیش‌تعریف‌شده → `daily_checkins` + **پلِ انرژی** (`energy_logs` + `default_energy_level`).
- ❌ **فقط‌نوشتنی:** هیچ تاریخچه/بازخوانی، هیچ روند، هیچ استریک، هیچ تحلیل. داده ثبت می‌شود ولی هرگز به کاربر برنمی‌گردد.
- ❌ **یتیم از آنالیتیکس:** نه `ReflectionEngine`، نه ارجاع در insights. حالِ تأمل به تحلیلِ انرژی/حال وصل نیست.
- 🧹 سه ثبتِ موازیِ حال: `daily_checkins.mood` (دسته‌ای)، `daily_reflections.mood_score` (۱..۵)، `mood_logs.valence` — هم‌پوشانیِ بدونِ خطِ تحلیلِ مشترک.

**نتیجه:** عملاً از صفر باید **صفحه‌ی کاملِ ژورنالینگ/خودارزیابی** ساخته شود — روی همان جداولِ موجود، بدونِ شکستنِ پلِ انرژیِ چک‌اینِ صبح.

---

## ۲) چهار تصمیمِ قطعی (تأییدشده)

1. **مربیِ کاملِ تأمل.** صفحه‌ی اختصاصی: ثبتِ ساختاریافته + تایم‌لاینِ تاریخچه + استریک + روندِ حال/یادگیری + پرسش‌های پویای AI + همبستگی. جای یک اپِ ژورنالِ مستقل را بگیرد.
2. **پرسش‌های ساختاریافته (همه اختیاری).** حال + **بردِ امروز** + **شکرگزاری** + **چالش/دشواری** + **درسِ آموخته** + **تمرکزِ فردا**. راهنمایی‌شده ولی بدونِ اجبار.
3. **ژورنالِ یکپارچه‌ی روز.** هم چک‌اینِ صبح و هم تأملِ عصر در یک **تایم‌لاینِ روزانه** کنار هم؛ روایتِ کاملِ صبح‌تا‌شب. چک‌اینِ صبح **همچنان به انرژی پل می‌خورد** (دست‌نخورده).
4. **اتصالِ حالِ تأمل به تحلیلِ انرژی/حال — بدونِ تکرار.** حالِ تأمل **فقط‌خواندنی** در همبستگی استفاده می‌شود؛ **هیچ نوشتنِ تکراری** در `mood_logs`. الگوی صادق: «روزهایی که تأمل می‌کنی، حالت معمولاً بهتره».

---

## ۳) معماریِ داده

> گسترشِ حداقلی؛ بدونِ جدولِ جدید. جداولِ ثبتِ حال یکی نمی‌شوند (ریسکِ شکستن)، فقط تحلیلِ مشترک رویشان ساخته می‌شود.

`daily_reflections` گسترش می‌یابد (با `_safeAddColumn`, `DEFAULT`/nullable):

| ستون | نوع | معنی |
|---|---|---|
| موجود: `date`/`mood_score`/`reflection_text`/`learnings`/`timestamp`/`createdAt`/... | — | حفظ |
| `gratitude` | TEXT | شکرگزاری — جدید |
| `wins` | TEXT | بردِ امروز — جدید |
| `challenges` | TEXT | چالش/دشواری — جدید |
| `tomorrowFocus` | TEXT | تمرکزِ فردا — جدید |

- `daily_checkins` و `energy_logs` و `mood_logs` **دست‌نخورده**. تایم‌لاین فقط از آن‌ها می‌خواند.
- بدونِ ماژول‌فلگِ جدید: تأمل عادتِ روزانه است و باید بی‌اصطکاک بماند. کاشیِ هاب «فعال» می‌ماند ولی به‌جای شیت، **صفحه‌ی جدید** را باز می‌کند.

**تنظیمات (seed با `INSERT OR IGNORE`):** `reflection_reminder_enabled` = `true` (یادآورِ ملایمِ عصر، اگر زیرساختِ نوتیف باشد)، `reflection_prompt_style` = `structured`.

---

## ۴) موتور (`reflection_engine.dart`, الگوی `CachedEngine`)

ورودی (فقط‌خواندنی): `daily_reflections`, `daily_checkins`, `energy_logs`, `mood_logs`, `today`, `horizonDays`.
خروجی:
- `currentStreak` / `longestStreak` — روزهای پیاپیِ تأمل.
- `entryCount` / `completionRate` — نرخِ تأمل در بازه.
- `avgMoodScore` و `moodTrend` (از `mood_score`).
- `themeFrequency` — واژگانِ پرتکرار در متن (سبک، اختیاری).
- `reflectionEnergyCorrelation` / `reflectionMoodCorrelation` (در [-1..1]، اگر داده کم بود null) + `correlationInsight` (صادق).

منطقِ خالص در helperِ تست‌پذیر.

---

## ۵) ساختار صفحه (ReflectionScreen)

**هیرو:** وضعیتِ امروز (چک‌این شد؟ تأمل شد؟) + **نشانِ استریک** («۵ روز پیاپی 🌱») + دکمه‌ی «ثبتِ تأملِ امروز» (و اگر صبح است و چک‌این نشده، میان‌برِ چک‌اینِ صبح).

تب‌ها:
1. **امروز** — ثبتِ ساختاریافته‌ی تأمل (حال + برد + شکرگزاری + چالش + درس + تمرکزِ فردا) + کارتِ چک‌اینِ صبحِ امروز.
2. **تاریخچه** — **تایم‌لاینِ یکپارچه‌ی روز**: هر روز یک کارت با چک‌اینِ صبح + تأملِ عصر؛ تپ برای دیدن/ویرایش. حالتِ خالیِ تشویقی.
3. **روند** — نمودارِ حال در چند هفته + استریک + نرخِ تکمیل + پربسامدترین درس‌ها/تم‌ها + **همبستگیِ تأمل↔انرژی/حال** (صادق).

**دستیارِ AI (🤖):** **پرسشِ پویای تأمل** بر اساسِ رویداد/انرژیِ امروز («امروز عصر خسته بودی؛ چه چیزی بیشترین انرژی رو ازت گرفت؟») + خلاصه‌ی ملایمِ الگوها. فقط دستیار: Preview→Edit→Save، بدونِ نوشتنِ کور.

---

## ۶) لحن و اصول

- تشویقی و بدونِ قضاوت؛ روزِ بد هرگز «شکست» نیست؛ تأملِ ازدست‌رفته سرزنش نمی‌شود.
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی.
- شفافیتِ علّی در همبستگی‌ها («چرا این الگو؟»)، بدونِ ادعای قطعیتِ علّی.
- حریم: تأمل خصوصی است؛ متن‌ها جایی بیرون درز نکنند.
- AI هرگز مستقیم در DB نمی‌نویسد.

---

## ۷) فعال‌سازی (هاب)

کاشیِ «خودارزیابی و بازتاب» «فعال» می‌ماند (بدونِ گیتِ فعال‌سازی) ولی به‌جای `DailyReflectionSheet`، **`ReflectionScreen`** را باز می‌کند. بنرهای داشبورد (چک‌اینِ صبح/تأملِ عصر) دست‌نخورده کار می‌کنند.

---

## ۸) ترتیبِ ساخت (خلاصه)

R1 مهاجرت (گسترشِ `daily_reflections` + تنظیمات) → R2 مدل‌ها → R3 موتورِ تأمل (استریک/روند/همبستگی) → R4 شیتِ ثبتِ ساختاریافته → R5 تایم‌لاینِ یکپارچه‌ی روز → R6 هیرو + استریک → R7 تبِ روند → R8 بخشِ همبستگی → R9 مونتاژ + هاب → R10 دستیارِ AI (پرسشِ پویا) → R11 پلِ تحلیلِ بدونِ‌تکرار + پرداخت.
