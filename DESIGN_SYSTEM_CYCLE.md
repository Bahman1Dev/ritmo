# 🌸 سند طراحی — سیستم «چرخه بدن» (محرمانه) — **ارتقا (v2)**

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_CYCLE.md`.
> این سند جایگزینِ سندِ ساختِ اولیه (v14) است. سیستم ساخته شده؛ این مرحله **عمیق‌سازی** است.
> رنگِ هویت: صورتی `#EC4899`. آیکن: `heart_fill`. صفحه پشتِ **قفلِ PIN/بیومتریک**، **فقط برای کاربرِ زن**.
>
> ⚠️ **بازگشتِ مهم نسبت به v14:** در v14 «پنجره‌ی باروری و تخمک‌گذاری» یک ستونِ اصلی بود. در این نسخه **باروری کاملاً پنهان می‌شود** (تصمیمِ مالک). تمرکز فقط سلامت/علائم و پیش‌بینیِ خودِ عادت.

---

## ۱) وضعیت فعلی — سیستمِ نسبتاً پخته با چند شکافِ کلیدی (~۶٫۵/۱۰)

- ✅ معماریِ حریمِ خصوصیِ تمیز: `CyclePrivacyGuard.isVisible` (genderِ زن)، `module_cycle_enabled`، رضایتِ جداگانه به ازای هر مصرف‌کننده (`cycle_consent_worship/energy/reminders/dashboard`)، و `cycle_lock_gate` (PIN + بیومتریک، قفلِ خودکار در پس‌زمینه).
- ✅ آنبوردینگِ ۳ مرحله‌ای + داشبورد: orbِ فاز، ثبتِ شروع/پایانِ عادت، ثبتِ روزانه‌ی علائم (`cycle_day_logs`: flow/mood/energyTag/symptoms/note با `logDate UNIQUE`)، تقویمِ جلالی، آمارِ بی‌نظمی، خودمراقبتیِ فازی.
- ✅ موتور: `cycle_engine.dart` (`CachedEngine`) — فاز، روزِ چرخه/عادت، پیش‌بینیِ خطی، آمار، تشخیصِ بی‌نظمی، بلوغِ داده.
- ✅ پلِ غیرمستقیم: `CycleConsentBridge` (`isUserMenstruating`, `isEnergyTuned`) تنها کانالِ مجازِ تأثیر بر بقیه‌ی سیستم‌ها؛ هیچ ارجاعِ مستقیم بیرون پیدا نشد. ✅
- ⚠️ **پیش‌بینیِ خطیِ ساده** (طولِ ثابت، تخمک‌گذاریِ روزِ ۱۴): شخصی‌سازی‌نشده، بدونِ بازه‌ی عدم‌قطعیت.
- ⚠️ **بدونِ تحلیلِ روند** (طول/مدت در چند ماه)، **بدونِ همبستگیِ علائم**، **بدونِ فازِ PMS/پیش‌از‌عادت**.
- 🐞 پلِ فقهی stub است (`addFastingDebtIfNeeded` خالی) — تعلیقِ عبادت/روزه‌ی قضا پیاده نشده.
- 🧹 موتور/جدولِ legacy (`hormonal_intelligence_engine` + `cycle_logs`) موازیِ مسیرِ اصلی مانده (استفاده در CycleHarmonyScreen) — نباید شکسته شود، ولی منبعِ حقیقت `cycle_engine`+`cycle_periods` است.

**نتیجه:** زیرساخت و حریم آماده‌اند؛ کاری که مانده **عمیق‌سازی به مربیِ کاملِ سلامت** + **پلِ فقهی** + **همبستگی** است — با حفظِ کاملِ محرمانگی.

---

## ۲) چهار تصمیمِ قطعی (تأییدشده)

1. **مربیِ کاملِ سلامت.** تحلیلِ روندِ چندماهه + نمره‌ی نظم + همبستگیِ علائم + پیش‌بینیِ شخصی‌سازی‌شده (بازه‌ای) + فازِ PMS/پیش‌از‌عادت. جای یک اپِ مستقلِ پریود را بگیرد.
2. **باروری پنهان (قیدِ سخت).** **هیچ پنجره‌ی بارور و هیچ تخمک‌گذاری در UI نمایش داده نشود.** مارکرهای طلاییِ تقویم و فازِ تخمک‌گذاریِ orb حذف شوند. خروجی‌های باروریِ موتور به UI داده نشوند.
3. **پلِ فقهیِ کامل (با رضایت).** در ایامِ عادت و **فقط با `cycle_consent_worship`**: وزنِ روتین‌های عبادی (نماز/روزه) به‌صورتِ خودکار و غیرمستقیم در سمتِ عبادت تعلیق شود و **روزه‌ی قضا** شمرده و در یک دفترِ بدهی نگه‌داری شود. (پیاده‌سازیِ `addFastingDebtIfNeeded`.)
4. **همبستگیِ دوسطحی، با حفظِ محرمانگی.**
   - **درونِ این صفحه (vault، پشتِ PIN):** همبستگیِ **صریح** علائم/حال/جریان با انرژی/خواب/روتین (فقط‌خواندنی).
   - **بیرون از این صفحه (با رضایت):** تأثیر فقط از `CycleConsentBridge` و با **لحنِ غیرمستقیم** («بر اساس ریتمِ بدنی‌ات این روزها...») — **هرگز واژه‌ی صریحِ پریود/قاعدگی/چرخه** بیرون دیده نشود.

---

## ۳) معماریِ داده

> گسترشِ حداقلی؛ جداولِ موجود حفظ.

- `cycle_periods` / `cycle_day_logs` — دست‌نخورده (منبعِ حقیقت).
- جدولِ جدیدِ سبک برای پلِ فقهی: **`fasting_debt`** (`id PK, dateIso, daysOwed INTEGER DEFAULT 1, reason TEXT, isResolved INTEGER DEFAULT 0, createdAt, updatedAt`) — دفترِ روزه‌ی قضا. نوشتن فقط با رضایتِ عبادت، از مسیرِ Bridge.
- `cycle_day_logs` کافی است؛ علائمِ فازِ لوتئال/PMS هم در همان روزها ثبت می‌شوند (فاز از تاریخ مشتق می‌شود).

**تنظیمات (seed با `INSERT OR IGNORE`):**
- موجود حفظ: `module_cycle_enabled`, `cycle_avg_length`, `cycle_avg_period`, `cycle_setup_done`, `cycle_biometric_enabled`, `cycle_consent_worship/energy/reminders/dashboard`, `user_gender`.
- جدید: `cycle_consent_sleep` = `false` (تأثیرِ غیرمستقیم بر خواب)، `cycle_fertility_visible` = `false` (کلیدِ سختِ پنهان‌ماندنِ باروری)، `cycle_pms_window_days` = `4`.
- اصلاح: `cycle_length_days`/`period_duration_days` (که `hormonal_intelligence_engine` می‌خواند ولی seed نشده) با مقدارِ آینه‌ایِ `cycle_avg_length`/`cycle_avg_period` seed شوند.

---

## ۴) موتورها

- **`cycle_engine` (ارتقا یا موتورِ تحلیلیِ همراه):**
  - **پیش‌بینیِ شخصی‌سازی‌شده‌ی بازه‌ای:** به‌جای طولِ ثابت، از میانه + پراکندگیِ تاریخی → پیش‌بینیِ «حدوداً X ± Y روز» (بازه، نه نقطه). مدتِ عادتِ شخصی.
  - **نمره‌ی نظم/regularity** و **سری‌های روند** (طولِ چرخه و مدتِ عادت در چند چرخه‌ی اخیر).
  - **فازِ PMS/پیش‌از‌عادت:** پنجره‌ی `cycle_pms_window_days` پیش از عادتِ بعدیِ پیش‌بینی‌شده (بدونِ هیچ ارجاعِ تخمک‌گذاری/باروری).
  - **خروجی‌های باروری (پنجره‌ی بارور/تخمک‌گذاری) به UI داده نشوند** (داخلی بمانند یا حذف).
- **همبستگیِ درون‌چرخه (helperِ تست‌پذیر):** جفت‌کردنِ `cycle_day_logs` با `energy_logs`/`mood_logs`/خواب(`bedtime_diagnostics`)/`routine_completions` بر اساسِ تاریخ → همبستگیِ صادق + بینش. فقط‌خواندنی.
- **آمارِ علائم:** فراوانیِ هر علامت + خوشه‌بندی بر اساسِ روزِ چرخه («علائمِ رایجِ تو حولِ روزِ X»).

---

## ۵) پلِ غیرمستقیم (`CycleConsentBridge`) — توسعه

- حفظِ `isUserMenstruating` / `isEnergyTuned`.
- افزودنِ **`bodyRhythmInfluence()`**: یک تأثیر/هینتِ **غیرمستقیمِ** مشتق از فاز + همبستگیِ شخصی، گیت‌خورده با رضایت، **بدونِ هیچ واژه‌ی صریح**. مصرف‌کننده‌ها: انرژی (`cycle_consent_energy`) و خواب (`cycle_consent_sleep`). متن همیشه از جنسِ «ریتمِ بدنی‌ات این روزها...».
- پلِ فقهی: متد(های) وضعیتِ تعلیقِ عبادت + ثبت/خواندنِ `fasting_debt`، گیت‌خورده با `cycle_consent_worship`.

---

## ۶) ساختار صفحه (CycleScreen — درونِ vault)

**هیرو (orb):** فازِ فعلی + روزِ چرخه/عادت + **پیش‌بینیِ بازه‌ایِ عادتِ بعدی** («حدوداً ۳ تا ۵ روزِ دیگه»). **بدونِ تخمک‌گذاری/پنجره‌ی بارور.**

تب‌ها/بخش‌ها:
1. **امروز** — ثبتِ شروع/پایانِ عادت + ثبتِ روزانه‌ی علائم (موجود) + خودمراقبتیِ فازی (ارتقا) + کارتِ فازِ PMS اگر نزدیک است.
2. **تقویم** — ماهانه؛ فقط **عادتِ واقعی + بازه‌ی پیش‌بینیِ عادت + پنجره‌ی PMS**. **مارکرهای باروری/تخمک‌گذاری حذف.** نقطه‌ی روزهای دارای علامت حفظ.
3. **روند و الگوها** — نمودارِ طول/مدت در چند چرخه + نمره‌ی نظم + **آمار و خوشه‌ی علائم** + هشدارِ بی‌نظمیِ موجود (با لحنِ آرام).
4. **همبستگی (vault)** — همبستگیِ **صریحِ** علائم/حال/جریان با انرژی/خواب/روتین (چون اینجا محرمانه و پشتِ PIN است). با لحنِ صادق و بدونِ قطعیتِ علّی.

**تنظیمات/رضایت:** پنل موجود + افزودنِ `cycle_consent_sleep` + توضیحِ شفافِ این‌که هر رضایت دقیقاً چه چیزِ غیرمستقیمی را بیرون می‌برد. باروری همیشه پنهان.

---

## ۷) پلِ فقهی (سمتِ عبادت)

- در ایامِ عادت + `cycle_consent_worship`: وزنِ روتین‌های عبادیِ متأثر (نماز/روزه) **خودکار و غیرمستقیم** تعلیق شود (در سمتِ عبادت، بدونِ واژه‌ی صریح).
- روزهای روزه‌ی واجبِ ازدست‌رفته → یک رکوردِ **`fasting_debt`** (روزه‌ی قضا) ساخته شود؛ دفترِ بدهی قابلِ مشاهده و «تسویه».
- بدونِ رضایت یا برای کاربرِ غیرِ این شرایط، هیچ اثری نباشد.

---

## ۸) لحن، حریم و اصول

- آرام، محترمانه، بدونِ قضاوت؛ بی‌نظمی «هشدارِ نرم» است نه ترس.
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی.
- **محرمانگیِ سخت:** بیرون از این صفحه هیچ واژه‌ی صریحِ پریود/قاعدگی/چرخه؛ تأثیر فقط از Bridge و غیرمستقیم. برای کاربرِ مرد یا رضایتِ خاموش هیچ ردّی نباشد.
- **باروری پنهان:** نه پنجره‌ی بارور، نه تخمک‌گذاری، هیچ‌جا.
- قفلِ PIN/بیومتریک و یادآورهای discreet حفظ شوند.
- AI فقط دستیار؛ هیچ نوشتنِ کور.

---

## ۹) ترتیبِ ساخت (خلاصه)

C1 مهاجرت (جدولِ `fasting_debt` + تنظیماتِ جدید/اصلاح) → C2 مدل‌ها → C3 ارتقای موتور (پیش‌بینیِ بازه‌ای/نظم/روند/PMS، پنهان‌کردنِ باروری) → C4 موتورِ همبستگیِ درون‌چرخه + آمارِ علائم → C5 توسعه‌ی Bridge (`bodyRhythmInfluence` + پلِ فقهی) → C6 پیاده‌سازیِ پلِ فقهی و دفترِ روزه‌ی قضا → C7 پاک‌سازیِ UIِ باروری (orb/تقویم) → C8 بخشِ روند و الگوها → C9 بخشِ همبستگیِ vault → C10 خودمراقبتیِ فازی + کارتِ PMS → C11 تنظیمات/رضایت (+sleep) و یادآورها → C12 بازبینیِ محرمانگی + اعتبارسنجی.

---

## ۱۰) ویژگی‌های تکمیلی نسخه ۲ (ویژگی‌های D1 تا D7)

- **نمودار تغییرات خلق‌وخو (D1):** رسم نمودار روند خلق‌وخو در طول چرخه بدنی با `CustomPainter` و نمایش نقاط بحرانی بدون تکیه بر پکیج‌های گرافیکی متفرقه.
- **فوریت تسکین درد (D2):** دکمه SOS درد قاعدگی با راهنماهای پوزیشن بدنی و خانگی در فازهای قاعدگی/PMS.
- **حالت بارداری (D4):** امکان ثبت LMP و محاسبه تاریخ تخمینی زایمان (۲۸۰ روز بعد). با فعالسازی این حالت، چرخه متوقف، نوتیفیکیشن‌های چرخه لغو و صفحه مخصوص بارداری با وضعیت رشد جنین و خودمراقبتی سه‌ماهه جایگزین کل UI می‌شود.
- **نوتیفیکیشن‌های محرمانه واقعی (D5):** اطلاع‌رسانی ۲ روز و ۱ روز قبل از پیش‌بینی با متون کاملاً محرمانه و بدون واژگان صریح بر روی صفحه قفل.
