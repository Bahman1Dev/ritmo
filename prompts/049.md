پرامپت اجرایی: بازسازی کامل بخش «حال و تعادل» — W-01 تا W-50 و پ‌۱ تا پ‌۱۱

این صفحه یک **پرامپت اجرایی** است، نه گزارش. کل محتوا را یک‌جا به ایجنت بده. ایجنت باید دقیقاً به همین ترتیب پیش برود، هیچ مرحله‌ای را جا نیندازد، و بعد از هر موج گزارش بدهد.

## ۰. نقش تو و قوانین مطلق

تو یک مهندس ارشد Flutter/Dart هستی که روی مخزن `Bahman1Dev/ritmo` کار می‌کنی. مأموریت تو اصلاح ۵۰ باگ مشخص و اجرای ۱۱ بهبود طراحی در بخش «حال و تعادل» است. تو **حق تصمیم‌گیری خلاقانه نداری** — فقط همین سند را اجرا کن.

<aside>
⛔

**پانزده قانون مطلق. نقض هرکدام یعنی شکست کل کار.**

</aside>

1. فقط فایل‌هایی را تغییر بده که اسمشان صریحاً در این سند آمده. هیچ فایل دیگری را حتی فرمت هم نکن.
2. هیچ ریفکتور سلیقه‌ای انجام نده. هیچ متغیری را چون «اسمش بهتر است» تغییر نده.
3. هیچ تستی را حذف، skip یا کامنت نکن. اگر تستی شکست، کد را درست کن نه تست را.
4. هیچ پکیج جدیدی به `pubspec.yaml` اضافه نکن. `flutter pub upgrade` اجرا نکن.
5. بعد از **هر Task**، `flutter analyze` اجرا کن. باید ۰ error و ۰ warning بدهد. اگر نداد، قبل از Task بعدی درستش کن.
6. **هرگز عدد جعلی برنگردان.** هیچ تابعی در نبود داده نباید `100` یا `0` برگرداند. مقدار درست `null` است.
7. هرگز رنگ هاردکد ننویس. هیچ `0xff...` و هیچ `Colors.white.withValues(...)` در لایهٔ `features/`.
8. هرگز فراخوانی شبکه یا AI را روی مسیر بارگذاری صفحه نگذار. AI فقط در پس‌زمینه و بعد از رندر.
9. هرگز رشتهٔ فارسی داخل `lib/core/` ننویس. متن کاربر فقط در لایهٔ presentation.
10. هرگز `DateTime.now()` داخل موتور تحلیلی ننویس. زمان باید از ورودی تزریق شود.
11. هرگز `as int` یا `as String` بدون گارد ننویس. از helper امنی که در T-0.3 می‌سازی استفاده کن.
12. **اگر فایل، تابع یا خطی که این سند می‌گوید را پیدا نکردی: آن Task را Skip کن**، چیزی را حدس نزن، و در «گزارش نهایی» زیر بخش «موارد پیدا نشده» با شمارهٔ Task ثبتش کن.
13. قطعه‌کدهای «پیدا کن» ممکن است دقیقاً کاراکتربه‌کاراکتر با مخزن یکی نباشند (فاصله، شکست خط). منطق را تطبیق بده، نه رشته را.
14. بعد از هر موج یک commit جدا با پیام دقیقی که در انتهای آن موج آمده بزن. commit چندموجی ممنوع.
15. تمام متن‌های قابل‌مشاهدهٔ کاربر فارسی‌اند و همهٔ اعداد نمایشی باید رقم فارسی باشند.

---

## ۱. پیش‌نیازها — قبل از اولین تغییر

```bash
git checkout -b fix/wellbeing-hardening
flutter --version
flutter pub get
flutter analyze > tool/analyze_before.txt
flutter test > tool/test_before.txt
```

سپس این فایل‌ها را **کامل بخوان** (فقط بخوان، تغییر نده):

| فایل | چرا باید بخوانی |
| --- | --- |
| `docs/CONSTITUTION.md` | قوانین ۱، ۲، ۶، ۷، ۸، ۱۶، ۱۷، ۱۸، ۱۹، ۲۱، ۲۲، ۲۳، ۲۸، ۳۲ در ادامه ارجاع داده می‌شوند |
| `docs/RITMO_DESIGN_TOKENS.md` | نام دقیق کلاس توکن‌ها |
| `lib/core/database/migration/migrations_registry.dart` | الگوی ثبت migration و اسکیمای فعلی |
| `lib/core/domain/engines/ritmo_event_bus.dart` | API دقیق subscribe و fire |
| `lib/features/wellbeing/presentation/wellbeing_screen.dart` | فایل اصلی این کار |
| `lib/core/analytics/sleep_engine.dart` | موتور خواب |
| `lib/core/analytics/energy_analytics_engine.dart` | موتور انرژی |
| `lib/core/analytics/reflection_engine.dart` | موتور بازتاب |
| `lib/core/analytics/life_balance_engine.dart` | موتور تعادل |
| `lib/core/services/sync/rhythm_snapshot_service.dart` | اسنپ‌شات روزانه |
| `lib/features/today/presentation/insights_screen.dart` | الگوی درستِ `RitmoEngineBus.instance.execute` |
| `lib/features/cycle/presentation/cycle_screen.dart` | تابع `_toPersianDigits` را از اینجا برمی‌داری |

در گزارش پیش‌نیاز این چهار مورد را صریح بنویس:

1. نام دقیق کلاس توکن رنگ (مثلاً `RitmoColors`) و مسیر فایلش.
2. امضای دقیق متد subscribe در `RitmoEventBus`.
3. نسخهٔ فعلی دیتابیس و شکل دقیق ثبت یک migration.
4. آیا ستون `lifeBalanceScore` در `daily_rhythm` با `NOT NULL` تعریف شده یا نه.

---

## ۲. موج ۰ — زیرساخت مشترک

این موج هیچ باگی را مستقیم حل نمی‌کند اما ابزار حل بقیه را می‌سازد. **بدون این موج جلو نرو.**

### T-0.1 · ساخت `RitmoDate` — تنها مبنای زمانی مجاز

**فایل جدید:** `lib/core/util/ritmo_date.dart`

```dart
/// تنها مرجع مجاز تبدیل زمان به کلید روز در کل اپ.
/// هیچ جای دیگری حق ندارد toIso8601String().substring(0, 10) بنویسد.
class RitmoDate {
  const RitmoDate._();

  /// کلید روز به فرمت YYYY-MM-DD بر مبنای زمان محلی دستگاه.
  static String dayKey(DateTime instant) {
    final local = instant.toLocal();
    final y = local.year.toString().padLeft(4, '0');
    final m = local.month.toString().padLeft(2, '0');
    final d = local.day.toString().padLeft(2, '0');
    return '$y-$m-$d';
  }

  static String dayKeyFromMillis(int millis) =>
      dayKey(DateTime.fromMillisecondsSinceEpoch(millis));

  static DateTime startOfDay(DateTime instant) {
    final l = instant.toLocal();
    return DateTime(l.year, l.month, l.day);
  }

  static int startOfDayMillis(DateTime instant) =>
      startOfDay(instant).millisecondsSinceEpoch;

  /// کلیدهای روز از قدیم به جدید، شامل امروز. طول خروجی دقیقاً days است.
  static List<String> lastNDayKeys(DateTime now, int days) {
    final base = startOfDay(now);
    return List<String>.generate(
      days,
      (i) => dayKey(base.subtract(Duration(days: days - 1 - i))),
    );
  }

  static DateTime? tryParseDayKey(String? value) {
    if (value == null || value.isEmpty) return null;
    return DateTime.tryParse(value);
  }
}
```

**معیار پذیرش:** `lastNDayKeys(DateTime(2026, 1, 3), 3)` باید دقیقاً `['2026-01-01', '2026-01-02', '2026-01-03']` بدهد.

### T-0.2 · ساخت `RitmoNumber` — ارقام فارسی

**فایل جدید:** `lib/core/util/ritmo_number.dart`

```dart
class RitmoNumber {
  const RitmoNumber._();

  static const List<String> _fa = ['۰','۱','۲','۳','۴','۵','۶','۷','۸','۹'];

  /// هر رقم لاتین داخل رشته را به رقم فارسی تبدیل می‌کند.
  static String fa(Object? input) {
    final s = input?.toString() ?? '';
    final buffer = StringBuffer();
    for (final code in s.runes) {
      if (code >= 0x30 && code <= 0x39) {
        buffer.write(_fa[code - 0x30]);
      } else {
        buffer.writeCharCode(code);
      }
    }
    return buffer.toString();
  }

  /// مثال: 62 -> ۶۲
  static String faInt(num value) => fa(value.round().toString());

  /// مثال: 62 -> ۶۲٪
  static String faPercent(num value) => '${faInt(value)}٪';

  /// مثال: 7.5 -> ۷٫۵ ساعت
  static String faHours(num hours) {
    final rounded = (hours * 10).round() / 10;
    final text = rounded % 1 == 0
        ? rounded.toInt().toString()
        : rounded.toStringAsFixed(1).replaceAll('.', '٫');
    return '${fa(text)} ساعت';
  }
}
```

سپس در `cycle_screen.dart` تابع محلی `_toPersianDigits` را **حذف** کن و همهٔ فراخوانی‌هایش را به `RitmoNumber.fa(...)` تغییر بده.

### T-0.3 · ساخت helper خواندن امن از دیتابیس

**فایل جدید:** `lib/core/util/safe_map.dart`

```dart
/// خواندن امن از ردیف‌های sqflite. هیچ‌جا مستقیم as int ننویس.
extension SafeMapRead on Map<String, Object?> {
  int? readInt(String key) {
    final v = this[key];
    if (v is int) return v;
    if (v is num) return v.toInt();
    if (v is String) return int.tryParse(v);
    return null;
  }

  double? readDouble(String key) {
    final v = this[key];
    if (v is double) return v;
    if (v is num) return v.toDouble();
    if (v is String) return double.tryParse(v);
    return null;
  }

  String? readString(String key) {
    final v = this[key];
    if (v == null) return null;
    if (v is String) return v.isEmpty ? null : v;
    return v.toString();
  }

  bool readBool(String key, {bool fallback = false}) {
    final v = this[key];
    if (v is bool) return v;
    if (v is num) return v != 0;
    if (v is String) return v == '1' || v.toLowerCase() == 'true';
    return fallback;
  }
}
```

### T-0.4 · افزودن توکن‌های رنگ معنایی

**فایل:** همان فایل توکن رنگ که در پیش‌نیاز شناسایی کردی.

شش توکن معنایی اضافه کن. هر توکن باید **دو مقدار** داشته باشد (روشن و تاریک). **دقیقاً از الگوی همان فایل تقلید کن** و ساختار جدید اختراع نکن.

| مقدار هاردکد فعلی | نام توکن جدید | معنی |
| --- | --- | --- |
| `0xff8B5CF6` | `sleepAccent` | خواب |
| `0xffEC4899` | `energyAccent` | انرژی |
| `0xff06B6D4` | `reflectionAccent` | بازتاب |
| `0xff22D3EE` | `reflectionAccentAlt` | گرادیان بازتاب |
| `0xffF59E0B` | `cautionAccent` | هشدار ملایم |
| `0xffF43F5E` | `dangerAccent` | وضعیت بحرانی |

برای تم روشن، مقدار تیره‌تر و اشباع‌شده‌تر همان رنگ را بگذار تا روی پس‌زمینهٔ سفید حداقل کنتراست ۴٫۵:۱ داشته باشد. دو توکن سطح شیشه‌ای هم اضافه کن:

```dart
// تیره: سفید با آلفای ۰٫۰۶ — روشن: مشکی با آلفای ۰٫۰۴
static Color glassSurface(BuildContext context) => ...
// تیره: سفید با آلفای ۰٫۱۲ — روشن: مشکی با آلفای ۰٫۱۰
static Color glassBorder(BuildContext context) => ...
```

### T-0.5 · Migration نسخهٔ ۹ — ایندکس‌ها و جدول رول‌آپ

**فایل:** `lib/core/database/migration/migrations_registry.dart`

نسخهٔ دیتابیس را از `8` به `9` ببر و یک migration جدید ثبت کن. **الگوی ثبت را دقیقاً از migration نسخهٔ ۸ کپی کن؛ API جدید اختراع نکن.**

```sql
CREATE TABLE IF NOT EXISTS wellbeing_daily (
  date TEXT PRIMARY KEY,
  indexValue REAL,
  confidence REAL NOT NULL DEFAULT 0,
  sleepScore REAL,
  energyScore REAL,
  moodScore REAL,
  reflectionScore REAL,
  contributionsJson TEXT,
  computedAt INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_energy_logs_loggedAt ON energy_logs(loggedAt);
CREATE INDEX IF NOT EXISTS idx_mood_logs_loggedAt ON mood_logs(loggedAt);
CREATE INDEX IF NOT EXISTS idx_routine_completions_time ON routine_completions(completionTime);
CREATE INDEX IF NOT EXISTS idx_bedtime_diagnostics_date ON bedtime_diagnostics(date);
CREATE INDEX IF NOT EXISTS idx_daily_rhythm_date ON daily_rhythm(date);
```

<aside>
⚠️

`indexValue` عمداً nullable است. اگر وسوسه شدی `NOT NULL DEFAULT 0` بگذاری، این کار دقیقاً همان باگ W-01 را در سطح دیتابیس بازتولید می‌کند. نگذار.

</aside>

روی `daily_reflections(date)` و `daily_checkins(date)` ایندکس نساز — این ستون‌ها از قبل `UNIQUE` دارند.

**معیار پذیرش موج ۰:** `flutter analyze` صفر مسئله؛ اپ روی دیتابیس نسخهٔ ۸ بدون crash ارتقا می‌یابد.

```bash
git commit -am "chore(wellbeing): wave-0 shared infra (RitmoDate, RitmoNumber, SafeMap, tokens, db v9)"
```

---

## ۳. موج ۱ — صداقت داده (P0، بلوکر انتشار)

### T-1.1 · پ‌۱ — ساخت `WellbeingEngine`

**فایل جدید:** `lib/core/analytics/wellbeing_engine.dart` — کلمه‌به‌کلمه بساز:

```dart
import 'dart:math' as math;

enum WellbeingSignal { sleep, energy, mood, reflection }

/// سهم یک سیگنال در شاخص نهایی — ورودی شیت چرا؟
class WellbeingContribution {
  const WellbeingContribution({
    required this.signal,
    required this.score,
    required this.weight,
    required this.confidence,
    required this.sampleCount,
    required this.windowDays,
    required this.isAiDerived,
  });

  final WellbeingSignal signal;
  final double score;      // 0..100
  final double weight;     // وزن ثابت سیگنال
  final double confidence; // 0..1
  final int sampleCount;   // n واقعی
  final int windowDays;    // پنجرهٔ زمانی
  final bool isAiDerived;  // قانون ۳۲

  double get effectiveWeight => weight * confidence;
}

/// سیگنالی که داده‌اش کافی نبود — ورودی حالت خالی صادقانه.
class WellbeingMissingSignal {
  const WellbeingMissingSignal({
    required this.signal,
    required this.have,
    required this.need,
  });
  final WellbeingSignal signal;
  final int have;
  final int need;
}

class WellbeingIndex {
  const WellbeingIndex({
    required this.value,
    required this.confidence,
    required this.contributions,
    required this.missing,
    required this.computedAtMillis,
  });

  /// null یعنی داده کافی نیست. هرگز این را با 0 جایگزین نکن.
  final double? value;
  final double confidence; // 0..1
  final List<WellbeingContribution> contributions;
  final List<WellbeingMissingSignal> missing;
  final int computedAtMillis;

  bool get hasValue => value != null;

  /// پهنای عدم قطعیت برای نوار اطمینان (پ‌۳).
  double get uncertainty => (1.0 - confidence) * 25.0;
  double? get lowerBound =>
      value == null ? null : (value! - uncertainty).clamp(0.0, 100.0);
  double? get upperBound =>
      value == null ? null : (value! + uncertainty).clamp(0.0, 100.0);

  /// آبشار سهم‌ها نسبت به پایهٔ ۵۰.
  /// تضمین ریاضی: 50 + مجموع مقادیر این نقشه == value
  Map<WellbeingSignal, double> get waterfall {
    final totalW =
        contributions.fold<double>(0, (a, c) => a + c.effectiveWeight);
    if (totalW <= 0) return const {};
    return {
      for (final c in contributions)
        c.signal: (c.effectiveWeight * (c.score - 50.0)) / totalW,
    };
  }
}

class WellbeingEngineInput {
  const WellbeingEngineInput({
    required this.now,
    this.horizonDays = 14,
    this.sleepNights = 0,
    this.avgSleepHours,
    this.targetSleepHours = 7.5,
    this.avgSleepQuality,
    this.sleepConsistency,
    this.energySamples = 0,
    this.avgEnergyLevel,
    this.energyIsAiDerived = false,
    this.moodSamples = 0,
    this.avgMoodScore,
    this.reflectionEntries = 0,
    this.avgReflectionMood,
  });

  final DateTime now;        // قانون ۱۰: زمان تزریق می‌شود
  final int horizonDays;

  final int sleepNights;
  final double? avgSleepHours;
  final double targetSleepHours;
  final double? avgSleepQuality;   // 0..100
  final double? sleepConsistency;  // 0..100 — null یعنی نامعلوم

  final int energySamples;
  final double? avgEnergyLevel;    // 1..3
  final bool energyIsAiDerived;

  final int moodSamples;
  final double? avgMoodScore;      // 1..5

  final int reflectionEntries;
  final double? avgReflectionMood; // 1..5
}

class WellbeingEngine {
  const WellbeingEngine();

  // وزن‌ها — مجموع دقیقاً ۱٫۰
  static const double wSleep = 0.35;
  static const double wEnergy = 0.30;
  static const double wMood = 0.20;
  static const double wReflection = 0.15;

  // حداقل نمونه برای اینکه سیگنال اصلاً وارد محاسبه شود
  static const int minSleepNights = 3;
  static const int minEnergySamples = 3;
  static const int minMoodSamples = 3;
  static const int minReflectionEntries = 3;

  // تعداد نمونه‌ای که اطمینان را به ۱٫۰ می‌رساند
  static const int fullSleepNights = 7;
  static const int fullEnergySamples = 14;
  static const int fullMoodSamples = 14;
  static const int fullReflectionEntries = 7;

  /// اگر مجموع وزن مؤثر از این کمتر باشد، شاخص null است.
  static const double minTotalEffectiveWeight = 0.5;

  static const double sigmaUnder = 2.0; // کم‌خوابی
  static const double sigmaOver = 2.5;  // پرخوابی

  /// 0 زیر حداقل، 0.5 روی حداقل، تا 1.0 روی حد کامل.
  static double confidenceFor(int have, int min, int full) {
    if (have < min) return 0.0;
    if (have >= full) return 1.0;
    return 0.5 + ((have - min) / (full - min)) * 0.5;
  }

  /// منحنی گاوسی حول هدف: هم کم‌خوابی هم پرخوابی افت می‌کند.
  static double sleepDurationScore(double hours, double target) {
    final delta = hours - target;
    final sigma = delta < 0 ? sigmaUnder : sigmaOver;
    final z = delta / sigma;
    return (100.0 * math.exp(-(z * z))).clamp(0.0, 100.0);
  }

  /// ترکیب سه بعد خواب با نرمال‌سازی خودکار وزن‌های موجود.
  static double sleepScore({
    required double hours,
    required double target,
    double? quality,
    double? consistency,
  }) {
    final terms = <List<double>>[
      [0.5, sleepDurationScore(hours, target)],
      if (quality != null) [0.3, quality.clamp(0.0, 100.0)],
      if (consistency != null) [0.2, consistency.clamp(0.0, 100.0)],
    ];
    final w = terms.fold<double>(0, (a, t) => a + t[0]);
    final s = terms.fold<double>(0, (a, t) => a + t[0] * t[1]);
    return (s / w).clamp(0.0, 100.0);
  }

  static double _map1to3(double v) => (((v - 1.0) / 2.0) * 100).clamp(0.0, 100.0);
  static double _map1to5(double v) => (((v - 1.0) / 4.0) * 100).clamp(0.0, 100.0);

  WellbeingIndex compute(WellbeingEngineInput i) {
    final contributions = <WellbeingContribution>[];
    final missing = <WellbeingMissingSignal>[];

    void addOrMiss({
      required WellbeingSignal signal,
      required int have,
      required int min,
      required int full,
      required double weight,
      required double? score,
      bool isAi = false,
    }) {
      final c = confidenceFor(have, min, full);
      if (c > 0 && score != null) {
        contributions.add(WellbeingContribution(
          signal: signal,
          score: score,
          weight: weight,
          confidence: c,
          sampleCount: have,
          windowDays: i.horizonDays,
          isAiDerived: isAi,
        ));
      } else {
        missing.add(
            WellbeingMissingSignal(signal: signal, have: have, need: min));
      }
    }

    addOrMiss(
      signal: WellbeingSignal.sleep,
      have: i.sleepNights,
      min: minSleepNights,
      full: fullSleepNights,
      weight: wSleep,
      score: i.avgSleepHours == null
          ? null
          : sleepScore(
              hours: i.avgSleepHours!,
              target: i.targetSleepHours,
              quality: i.avgSleepQuality,
              consistency: i.sleepConsistency,
            ),
    );

    addOrMiss(
      signal: WellbeingSignal.energy,
      have: i.energySamples,
      min: minEnergySamples,
      full: fullEnergySamples,
      weight: wEnergy,
      score: i.avgEnergyLevel == null ? null : _map1to3(i.avgEnergyLevel!),
      isAi: i.energyIsAiDerived,
    );

    addOrMiss(
      signal: WellbeingSignal.mood,
      have: i.moodSamples,
      min: minMoodSamples,
      full: fullMoodSamples,
      weight: wMood,
      score: i.avgMoodScore == null ? null : _map1to5(i.avgMoodScore!),
    );

    addOrMiss(
      signal: WellbeingSignal.reflection,
      have: i.reflectionEntries,
      min: minReflectionEntries,
      full: fullReflectionEntries,
      weight: wReflection,
      score:
          i.avgReflectionMood == null ? null : _map1to5(i.avgReflectionMood!),
    );

    final totalEff =
        contributions.fold<double>(0, (a, c) => a + c.effectiveWeight);
    const totalW = wSleep + wEnergy + wMood + wReflection;
    final confidence = (totalEff / totalW).clamp(0.0, 1.0);

    double? value;
    if (totalEff >= minTotalEffectiveWeight) {
      final sum = contributions.fold<double>(
          0, (a, c) => a + c.effectiveWeight * c.score);
      value = (sum / totalEff).clamp(0.0, 100.0);
    }

    return WellbeingIndex(
      value: value,
      confidence: confidence,
      contributions: contributions,
      missing: missing,
      computedAtMillis: i.now.millisecondsSinceEpoch,
    );
  }
}
```

<aside>
🧠

**چرا این فرمول همه‌چیز را حل می‌کند:** سیگنالی که داده ندارد `confidence = 0` می‌گیرد، پس هم از صورت و هم از مخرج حذف می‌شود. کد فعلی صفر را در صورت می‌ریخت و مخرج را هم زیاد می‌کرد. این یک تغییر، W-01، W-02، W-03، W-20 و W-21 را یک‌جا می‌بندد.

</aside>

### T-1.2 · W-01 — حذف `_computeWellbeingIndex` و اتصال به موتور

**فایل:** `lib/features/wellbeing/presentation/wellbeing_screen.dart`

**گام ۱ — این خط را حذف کن (نه اصلاح، حذف):**

```dart
_reflectionOutput ??= ReflectionEngineOutput(avgMoodScore: 0, ...);
```

همین‌طور هر `??=` مشابهی که خروجی خالی ساختگی برای `_energyOutput`، `_moodOutput` یا `_sleepOutput` می‌سازد. این فیلدها باید nullable بمانند و وقتی داده نیست null باشند.

**گام ۲ — کل بدنهٔ `_computeWellbeingIndex()` را حذف و با این جایگزین کن:**

```dart
WellbeingIndex? _wellbeing;
bool _loadFailed = false;   // برای تفکیک خطا از داده نیست

WellbeingIndex _buildWellbeing(DateTime now) {
  return const WellbeingEngine().compute(
    WellbeingEngineInput(
      now: now,
      horizonDays: 14,
      sleepNights: _sleepOutput?.loggedNights ?? 0,
      avgSleepHours: _sleepOutput?.avgDurationHours,
      targetSleepHours: _targetSleepMinutes / 60.0,
      avgSleepQuality: _sleepOutput?.avgQuality,
      sleepConsistency: _sleepOutput?.consistencyScore, // بدون ?? 100
      energySamples: _energyOutput?.sampleCount ?? 0,
      avgEnergyLevel: _energyOutput?.avgLevel,
      energyIsAiDerived: _energyOutput?.isAiDerived ?? false,
      moodSamples: _moodOutput?.sampleCount ?? 0,
      avgMoodScore: _moodOutput?.avgScore,
      reflectionEntries: _reflectionOutput?.activeCount ?? 0,
      avgReflectionMood: _reflectionOutput?.avgMoodScore,
    ),
  );
}
```

<aside>
🔧

نام دقیق فیلدهای خروجی موتورها ممکن است متفاوت باشد. **اسم واقعی را از تعریف کلاس خروجی همان موتور بردار.** اگر فیلدی مثل `sampleCount` یا `avgLevel` وجود ندارد، آن را به کلاس خروجی موتور **اضافه کن** و در محل ساخت خروجی مقداردهی کن.

</aside>

**گام ۳ — سه حالت نمایش را از هم تفکیک کن.** هر جا که قبلاً عدد شاخص را رندر می‌کردی:

```dart
if (_loadFailed) {
  return _buildErrorCard();          // مشکلی در خواندن داده + دکمه تلاش دوباره
}
final w = _wellbeing;
if (w == null || !w.hasValue) {
  return _buildNotEnoughDataCard(w); // هنوز داده کافی نیست + لیست w.missing
}
return _buildIndexRing(w);           // عدد واقعی
```

**هرگز** در حالت خطا یا نبود داده، عدد ۰ با برچسب قرمز «نیاز به توجه» نشان نده.

**گام ۴ —** در `catch` بلوک `_loadAllData` مقدار `_loadFailed = true` بگذار و در ابتدای هر بارگذاری موفق آن را `false` کن.

### T-1.3 · W-02 — امتیاز تعادل باید nullable شود

**فایل:** `lib/core/analytics/life_balance_engine.dart`

```dart
// قبل
if (activeRoutines.isEmpty) return 100;
if (n <= 1) return 100;
if (totalCompletions == 0) return 100;

// بعد — امضا به double? تغییر می‌کند
if (activeRoutines.isEmpty) return null;   // داده نداریم، نه تعادل کامل
if (n <= 1) return null;                   // با یک بعد، تعادل بی‌معناست
if (totalCompletions < minCompletionsForBalance) return null;
```

یک ثابت عمومی هم اضافه کن تا تست بتواند به آن ارجاع دهد:

```dart
static const int minCompletionsForBalance = 10;
```

سپس **همهٔ** مصرف‌کننده‌ها را اصلاح کن (حداقل `insights_screen.dart`). در UI حالت null باید متن «برای محاسبهٔ تعادل، حداقل ۱۰ روتین تکمیل‌شده لازم است» نشان دهد — نه عدد.

### T-1.4 · W-03 — ثبات خواب باید nullable شود (هر سه لایه)

**لایه ۱ —** `sleep_engine.dart`:

```dart
static const int minLogsForConsistency = 5;

double? _calculateConsistency(List<SleepLog> logs) {
  if (logs.length < minLogsForConsistency) return null;
  ...
  if (bedtimeDevs.isEmpty || wakeDevs.isEmpty) return null;
  ...
}
```

**لایه ۲ —** فیلد `consistencyScore` در `SleepEngineOutput` را `double?` کن.

**لایه ۳ —** در `wellbeing_screen.dart` این را حذف کن:

```dart
consistencyScore: _sleepOutput?.consistencyScore ?? 100,
```

به‌جایش مستقیم `_sleepOutput?.consistencyScore` بده، و در UI اگر null بود متن «هنوز محاسبه نشده» بنویس.

### T-1.5 · W-04 — آستانهٔ همبستگی از ۳ به ۳۰

**فایل‌ها:** `sleep_engine.dart` و `reflection_engine.dart`

```dart
/// قانون ۷ قانون اساسی: تحلیل همبستگی حداقل ۳۰ نقطه داده لازم دارد.
static const int minPointsForCorrelation = 30;

// قبل
if (xs.length >= 3) { ... }
// بعد
if (xs.length >= minPointsForCorrelation) { ... }
```

**و زبان خروجی را از قطعی به فرضی تغییر بده.** این جدول ترجمهٔ اجباری است:

| متن ممنوع فعلی | متن جایگزین |
| --- | --- |
| خواب باکیفیت‌تر دیشب با انرژی بالاتر روز بعد ارتباط مستقیم دارد. | به نظر می‌رسد شب‌هایی که بهتر خوابیده‌ای، روز بعدش انرژی بیشتری ثبت کرده‌ای. (بر پایهٔ ۳۰ شب) |
| باعث می‌شود | همراه بوده با |
| نشان می‌دهد که | به نظر می‌رسد |
| ثابت می‌کند | این الگو تکرار شده |

هر متن همبستگی باید n و پنجرهٔ زمانی را در خودش داشته باشد (قانون ۲).

### T-1.6 · W-05 — حذف دادهٔ ساختگی از ورودی پیرسون

**فایل:** `sleep_engine.dart`، داخل `_calculateCorrelation`

**این دو خط را حذف کن:**

```dart
nextDayEnergies.add(2); // baseline placeholder for Pearson sync length
nextDayMoods.add(3);    // baseline
```

منطق درست: به‌جای پر کردن جای خالی، آن جفت را کلاً از هر دو آرایه حذف کن:

```dart
final pairs = <List<double>>[];
for (final night in nights) {
  final nextKey = RitmoDate.dayKey(
      RitmoDate.startOfDay(night.dateTime).add(const Duration(days: 1)));
  final energy = energyByDay[nextKey];
  final mood = moodByDay[nextKey];
  if (energy == null || mood == null) continue; // جفت ناقص = حذف کامل
  pairs.add([night.qualityScore, energy, mood]);
}
```

و در `_calculatePearson` این گاردها را در خط اول اضافه کن و خروجی را `double?` کن:

```dart
if (x.length != y.length) {
  throw ArgumentError('Pearson requires equal-length series');
}
if (x.length < 2) return null;
```

### T-1.7 · W-06 — حداقل نمونه برای بهترین پنجرهٔ خواب

```dart
static const int minSamplesPerBedtimeWindow = 4;

// قبل
var highestRating = -999.0;

// بعد — مقدار جادویی -999 کاملاً از فایل حذف می‌شود
String? bestWindow;
double? bestRating;
for (final entry in dayRatingsByBedtimeWindow.entries) {
  if (entry.value.length < minSamplesPerBedtimeWindow) continue;
  final avg = entry.value.reduce((a, b) => a + b) / entry.value.length;
  if (bestRating == null || avg > bestRating) {
    bestRating = avg;
    bestWindow = entry.key;
  }
}
```

`bestBedtimeWindow` در خروجی موتور باید `String?` شود.

### T-1.8 · W-13 — چک‌ین خنثی نباید ناپدید شود

**فایل:** `wellbeing_screen.dart`، تابع `_buildTimelineDays`

```dart
// قبل — شیء نگهبان با یک رکورد واقعی اشتباه گرفته می‌شود
final checkin = checkins.firstWhere(
  (c) => c.date == dateStr,
  orElse: () => CheckinEntry(date: dateStr, mood: 'NEUTRAL'),
);
checkin: checkin.mood == 'NEUTRAL' && checkin.note == null ? null : checkin,

// بعد — ایندکس Map، بدون شیء نگهبان، بدون حدس
// این دو نقشه یک بار قبل از حلقه ساخته می‌شوند، نه داخلش
final checkinByDate = <String, CheckinEntry>{
  for (final c in checkins) c.date: c,
};
final reflectionByDate = <String, ReflectionEntry>{
  for (final r in reflections) r.date: r,
};
// ... داخل حلقه:
final CheckinEntry? checkin = checkinByDate[dateStr];
final ReflectionEntry? reflection = reflectionByDate[dateStr];
```

این همزمان W-34 (پیچیدگی n²) را هم حل می‌کند.

**معیار پذیرش موج ۱:** کاربر تازه‌نصب هیچ‌جا عدد ۱۰۰ یا ۰ نمی‌بیند؛ متن «داده کافی نیست» می‌بیند. با خطای دیتابیس، کارت خطا نمایش داده می‌شود نه شاخص ۰. چک‌ین خنثی بدون یادداشت در دفتر دیده می‌شود.

```bash
git commit -am "fix(wellbeing): wave-1 data honesty - WellbeingEngine, nullable scores, correlation gate (W-01..W-06, W-13)"
```

---

## ۴. موج ۲ — عملکرد

### T-2.1 · W-25 — همهٔ موتورها از گذرگاه عبور کنند

**فایل:** `wellbeing_screen.dart`

```dart
// قبل — سه موتور مستقیم new می‌شوند، کش صفر
_energyOutput = await EnergyAnalyticsEngine().calculate(...);
_moodOutput   = await MoodEngine().calculate(...);
_sleepOutput  = await SleepEngine().calculate(...);

// بعد — هر چهار موتور از گذرگاه (قانون ۱۶)
_energyOutput = await RitmoEngineBus.instance.execute(...);
_moodOutput   = await RitmoEngineBus.instance.execute(...);
_sleepOutput  = await RitmoEngineBus.instance.execute(...);
```

**الگوی دقیق فراخوانی را از همان خط `ReflectionEngine` در همین فایل کپی کن.** امضای `execute` را حدس نزن.

### T-2.2 · W-23 و W-24 — کوئری‌های محدود و بدون تکرار

قانون: **هیچ کوئری‌ای بدون `where` بازه‌ای و بدون `limit` مجاز نیست.**

```dart
final now = DateTime.now();
final horizonMs = RitmoDate.startOfDayMillis(now.subtract(const Duration(days: 14)));
final horizonKey = RitmoDate.dayKey(now.subtract(const Duration(days: 14)));

final energyRows = await db.query('energy_logs',
    where: 'loggedAt >= ?', whereArgs: [horizonMs],
    orderBy: 'loggedAt DESC', limit: 500);

final moodRows = await db.query('mood_logs',
    where: 'loggedAt >= ?', whereArgs: [horizonMs],
    orderBy: 'loggedAt DESC', limit: 500);

final rhythmRows = await db.query('daily_rhythm',
    where: 'date >= ?', whereArgs: [horizonKey],
    orderBy: 'date DESC', limit: 60);

final completionRows = await db.query('routine_completions',
    where: 'completionTime >= ?', whereArgs: [horizonMs],
    orderBy: 'completionTime DESC', limit: 2000);

// W-24: فقط یک بار. مرتب‌سازی دوم در Dart انجام می‌شود.
final bedtimeRows = await db.query('bedtime_diagnostics',
    where: 'date >= ?', whereArgs: [horizonKey],
    orderBy: 'date DESC', limit: 90);
final bedtimeByLoggedAt = [...bedtimeRows]
  ..sort((a, b) => (b.readInt('loggedAt') ?? 0).compareTo(a.readInt('loggedAt') ?? 0));
```

**دومین کوئری `bedtime_diagnostics` را کامل حذف کن.** برای هیستوگرام ساعتی انرژی، به‌جای پیمایش در Dart از SQL استفاده کن:

```dart
final hourly = await db.rawQuery('''
  SELECT CAST(strftime('%H', loggedAt / 1000, 'unixepoch', 'localtime') AS INTEGER) AS hour,
         AVG(level) AS avgLevel,
         COUNT(*)   AS n
  FROM energy_logs
  WHERE loggedAt >= ?
  GROUP BY hour
  ORDER BY hour ASC
''', [horizonMs]);
```

اگر نام ستون سطح انرژی `level` نیست، از اسکیمای واقعی بردار.

### T-2.3 · W-26 و W-27 — AI را از مسیر بارگذاری بردار و N+1 چرخه را بکش

**فایل:** `lib/core/analytics/energy_analytics_engine.dart`

**گام ۱ —** متد `calculateAdvancedDynamicEnergy` را به دو متد بشکن:

```dart
/// کاملاً محلی، بدون شبکه، بدون باز کردن مجدد دیتابیس. همیشه سریع.
EnergyEstimate calculateDynamicEnergyLocal(EnergyAnalyticsInput input) { ... }

/// فقط از پس‌زمینه صدا زده می‌شود. هرگز روی مسیر رندر نه.
Future<EnergyEstimate?> refineWithAi(EnergyAnalyticsInput input) async { ... }
```

`calculate()` باید فقط نسخهٔ محلی را صدا بزند. هیچ `await AIGateway...` در مسیر `calculate()` باقی نماند.

**گام ۲ — حذف N+1 موتور چرخه.** این سنگین‌ترین نقطهٔ کل بخش است:

```dart
// قبل — ۵۰ بار اجرای کامل موتور چرخه در هر بار باز شدن صفحه
for (final log in logs) {
  final cycleOut = await CycleEngine().calculate(...);
}

// بعد — یک بار اجرا، سپس lookup از روی نقشه
final cycleOut = await CycleEngine().calculate(...); // دقیقاً یک بار
final Map<String, CyclePhase> phaseByDay = cycleOut.phaseTimeline;
for (final log in logs) {
  final phase = phaseByDay[RitmoDate.dayKeyFromMillis(log.loggedAt)];
  if (phase == null) continue;
}
```

اگر `CycleEngine` خروجی تایم‌لاین فاز ندارد، یک متد `phasesForRange(DateTime from, DateTime to)` به آن اضافه کن که با یک محاسبه کل بازه را برگرداند.

**گام ۳ — هشت کوئری anchor را به یک کوئری تبدیل کن:**

```dart
// قبل: حلقهٔ for i in 1..7 به‌علاوهٔ یک کوئری جداگانهٔ دیروز که با i=1 تکراری است
final anchors = await db.rawQuery('''
  SELECT strftime('%Y-%m-%d', loggedAt / 1000, 'unixepoch', 'localtime') AS day,
         AVG(level) AS avgLevel, COUNT(*) AS n
  FROM energy_logs
  WHERE loggedAt >= ?
  GROUP BY day
  ORDER BY day DESC
''', [sevenDaysAgoMs]);
```

**گام ۴ —** موتور نباید خودش `DatabaseHelper` را دوباره باز کند یا `app_settings` را از نو بخواند. این مقادیر را به `EnergyAnalyticsInput` اضافه کن و از صفحه تزریق کن.

**گام ۵ — کش AI.** دو کلید جدید در `app_settings`:

```
wellbeing_ai_energy_cache      -> JSON خروجی
wellbeing_ai_energy_cached_at  -> millis
```

قانون: اگر `now - cachedAt < 3600000` بود، کش را برگردان و **هیچ فراخوانی شبکه‌ای نکن**. حداکثر یک فراخوانی AI در ساعت.

**گام ۶ — اتصال در صفحه:**

```dart
// ۱. تخمین محلی فوری، رندر بلافاصله
// ۲. سپس، فقط اگر آنلاین است، در پس‌زمینه:
unawaited(_refineEnergyWithAi());

Future<void> _refineEnergyWithAi() async {
  final token = _loadToken;
  final refined = await const EnergyAnalyticsEngine().refineWithAi(input);
  if (!mounted || token != _loadToken || refined == null) return;
  setState(() => _energyOutput = refined.copyWith(isAiDerived: true));
}
```

خروجی AI باید در UI نشان «AI» داشته باشد (قانون ۳۲).

### T-2.4 · W-22 و W-28 — بارگذاری بدون انفجار، با debounce و توکن لغو

```dart
Timer? _reloadDebounce;
int _loadToken = 0;
bool _hasLoadedOnce = false;
bool _isRefreshing = false;

void _requestReload() {
  _reloadDebounce?.cancel();
  _reloadDebounce = Timer(const Duration(milliseconds: 300), () {
    unawaited(_loadAllData());
  });
}

Future<void> _loadAllData() async {
  final token = ++_loadToken;
  if (mounted) setState(() => _isRefreshing = true);
  try {
    // ... همهٔ کوئری‌ها و موتورها ...
    if (!mounted || token != _loadToken) return; // نتیجهٔ کهنه را دور بریز
    setState(() {
      _loadFailed = false;
      _hasLoadedOnce = true;
      _isRefreshing = false;
    });
  } catch (e, st) {
    if (!mounted || token != _loadToken) return;
    setState(() { _loadFailed = true; _isRefreshing = false; });
  }
}

@override
void dispose() {
  _reloadDebounce?.cancel();
  _loadToken++;               // هر نتیجهٔ در راه را باطل کن
  super.dispose();
}
```

**فیلد `_isLoading` را کامل حذف کن.** اسکلت فقط وقتی نمایش داده شود که `!_hasLoadedOnce` باشد. در رفرش، دادهٔ قبلی روی صفحه بماند و فقط `RefreshIndicator` بچرخد. همهٔ پنج `onSaved: _loadAllData` و لیسنرهای رویداد باید به `_requestReload` تغییر کنند.

### T-2.5 · W-29 تا W-33 — رندر

| شناسه | کار دقیق |
| --- | --- |
| W-29 | `SingleChildScrollView` به‌علاوهٔ `Column` هر تب را به `CustomScrollView` با `SliverList` و `SliverToBoxAdapter` تبدیل کن تا بخش‌های خارج از دید ساخته نشوند |
| W-30 | در هدر ثابت **حداکثر یک** `BackdropFilter` بماند. سه‌تای دیگر را با `Container` و رنگ `RitmoColors.glassSurface(context)` جایگزین کن |
| W-31 | `AnimatedBuilder(animation: _tabController)` دور برچسب تب‌ها را **کامل حذف** کن؛ از `labelColor` و `unselectedLabelColor` خود `TabBar` استفاده کن |
| W-32 | `_pageController`، هر دو لیسنر همگام‌سازی و گارد `indexIsChanging` را حذف کن و `PageView` را با `TabBarView(controller: _tabController, children: [...])` جایگزین کن |
| W-33 | سه بار `DateTime.now()` در یک عبارت را با `RitmoDate.startOfDay(_loadedAt)` جایگزین کن؛ `_loadedAt` یک بار در `_loadAllData` ست می‌شود |

### T-2.6 · W-35 — تحلیل بسامد واژه

**فایل:** `reflection_engine.dart`

محاسبهٔ `themeFrequency` را از کل تاریخچه به همان `horizonDays` محدود کن و پشت یک پرچم ورودی بگذار:

```dart
final bool computeThemes; // پیش‌فرض false
```

صفحهٔ حال و تعادل باید `computeThemes: false` بفرستد چون خروجی‌اش را مصرف نمی‌کند (قانون ۸). اگر هیچ مصرف‌کننده‌ای در کل کدبیس ندارد، کد را **حذف** کن و در گزارش نهایی بنویس.

**معیار پذیرش موج ۲:** باز شدن صفحه روی دستگاه میان‌رده زیر ۲ ثانیه (قانون ۲۱)؛ در حالت آفلاین صفحه بلافاصله کامل رندر می‌شود؛ pull-to-refresh موقعیت اسکرول و تب فعال را حفظ می‌کند.

```bash
git commit -am "perf(wellbeing): wave-2 bounded queries, engine bus, AI off load path, sliver rendering (W-22..W-35)"
```

---

## ۵. موج ۳ — درستی محاسبات

### T-3.1 · W-07 — «هفته» به «۱۴ روز»

```dart
// قبل
return 'بدهی خواب هفته: $debtH ساعت. یک خواب جبرانی کوتاه توصیه می‌شود.';
// بعد
return 'کسری خواب ۱۴ روز گذشته: ${RitmoNumber.faHours(debtH)}. یک خواب جبرانی کوتاه می‌تواند کمک کند.';
```

قانون: **پنجرهٔ زمانی هر عدد باید در متن کنارش نوشته شود.** هر متن دیگری که کلمهٔ هفته دارد ولی داده‌اش ۱۴ روزه است را پیدا و اصلاح کن.

### T-3.2 · W-08 و پ‌۷ — «بانک خواب» به‌جای «بدهی خواب»

```dart
/// دفتر دوطرفه: شب‌های پرخواب اعتبار می‌سازند، بدهی قدیمی زوال نمایی دارد.
static const double halfLifeDays = 7.0;

double calculateSleepBalanceHours({
  required List<SleepLog> logs,
  required double targetHours,
  required DateTime now,
}) {
  double balance = 0;
  for (final log in logs) {
    final logDate = RitmoDate.tryParseDayKey(log.date);
    if (logDate == null) continue;
    final ageDays = RitmoDate.startOfDay(now)
        .difference(RitmoDate.startOfDay(logDate)).inDays.toDouble();
    if (ageDays < 0) continue;
    final decay = math.pow(0.5, ageDays / halfLifeDays).toDouble();
    final delta = log.durationHours - targetHours; // منفی = کسری، مثبت = ذخیره
    balance += delta * decay;
  }
  return balance;
}
```

نام فیلد خروجی از `sleepDebtHours` به `sleepBalanceHours` تغییر کند. در UI:

- منفی می‌شود «کسری خواب: ۳٫۲ ساعت» با رنگ `cautionAccent`
- مثبت می‌شود «ذخیرهٔ خواب: ۱٫۵ ساعت» با رنگ سبز موفقیت
- نمایش به‌صورت یک مخزن کوچک که پر و خالی می‌شود، نه عدد قرمز خشک

**ممنوع:** هیچ متنی نباید حس گناه القا کند. کلمات «بدهکاری»، «باید» و «شکست خوردی» ممنوعند (ضدالگوی ۴).

### T-3.3 · W-09 و W-10 — یکسان‌سازی پنجرهٔ زمانی

یک قانون واحد برای کل بخش: **«۱۴ روز اخیر» یعنی ۱۴ کلید روز که آخرینشان امروز است.**

```dart
final keys = RitmoDate.lastNDayKeys(input.now, input.horizonDays); // طول دقیقاً 14
final allowed = keys.toSet();
final inWindow = rows.where((r) => allowed.contains(r.date)).toList();

// قبل — پنجرهٔ ۱۵ روزه تقسیم بر ۱۴ که می‌تواند ۱۰۷٪ شود
final compRate = activeReflections.length / input.horizonDays;
// بعد — صورت و مخرج از یک منبع
final compRate = (inWindow.length / keys.length).clamp(0.0, 1.0);
```

### T-3.4 · W-11 — برچسب بازه روی هر عدد

```dart
'${RitmoNumber.faInt(entryCount)} ثبت — از ابتدا'
'${RitmoNumber.faPercent(completionRate * 100)} — ۱۴ روز اخیر'
```

### T-3.5 · W-12 — یک مبنای زمانی

1. در `energy_analytics_engine.dart` تابع `toIranLocal()` و ثابت `+3:30` را **کامل حذف** کن.
2. هر `toIso8601String().substring(0, 10)` در کل `lib/` را با `RitmoDate.dayKey(...)` جایگزین کن.
3. هر `DateTime.fromMillisecondsSinceEpoch(x).toIso8601String().substring(0,10)` را با `RitmoDate.dayKeyFromMillis(x)` جایگزین کن.
4. بعد از این کار، تنها فایلی که مجاز است این الگو را داشته باشد `lib/core/util/ritmo_date.dart` است. تست موج ۶ این را اجبار می‌کند.

### T-3.6 · W-14 — گوش دادن به `ReflectionChanged`

```dart
// در initState — علاوه بر لیسنر فعلی
_reflectionSub = RitmoEventBus().on('ReflectionChanged').listen((_) => _requestReload());
// اگر API با نام دیگری است، از ritmo_event_bus.dart بردار. حدس نزن.

// در dispose
_reflectionSub?.cancel();
```

همین کار را برای `SleepLogged`، `EnergyLogged` و `CheckinChanged` هم انجام بده اگر در `RitmoEventBus` تعریف شده‌اند.

### T-3.7 · W-15 و W-16 — یک منبع حقیقت

**گام ۱:** `insights_screen.dart` دیگر نباید امتیاز خودش را بسازد. هر دو صفحه باید `WellbeingIndex` را از `RitmoEngineBus` بگیرند و **همان عدد** را نشان دهند.

**گام ۲:** «امتیاز تعادل» را از یک عدد رقیب به یک زیرنمای شاخص حال تنزل بده — با عنوان صریح «توزیع تلاش بین حوزه‌ها» نه «امتیاز تعادل زندگی».

**گام ۳ — W-16:** در `rhythm_snapshot_service.dart` خط `'lifeBalanceScore': 0,` را با مقدار واقعی از `LifeBalanceEngine` جایگزین کن. سپس بر اساس یافتهٔ پیش‌نیاز شمارهٔ ۴:

- **شاخه الف — ستون nullable است:** وقتی نتیجه `null` بود، `'lifeBalanceScore': null` بنویس.
- **شاخه ب — ستون `NOT NULL` است:** در migration v9 جدول `daily_rhythm` را با روش استاندارد SQLite بازسازی کن (ساخت جدول موقت با ستون nullable، `INSERT INTO ... SELECT`، `DROP`، `ALTER TABLE ... RENAME`) و بعد شاخهٔ الف را اجرا کن. حتماً `PRAGMA foreign_keys` را قبل و بعد مدیریت کن.

### T-3.8 · W-17 — دستهٔ سفارشی نباید «فراغت» شود

```dart
// قبل
return 'FREE'; // Fallback
// بعد — دستهٔ ناشناخته یک حوزهٔ مستقل است، نه فراغت
return 'CUSTOM';
```

`'CUSTOM'` را به لیست حوزه‌ها اضافه کن. اگر معماری اجازهٔ حوزهٔ هفتم نمی‌دهد، آن رکورد را از محاسبه **خارج کن** و تعداد خارج‌شده‌ها را در خروجی گزارش بده. **هرگز** آن را به یک حوزهٔ نامرتبط نچسبان.

### T-3.9 · W-18 — موتور تعادل قطعی و بدون فارسی

```dart
// قبل
final now = DateTime.now();                 // غیرقطعی، کش‌ناپذیر
trends['هفته ${w + 1}'] = score;            // رشتهٔ فارسی داخل core

// بعد
Map<int, double?> calculateBalanceTrend({required DateTime now, ...}) {
  trends[w + 1] = score;   // کلید عددی، بدون هیچ رشتهٔ نمایشی
}
```

ترجمهٔ «هفته ۱» فقط در لایهٔ presentation: `'هفته ${RitmoNumber.faInt(weekIndex)}'`.

### T-3.10 · W-19 — حذف کست‌های محافظت‌نشده

در هر چهار موتور هر یک از این الگوها را با نسخهٔ امن جایگزین کن:

| الگوی ممنوع | جایگزین |
| --- | --- |
| `comp['completionTime'] as int` | `comp.readInt('completionTime')` سپس `if (v == null) continue;` |
| `comp['routineId'] as String` | `comp.readString('routineId')` سپس گارد null |
| `r['category'] as String` | `r.readString('category') ?? 'CUSTOM'` |
| `(r['rhythmScore'] as num)` | `r.readDouble('rhythmScore')` سپس گارد null |
| `DateTime.parse(log.date)` | `RitmoDate.tryParseDayKey(log.date)` سپس گارد null |

قانون: **رکورد خراب باید skip شود، نه اینکه کل موتور را بیندازد.** تعداد رکوردهای skip شده را در فیلد `skippedRows` در خروجی موتور برگردان.

همچنین در `_loadAllData`، `try/catch` واحد را به **چهار `try/catch` جدا** (یکی به‌ازای هر موتور) بشکن تا خطای یک موتور کل صفحه را خالی نکند.

### T-3.11 · W-20 و W-21 — فقط راستی‌آزمایی

هر دو با T-1.1 و T-1.2 حل شده‌اند. فقط بررسی کن که:

1. هیچ‌جای کد دیگری `((dur / targetH) * 100).clamp(0.0, 100.0)` باقی نمانده باشد.
2. `avgQuality` و `consistencyScore` واقعاً به `WellbeingEngineInput` پاس می‌شوند.

```bash
git commit -am "fix(wellbeing): wave-3 correctness - windows, timebase, safe casts, sleep bank (W-07..W-21)"
```

---

## ۶. موج ۴ — رابط کاربری، تم و دسترسی‌پذیری

### T-4.1 · W-36 و W-37 — پاکسازی کامل رنگ هاردکد و تم روشن

**گام ۱ —** در پوشهٔ `lib/features/wellbeing/` این دو جستجو را اجرا کن و خروجی هر دو باید در پایان **خالی** شود:

```bash
grep -rn "0x[fF][fF][0-9a-fA-F]\{6\}" lib/features/wellbeing/
grep -rn "Colors\." lib/features/wellbeing/
```

**گام ۲ —** هر مورد را طبق جدول T-0.4 با توکن جایگزین کن. مواردی که در جدول نیستند:

| الگوی فعلی | جایگزین |
| --- | --- |
| `Colors.white` در متن | `Theme.of(context).colorScheme.onSurface` |
| `Colors.white70` یا `Colors.white60` | `Theme.of(context).colorScheme.onSurfaceVariant` |
| `Colors.white.withValues(alpha: 0.06)` | `RitmoColors.glassSurface(context)` |
| `Colors.white.withValues(alpha: 0.12)` | `RitmoColors.glassBorder(context)` |
| `Colors.black` در سایه | `Theme.of(context).shadowColor` |
| `Colors.green` موفقیت | توکن موفقیت موجود در فایل توکن |

**گام ۳ — تست دستی اجباری:** اپ را در **تم روشن** باز کن و هر سه تب بخش حال و تعادل را ببین. هیچ متنی نباید محو یا کم‌کنتراست باشد. اگر بود، مقدار روشن آن توکن را تیره‌تر کن تا کنتراست حداقل ۴٫۵:۱ شود. در گزارش نهایی بنویس که هر سه تب را در تم روشن چک کردی.

### T-4.2 · W-38 — اندازهٔ قلم زیر حد خوانایی

قانون: **هیچ متنی در کل بخش زیر `12` نباشد؛ متن فارسی بدنه حداقل `13`.**

```bash
grep -rn "fontSize: 9\|fontSize: 10\|fontSize: 11" lib/features/wellbeing/
```

هر مورد را به سبک تم تبدیل کن، نه فقط عدد را بزرگ کن:

```dart
// قبل
style: const TextStyle(fontSize: 9, color: Colors.white70)
// بعد
style: Theme.of(context).textTheme.labelSmall // حداقل 12
    ?.copyWith(color: Theme.of(context).colorScheme.onSurfaceVariant)
```

اگر بعد از بزرگ کردن، متن در عرض جا نشد، **متن را کوتاه کن، قلم را کوچک نکن.**

### T-4.3 · W-39 — مقاومت در برابر بزرگ‌نمایی متن

سه ستون داخل `Expanded` در هیروی نبض تعادل در `textScale = 1.3` سرریز می‌کنند.

```dart
// قبل — ارتفاع ثابت، سرریز قطعی
Row(children: [Expanded(child: _stat(...)), Expanded(...), Expanded(...)])

// بعد — در مقیاس بزرگ خودبه‌خود می‌شکند
LayoutBuilder(builder: (context, c) {
  final scale = MediaQuery.textScalerOf(context).scale(14) / 14;
  if (scale > 1.2 || c.maxWidth < 340) {
    return Wrap(spacing: 12, runSpacing: 12, children: [...]);
  }
  return Row(children: [...]);
})
```

**ممنوع:** استفاده از `MediaQuery.of(context).textScaleFactor` (منسوخ). فقط `MediaQuery.textScalerOf(context)`.

**تست دستی:** در تنظیمات سیستم، اندازهٔ قلم را روی بزرگ‌ترین حالت بگذار و هر سه تب را باز کن. هیچ `RenderFlex overflowed` در کنسول نباید ببینی.

### T-4.4 · W-40 و W-41 — دسترسی‌پذیری (قانون ۲۳)

**W-40 —** دکمهٔ آیکونی دستیار AI در هدر، نه tooltip دارد نه برچسب صوتی:

```dart
Semantics(
  button: true,
  label: 'دستیار هوشمند حال و تعادل',
  child: IconButton(
    tooltip: 'دستیار هوشمند',
    icon: const Icon(Icons.auto_awesome),
    onPressed: _openAssistant,
  ),
)
```

همین الگو را برای **هر** `IconButton` و هر `GestureDetector` قابل لمس در این بخش تکرار کن. همچنین هر ناحیهٔ لمسی باید حداقل `48x48` لاجیکال پیکسل باشد.

**W-41 —** وضعیت فقط با رنگ منتقل می‌شود که برای کاربر کوررنگ بی‌معنیست. هر نشانگر وضعیت باید **همزمان آیکون و متن** داشته باشد:

| بازه | آیکون | متن | توکن |
| --- | --- | --- | --- |
| ۷۰ تا ۱۰۰ | `Icons.trending_up` | روند خوب | موفقیت |
| ۴۰ تا ۶۹ | `Icons.trending_flat` | در مسیر | `cautionAccent` |
| زیر ۴۰ | `Icons.trending_down` | نیاز به توجه | `dangerAccent` |
| null | `Icons.help_outline` | داده کافی نیست | خنثی |

### T-4.5 · W-42 — ارقام فارسی در همه‌جا

```dart
// قبل
Text('${score.toInt()}')
Text('${percent.toStringAsFixed(0)}%')
Text('${hours.toStringAsFixed(1)}h')

// بعد
Text(RitmoNumber.faInt(score))
Text(RitmoNumber.faPercent(percent))
Text(RitmoNumber.faHours(hours))
```

قواعد: علامت درصد فارسی `٪` است نه `%`؛ جداکنندهٔ اعشار `٫` است نه نقطه؛ واحد زمان `ساعت` است نه `h`. این جستجو باید خالی شود:

```bash
grep -rn "toStringAsFixed\|toInt()}" lib/features/wellbeing/presentation/
```

### T-4.6 · W-43 و W-44 — RTL درست

**W-43 —** هر چینش جهت‌دار باید نسخهٔ Directional شود:

| ممنوع | جایگزین |
| --- | --- |
| `Alignment.centerLeft` | `AlignmentDirectional.centerStart` |
| `Alignment.centerRight` | `AlignmentDirectional.centerEnd` |
| `Alignment.topLeft` | `AlignmentDirectional.topStart` |
| `EdgeInsets.only(left: x)` | `EdgeInsetsDirectional.only(start: x)` |
| `EdgeInsets.only(right: x)` | `EdgeInsetsDirectional.only(end: x)` |
| `BorderRadius.only(topLeft: ...)` | `BorderRadiusDirectional.only(topStart: ...)` |
| `Positioned(left: x)` | `PositionedDirectional(start: x)` |

**استثنا:** گرادیانت تزئینی که جهت معنایی ندارد می‌تواند `Alignment` بماند.

**W-44 —** هر `Directionality(textDirection: TextDirection.rtl, ...)` هاردکد داخل این بخش را **حذف** کن. جهت باید از `MaterialApp.locale` بیاید تا وقتی l10n انگلیسی کامل شد، بخش خودبه‌خود LTR شود.

### T-4.7 · W-45 — یک کامپوننت واحد برای شیت

در این فایل پنج `showModalBottomSheet` خام با پارامترهای متفاوت وجود دارد. یک کامپوننت مشترک بساز:

**فایل جدید:** `lib/core/widgets/ritmo_sheet_scaffold.dart`

```dart
class RitmoSheetScaffold extends StatelessWidget {
  const RitmoSheetScaffold({
    super.key,
    required this.title,
    required this.child,
    this.subtitle,
  });

  final String title;
  final String? subtitle;
  final Widget child;

  /// تنها راه مجاز باز کردن شیت در بخش حال و تعادل.
  static Future<T?> present<T>({
    required BuildContext context,
    required String title,
    String? subtitle,
    required WidgetBuilder builder,
  }) {
    return showModalBottomSheet<T>(
      context: context,
      isScrollControlled: true,
      useSafeArea: true,
      backgroundColor: Colors.transparent,
      builder: (ctx) => RitmoSheetScaffold(
        title: title,
        subtitle: subtitle,
        child: builder(ctx),
      ),
    );
  }
  // بدنه: دستگیره، عنوان، زیرعنوان، پدینگ یکسان، گوشهٔ ۲۴،
  // احترام به viewInsets برای کیبورد، حداکثر ارتفاع ۰٫۹ صفحه
}
```

هر پنج فراخوانی را به `RitmoSheetScaffold.present(...)` تغییر بده. تست موج ۶ مانع بازگشت `showModalBottomSheet` خام می‌شود.

### T-4.8 · W-46 و W-47 — ماژول غیرفعال نباید بن‌بست بسازد

**W-46 —** دکمهٔ اقدام سریع ماژول غیرفعال نباید دیده شود:

```dart
children: [
  if (_energyEnabled) _quickAction(label: 'ثبت انرژی', onTap: _logEnergy),
  if (_sleepEnabled)  _quickAction(label: 'ثبت خواب',  onTap: _logSleep),
  _quickAction(label: 'ثبت حال', onTap: _logMood), // همیشه فعال
]
```

**W-47 —** اگر کاربر روی کاشی هیروی یک ماژول غیرفعال زد، مستقیم شیت فعال‌سازی باز شود:

```dart
// قبل — پرت به تبی که فقط یک کارت خالی دارد
onTap: () => _switchToSection(1),
// بعد — یک قدم کمتر
onTap: _sleepEnabled ? () => _switchToSection(1) : () => _activateModule('sleep'),
```

### T-4.9 · W-48 — روندها در جای درست

بخش `_buildUnifiedTrendsSection` هر سه حوزه را نشان می‌دهد ولی فقط در تب خواب رندر می‌شود. آن را از تب خواب **خارج** کن و به انتهای تب اول (نمای کلی) منتقل کن.

### T-4.10 · W-49 — یک حلقهٔ پیشرفت واحد

سه پیاده‌سازی مختلف حلقهٔ پیشرفت وجود دارد. یک ویجت مشترک `RitmoProgressRing` بساز که:

- ورودی `double?` می‌گیرد و در حالت null حلقهٔ خاکستری خط‌چین با علامت سؤال نشان می‌دهد
- با `TweenAnimationBuilder` در `600ms` و منحنی `Curves.easeOutCubic` از مقدار قبلی به جدید می‌رود
- اگر `MediaQuery.disableAnimationsOf(context)` درست بود، بدون انیمیشن می‌پرد (قانون ۲۳)
- بازهٔ عدم قطعیت پ‌۳ را به‌صورت یک کمان کم‌رنگ پشت کمان اصلی رسم می‌کند

هر سه پیاده‌سازی قدیمی را حذف کن.

### T-4.11 · W-50 — کد مرده

در `_buildBalancePulseHero` شاخهٔ `else` که پشت گارد `if (!_energyEnabled && !_sleepEnabled)` قرار گرفته هرگز اجرا نمی‌شود. آن را **حذف** کن. قبل از حذف، یک بار مطمئن شو که واقعاً غیرقابل‌دسترس است؛ اگر نبود، به‌جای حذف گارد را اصلاح کن و در گزارش بنویس.

**معیار پذیرش موج ۴:** دو جستجوی گام ۱ خالی؛ تم روشن سالم؛ در `textScale` حداکثر هیچ سرریزی نیست؛ TalkBack هر دکمه را نام‌برداری می‌کند؛ هیچ رقم لاتینی در UI دیده نمی‌شود.

```bash
git commit -am "fix(wellbeing): wave-4 UI, theming, a11y, RTL (W-36..W-50)"
```

---

## ۷. موج ۵ — بازطراحی تجربه (پ‌۲ تا پ‌۹)

<aside>
🎯

پ‌۱ در T-1.1 و پ‌۷ در T-3.2 انجام شدند. این موج بقیه را پیاده می‌کند. برخلاف موج‌های قبلی، اینجا **مجازی در جزئیات بصری از توکن‌های موجود استفاده کنی**، اما ساختار داده و متن‌ها دقیقاً همین‌هاییست که نوشته شده.

</aside>

### T-5.1 · پ‌۲ — شیت «چرا این عدد؟»

مشکل: کاربر عدد ۶۲ را می‌بیند و هیچ راهی برای فهمیدنش ندارد (نقض قانون ۱۹).

**رفتار:** تپ روی حلقهٔ شاخص باید یک شیت باز کند که **دقیقاً چهار بخش** دارد:

**بخش ۱ — نمودار آبشاری.** مستقیم از `WellbeingIndex.waterfall` ساخته می‌شود:

```
پایه                       ۵۰
خواب      ▓▓▓▓▓▓▓▓▓▓    ٪+۹
انرژی     ▓▓▓▓▓         ٪+۵
حال       ▓▓            ٪۲-
بازتاب    ▓▓▓           ٪+۰
شاخص نهایی              ۶۲
```

تضمین ریاضی: `50 + مجموع سهم‌ها == value`. تست موج ۶ این را چک می‌کند.

**بخش ۲ — ردیف هر سیگنال** با قالب ثابت:

```
خواب  —  ۷۸ از ۱۰۰  —  وزن ۳۵٪  —  ۶ شب از ۱۴ روز  —  اطمینان زیاد
```

ترجمهٔ اطمینان: زیر ۰٫۵ «کم»، ۰٫۵ تا ۰٫۸ «متوسط»، بالای ۰٫۸ «زیاد». اگر `isAiDerived` بود، بج «AI» کنار ردیف (قانون ۳۲).

**بخش ۳ — سیگنال‌های غایب** از `WellbeingIndex.missing`، هر کدام با دکمهٔ مستقیم ثبت:

```
حال — ۱ ثبت از ۳ ثبت لازم   [ثبت حال امروز]
```

**بخش ۴ — یک جملهٔ تولیدی محلی** (بدون AI، بدون شبکه) که بزرگ‌ترین سهم مثبت و منفی را نام می‌برد:

```dart
'بیشترین اثر مثبت این دو هفته از خواب بوده، '
'و بیشترین افت از حال روزانه.'
```

شیت با `RitmoSheetScaffold.present` باز می‌شود. عنوان:  `'این عدد از کجا آمده؟'`.

### T-5.2 · پ‌۳ — نوار اطمینان روی حلقه

عدد تکی بدون عدم قطعیت، دقت کاذب می‌سازد. در `RitmoProgressRing`:

- کمان اصلی روی `value`
- یک کمان کم‌رنگ‌تر از `lowerBound` تا `upperBound` درست پشت آن
- زیر عدد، یک خط کوچک: `'تقریباً ۶۲ — دامنهٔ ۵۴ تا ۷۰'`
- وقتی `confidence >= 0.8`، نوار را پنهان کن تا بصری شلوغ نشود

### T-5.3 · پ‌۴ — «نبض دو هفته» جایگزین سه نمودار جدا

مشکل: سه نمودار جدا کاربر را وادار می‌کند خودش هم‌زمانی را در ذهن پیدا کند.

**یک نمودار واحد با محور زمانی مشترک (۱۴ روز):**

| لایه | نمایش | توکن |
| --- | --- | --- |
| خواب | نوار عمودی پشتی، ارتفاع متناسب با مدت | `sleepAccent` |
| انرژی | خط روی نوارها | `energyAccent` |
| حال | نقطه روی خط انرژی | رنگ متناسب با مقدار |
| بازتاب | تیک کوچک زیر محور | `reflectionAccent` |
| روز بدون داده | فاصلهٔ خالی | — |

<aside>
⛔

**روز بدون داده را هرگز با صفر رسم نکن و هرگز بین دو نقطه میانیابی نکن.** جای خالی باید خالی بماند.

</aside>

تپ روی هر روز، یک پاپ‌آپ کوچک با مقادیر دقیق همان روز باز کند.

### T-5.4 · پ‌۵ — تب دوم از «بازتاب» به «آینه»

مشکل: تب بازتاب یک دفتر ثبت است و درصد تکمیل را مثل تکلیف نشان می‌دهد — دقیقاً ضدالگوی ۴.

ترتیب جدید تب دوم از بالا به پایین:

1. **«دو هفتهٔ تو در یک جمله»** — یک جملهٔ تولیدی از دادهٔ واقعی، بدون قضاوت
2. **«چیزهایی که خوب بودند»** — حداکثر سه نقل‌قول مستقیم از `goodThing` خود کاربر
3. **«الگویی که دیده می‌شود»** — فقط و فقط اگر `n >= 30`
4. **دفتر ثبت‌ها** — همان تایم‌لاین فعلی، اما ته صفحه

**درصد تکمیل را از بالای این تب حذف کن.** به‌جایش بنویس: `'۹ بازتاب در ۱۴ روز اخیر'`. عدد مطلق، نه درصدی که مثل نمره به نظر می‌رسد.

### T-5.5 · پ‌۶ — «پنجرهٔ طلایی امروز»

مشکل: هیستوگرام ساعتی انرژی تحلیلیست ولی هیچ اقدامی از دلش درنمی‌آید.

یک کارت جدید بالای تب انرژی:

```
پنجرهٔ طلایی امروز: ۹ تا ۱۱ صبح
در این بازه بیشترین انرژی را ثبت کرده‌ای (بر پایهٔ ۲۲ ثبت در ۱۴ روز)

مهم‌ترین کار امروزت الان ساعت ۱۴ است.
[منتقل کن به پنجرهٔ طلایی]
```

**قواعد اجباری:**

1. اگر تعداد ثبت‌های انرژی کمتر از `20` است، کل کارت را نشان نده.
2. دکمه فقط وقتی دیده شود که **درواقع یک کار قابل جابجایی وجود دارد**.
3. جابجایی باید از همان موتور بازچینش موجود عبور کند — هیچ منطق برنامه‌ریزی جدیدی اینجا ننویس.
4. اگر موتور بازچینش پیدا نکردی، دکمه را نساز و این Task را Skip کن (قانون ۱۲).

### T-5.6 · پ‌۸ — ثبت بدون اصطکاک

مشکل: ثبت هر سیگنال دو تا سه تپ می‌خواهد و شیت باز می‌کند؛ پس کاربر ثبت نمی‌کند؛ پس شاخص null می‌ماند.

**در کارت هیروی بالای تب اول**، یک ردیف سه‌حالته بگذار که **درجا ثبت می‌کند**:

```
الان چطوری؟     [ 😔 ]  [ 😐 ]  [ 🙂 ]
```

- تپ روی هر دکمه = ثبت فوری در `mood_logs`، بدون شیت، بدون تأیید
- پس از ثبت: `RitmoHaptics.tap()` به‌علاوهٔ انیمیشن تیک به‌علاوهٔ یک `SnackBar` با دکمهٔ **«برگردان»** به مدت ۵ ثانیه
- اگر امروز قبلاً ثبت شده، حالت فعلی هایلایت باشد و تپ دوباره مقدار را **ویرایش** کند نه رکورد دوم بسازد
- هر سه دکمه باید `Semantics(label: ...)` داشته باشند؛ ایموجی تنها برای صفحه‌خوان کافی نیست

همین الگو را برای انرژی (کم/متوسط/زیاد) هم در تب انرژی تکرار کن.

### T-5.7 · پ‌۹ — معماری اطلاعات روایت‌محور

مشکل: تب‌ها بر اساس **منبع داده** تقسیم شده‌اند، نه بر اساس **سؤال کاربر**.

| تب | عنوان جدید | سؤالی که جواب می‌دهد | محتوا |
| --- | --- | --- | --- |
| ۱ | امروزِ من | الان در چه وضعیتی‌ام؟ | حلقهٔ شاخص، ثبت سه‌حالته، پنجرهٔ طلایی، یک پیشنهاد |
| ۲ | روند من | دو هفتهٔ اخیر چطور بود؟ | نمودار نبض دو هفته، توزیع تلاش، بانک خواب |
| ۳ | آینه | خودم چه نوشته‌ام؟ | جملهٔ خلاصه، نقل‌قول‌ها، دفتر ثبت |

**این یک تغییر معماریست و باید آخرین کار باشد.** اگر در زمان رسیدن به این Task تعداد تغییرات باز بیش از حد شد، آن را در یک کامیت جداگانه بزن و در گزارش علامت‌گذاری کن.

### T-5.8 · پ‌۱۰ — راستی‌آزمایی عملکرد

پس از اتمام موج ۵، این اعداد را **اندازه بگیر** (با `Stopwatch` در `_loadAllData`) و در گزارش بنویس:

| معیار | قبل | بعد | سقف مجاز |
| --- | --- | --- | --- |
| زمان باز شدن صفحه (آفلاین) | ? | ? | ۲۰۰۰ms |
| تعداد کوئری در یک بار لود | ? | ? | ۱۵ |
| تعداد اجرای CycleEngine | ~۵۰ | ? | ۱ |
| تعداد فراخوانی AI در لود | ۱ | ? | ۰ |
| تعداد BackdropFilter | ۴ | ? | ۱ |

```bash
git commit -am "feat(wellbeing): wave-5 explainability, pulse chart, mirror tab, frictionless logging (P2..P10)"
```

---

## ۸. موج ۶ — تست‌های نگهبان (پ‌۱۱)

<aside>
🔒

بدون این موج، همهٔ کار قبلی در سه ماه آینده دوباره می‌شکند. **این موج اختیاری نیست.**

</aside>

### T-6.1 · تست واحد موتور شاخص

**فایل جدید:** `test/core/analytics/wellbeing_engine_test.dart` — عیناً بساز:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:ritmo/core/analytics/wellbeing_engine.dart';

final _now = DateTime(2026, 1, 15, 12);

WellbeingEngineInput _input({
  int sleepNights = 0,
  double? avgSleepHours,
  double? avgSleepQuality,
  double? sleepConsistency,
  int energySamples = 0,
  double? avgEnergyLevel,
  int moodSamples = 0,
  double? avgMoodScore,
  int reflectionEntries = 0,
  double? avgReflectionMood,
}) {
  return WellbeingEngineInput(
    now: _now,
    sleepNights: sleepNights,
    avgSleepHours: avgSleepHours,
    avgSleepQuality: avgSleepQuality,
    sleepConsistency: sleepConsistency,
    energySamples: energySamples,
    avgEnergyLevel: avgEnergyLevel,
    moodSamples: moodSamples,
    avgMoodScore: avgMoodScore,
    reflectionEntries: reflectionEntries,
    avgReflectionMood: avgReflectionMood,
  );
}

void main() {
  const engine = WellbeingEngine();

  test('no data returns null, never zero', () {
    final r = engine.compute(_input());
    expect(r.value, isNull);
    expect(r.hasValue, isFalse);
    expect(r.confidence, 0.0);
    expect(r.missing.length, 4);
  });

  test('below minimum samples still returns null', () {
    final r = engine.compute(_input(sleepNights: 2, avgSleepHours: 7.5));
    expect(r.value, isNull);
  });

  test('missing signals do not drag the index down', () {
    // فقط خواب و انرژی داده دارند و هر دو کاملند.
    final r = engine.compute(_input(
      sleepNights: 7, avgSleepHours: 7.5,
      energySamples: 14, avgEnergyLevel: 3.0,
    ));
    expect(r.value, isNotNull);
    // میانگین دو سیگنال کامل باید بالا بماند، نه نصف شود.
    expect(r.value, greaterThan(90));
    expect(r.confidence, closeTo(0.65, 0.01)); // 0.35 + 0.30
  });

  test('oversleeping is penalized, not rewarded', () {
    final perfect = WellbeingEngine.sleepDurationScore(7.5, 7.5);
    final over = WellbeingEngine.sleepDurationScore(12.0, 7.5);
    final under = WellbeingEngine.sleepDurationScore(4.0, 7.5);
    expect(perfect, closeTo(100, 0.001));
    expect(over, lessThan(perfect));
    expect(under, lessThan(perfect));
  });

  test('sleep score renormalizes when quality is unknown', () {
    final withQuality = WellbeingEngine.sleepScore(
        hours: 7.5, target: 7.5, quality: 100, consistency: 100);
    final withoutQuality =
        WellbeingEngine.sleepScore(hours: 7.5, target: 7.5);
    expect(withQuality, closeTo(100, 0.001));
    expect(withoutQuality, closeTo(100, 0.001)); // نه ۵۰
  });

  test('confidence ramps from min to full', () {
    expect(WellbeingEngine.confidenceFor(2, 3, 7), 0.0);
    expect(WellbeingEngine.confidenceFor(3, 3, 7), 0.5);
    expect(WellbeingEngine.confidenceFor(7, 3, 7), 1.0);
    expect(WellbeingEngine.confidenceFor(99, 3, 7), 1.0);
  });

  test('waterfall always reconciles with the final value', () {
    final r = engine.compute(_input(
      sleepNights: 7, avgSleepHours: 6.0,
      energySamples: 14, avgEnergyLevel: 2.0,
      moodSamples: 14, avgMoodScore: 3.0,
      reflectionEntries: 7, avgReflectionMood: 4.0,
    ));
    final sum = r.waterfall.values.fold<double>(0, (a, b) => a + b);
    expect(50 + sum, closeTo(r.value!, 0.0001));
  });

  test('uncertainty shrinks as confidence grows', () {
    final low = engine.compute(_input(sleepNights: 3, avgSleepHours: 7.5,
        energySamples: 3, avgEnergyLevel: 2.0));
    final high = engine.compute(_input(sleepNights: 7, avgSleepHours: 7.5,
        energySamples: 14, avgEnergyLevel: 2.0,
        moodSamples: 14, avgMoodScore: 3.0,
        reflectionEntries: 7, avgReflectionMood: 3.0));
    expect(high.uncertainty, lessThan(low.uncertainty));
  });

  test('index is always inside 0..100', () {
    final r = engine.compute(_input(
      sleepNights: 7, avgSleepHours: 0.5,
      energySamples: 14, avgEnergyLevel: 1.0,
      moodSamples: 14, avgMoodScore: 1.0,
      reflectionEntries: 7, avgReflectionMood: 1.0,
    ));
    expect(r.value, inInclusiveRange(0, 100));
  });

  test('engine is deterministic and does not read the clock', () {
    final a = engine.compute(_input(sleepNights: 7, avgSleepHours: 7.0,
        energySamples: 14, avgEnergyLevel: 2.5));
    final b = engine.compute(_input(sleepNights: 7, avgSleepHours: 7.0,
        energySamples: 14, avgEnergyLevel: 2.5));
    expect(a.value, b.value);
    expect(a.computedAtMillis, b.computedAtMillis);
  });
}
```

### T-6.2 · تست برای بقیهٔ موتورها

برای **هر ردیف** یک تست بنویس. نام تست دقیقاً همین باشد تا بعداً قابل ردیابی باشد:

| فایل تست | نام تست | انتظار |
| --- | --- | --- |
| `life_balance_engine_test.dart` | `life balance with no routines is unknown` | خروجی `null` است، نه `100` |
| `life_balance_engine_test.dart` | `life balance needs ten completions` | با ۹ تکمیل `null`، با ۱۰ تکمیل عدد |
| `life_balance_engine_test.dart` | `balance trend keys are numeric` | کلیدها `int` اند و هیچ رشتهٔ فارسی در خروجی نیست |
| `sleep_engine_test.dart` | `consistency requires five logs` | با ۴ لاگ `null`، با ۵ لاگ عدد |
| `sleep_engine_test.dart` | `sleep balance is two sided` | پرخوابی مثبت، کم‌خوابی منفی |
| `sleep_engine_test.dart` | `old sleep debt decays` | کسری ۱۴ روز پیش اثرش کمتر از کسری دیشب است |
| `sleep_engine_test.dart` | `correlation requires thirty points` | با ۲۹ جفت `null`، با ۳۰ جفت عدد |
| `sleep_engine_test.dart` | `pearson rejects unequal series` | `ArgumentError` پرتاب می‌شود |
| `sleep_engine_test.dart` | `best bedtime window needs four samples` | با ۳ نمونه `null`؛ هیچ `-999` در خروجی نیست |
| `reflection_engine_test.dart` | `completion rate never exceeds one` | با ۱۵ بازتاب در پنجرهٔ ۱۴ روزه، خروجی دقیقاً `1.0` |
| `reflection_engine_test.dart` | `neutral checkin is preserved` | چک‌ین `NEUTRAL` بدون یادداشت در خروجی هست |
| `ritmo_date_test.dart` | `lastNDayKeys length matches request` | طول دقیقاً `days`؛ آخرین کلید امروز است |
| `ritmo_number_test.dart` | `percent uses persian glyphs` | `faPercent(62)` برابر `'۶۲٪'` |

### T-6.3 · تست‌های نگهبان معماری

**فایل جدید:** `test/architecture/wellbeing_guards_test.dart` — عیناً بساز:

```dart
import 'dart:io';
import 'package:flutter_test/flutter_test.dart';

Iterable<File> _dartFiles(String dir) {
  final d = Directory(dir);
  if (!d.existsSync()) return const <File>[];
  return d
      .listSync(recursive: true)
      .whereType<File>()
      .where((f) => f.path.endsWith('.dart'));
}

List<String> _matching(String dir, RegExp re, {Set<String> allow = const {}}) {
  final hits = <String>[];
  for (final f in _dartFiles(dir)) {
    final normalized = f.path.replaceAll(r'\', '/');
    if (allow.any(normalized.endsWith)) continue;
    if (re.hasMatch(f.readAsStringSync())) hits.add(normalized);
  }
  return hits;
}

void main() {
  test('no hardcoded hex colors inside the wellbeing feature', () {
    final hits = _matching(
        'lib/features/wellbeing', RegExp(r'0x[fF][fF][0-9a-fA-F]{6}'));
    expect(hits, isEmpty,
        reason: 'W-36: رنگ هاردکد بازگشته است: $hits');
  });

  test('only RitmoDate may build day keys', () {
    final hits = _matching(
      'lib',
      RegExp(r'toIso8601String\(\)\s*\.\s*substring\('),
      allow: {'lib/core/util/ritmo_date.dart'},
    );
    expect(hits, isEmpty,
        reason: 'W-12: مبنای زمانی دوم بازگشته است: $hits');
  });

  test('features never instantiate engines directly', () {
    final hits = _matching(
      'lib/features',
      RegExp(
        r'\b(EnergyAnalyticsEngine|MoodEngine|SleepEngine|ReflectionEngine'
        r'|LifeBalanceEngine|CycleEngine)\(\)\s*\.\s*calculate',
      ),
    );
    expect(hits, isEmpty,
        reason: 'W-25: موتور بدون گذرگاه صدا زده شده: $hits');
  });

  test('wellbeing sheets go through RitmoSheetScaffold', () {
    final hits = _matching(
      'lib/features/wellbeing',
      RegExp(r'showModalBottomSheet\s*<'),
    );
    expect(hits, isEmpty,
        reason: 'W-45: شیت خام بازگشته است: $hits');
  });

  test('analytics engines never read the wall clock', () {
    final hits =
        _matching('lib/core/analytics', RegExp(r'DateTime\s*\.\s*now\(\)'));
    expect(hits, isEmpty,
        reason: 'W-18 / قانون ۱۰: موتور باید زمان تزریقی بگیرد: $hits');
  });

  test('no persian strings inside core analytics', () {
    final hits =
        _matching('lib/core/analytics', RegExp(r"'[^']*[؀-ۿ][^']*'"));
    expect(hits, isEmpty,
        reason: 'قانون ۹: متن فارسی در core: $hits');
  });

  test('no magic sentinel numbers in analytics', () {
    final hits = _matching('lib/core/analytics', RegExp(r'-999'));
    expect(hits, isEmpty, reason: 'W-06: مقدار جادویی -999: $hits');
  });
}
```

<aside>
⚠️

اگر یکی از این تست‌ها قرمز شد، **کد را درست کن، تست را نرم نکن.** تغییر الگوی regex برای رد شدن تست، نقض قانون ۳ است. تنها استثنا: اگر یک مطابقت واقعاً کاذب است (مثلاً داخل کامنت)، مسیر را به `allow` اضافه کن و دلیلش را در گزارش بنویس.

</aside>

```bash
flutter test
flutter analyze > tool/analyze_after.txt
flutter test > tool/test_after.txt
git commit -am "test(wellbeing): wave-6 engine unit tests and architecture guards (P11)"
```

---

## ۹. چک‌لیست نهایی — تعریف «تمام شد»

**کار زمانی تمام است که هر ۲۰ خط زیر تیک خورده باشد.** هیچ کدام را بدون اجرای واقعی تیک نزن.

- [ ]  `flutter analyze`: ۰ error و ۰ warning
- [ ]  `flutter test`: همهٔ تست‌ها سبز، هیچ تستی skip نشده
- [ ]  تعداد تست‌های `tool/test_after.txt` **بیشتر** از `tool/test_before.txt` است
- [ ]  هفت تست نگهبان `wellbeing_guards_test.dart` سبزند
- [ ]  کاربر تازه‌نصب: هیچ `۱۰۰` و هیچ `۰` جعلی در کل بخش دیده نمی‌شود
- [ ]  قطع کردن دیتابیس به جای شاخص ۰، کارت خطا نشان می‌دهد
- [ ]  باز شدن صفحه در حالت آفلاین زیر `2000ms`
- [ ]  در یک بار بارگذاری، `CycleEngine` دقیقاً یک بار اجرا می‌شود
- [ ]  در مسیر بارگذاری صفر فراخوانی شبکه‌ای انجام می‌شود
- [ ]  ثبت سریع حال و انرژی با **یک تپ** کار می‌کند و دکمهٔ «برگردان» دارد
- [ ]  تپ روی حلقه، شیت «این عدد از کجا آمده؟» با هر چهار بخش باز می‌شود
- [ ]  مجموع سهم‌های آبشار به‌علاوهٔ ۵۰ دقیقاً برابر عدد نمایشی است
- [ ]  صفحهٔ «امروز» و بخش حال و تعادل **دقیقاً یک عدد** را نشان می‌دهند
- [ ]  تم روشن در هر سه تب سالم است
- [ ]  در بزرگ‌ترین اندازهٔ قلم سیستم، هیچ `RenderFlex overflowed` در کنسول نیست
- [ ]  TalkBack روی هر دکمه یک برچسب معنادار می‌خواند
- [ ]  هیچ رقم لاتینی در کل UI دیده نمی‌شود
- [ ]  ارتقا از دیتابیس نسخهٔ ۸ به ۹ روی یک دیتابیس واقعی بدون crash انجام شد
- [ ]  دقیقاً هفت commit با همان پیام‌های مشخص‌شده زده شده
- [ ]  گزارش نهایی طبق قالب بخش ۱۰ نوشته شده

---

## ۱۰. قالب اجباری گزارش نهایی

در پایان کار، **دقیقاً این قالب** را پر کن. چیزی اضافه نکن، چیزی حذف نکن.

```markdown
## ۱. یافته‌های پیش‌نیاز
- کلاس توکن رنگ: <نام> در <مسیر>
- امضای subscribe در RitmoEventBus: <امضا>
- نسخهٔ دیتابیس قبل از کار: <عدد>
- lifeBalanceScore در daily_rhythm: NOT NULL بود / nullable بود
- شاخهٔ اجراشده در T-3.7: الف / ب

## ۲. وضعیت هر Task
| Task | وضعیت | فایل‌های تغییریافته | یادداشت |
| --- | --- | --- | --- |
| T-0.1 | انجام شد / Skip | ... | ... |
(هر چهل و چند Task را بنویس، هیچ‌کدام را جا نینداز)

## ۳. موارد پیدا نشده
(طبق قانون ۱۲ — هر فایل، تابع یا خطی که پیدا نکردی، با شمارهٔ Task)
- T-x.y: دنبال <چی> گشتم در <کجا>، پیدا نشد. اقدامی انجام ندادم.

## ۴. انحراف از سند
(هر جایی که مجبور شدی جور دیگری عمل کنی و چرا)

## ۵. اعداد عملکرد (جدول T-5.8 با مقادیر واقعی)

## ۶. خروجی دستورات
- flutter analyze: <تعداد error> error ، <تعداد warning> warning
- flutter test: <عدد> passed ، <عدد> failed ، <عدد> skipped
- تعداد تست قبل: <عدد> — بعد: <عدد>

## ۷. تست دستی
- تم روشن هر سه تب: سالم / مشکل <توضیح>
- بزرگ‌ترین اندازهٔ قلم: بدون سرریز / سرریز در <کجا>
- ارتقای v8 به v9: موفق / ناموفق <خطا>
- کاربر تازه‌نصب: چه دید؟ <توصیف دقیق>

## ۸. کامیت‌ها
(لیست hash و پیام هر کامیت)

## ۹. ریسک‌های باقی‌مانده
(چیزهایی که درست کردی ولی نگرانشانی)
```

<aside>
🏁

**یادآوری آخر:** اگر در هر لحظه مطمئن نبودی، قانون ۱۲ را اجرا کن: **Skip کن، حدس نزن، ثبت کن.** یک Task انجام‌نشدهٔ گزارش‌شده بسیار بهتر از یک Task حدسی است که چیز دیگری را می‌شکند.

</aside>