# 🎓 سند طراحی: صفحه‌ی «دوره‌های آموزشی» (Courses / Learning)

> نویسنده: Claude (معمار) · مالک محصول تصمیم‌های کلیدی را تأیید کرده.
> این سند مبنای `AGENT_PROMPT_COURSES.md` است (پرامپت اجرایی Gemini).

---

## 🎯 بینش محوری
صفحه‌ی دوره‌ها یک **«ردیاب مسیر یادگیری»** است — نه فقط لیست جلسات. هر دوره یک مسیر است که:
- از **واحدهایی** ساخته شده (جلسه‌ی ویدیویی / فصل کتاب / درس / تمرین).
- یک **ریتم هفتگی** دارد («هفته‌ای ۳ جلسه») که برنامه خودش جلسات را روی تقویم پخش می‌کند و یادآوری می‌سازد.
- به **انرژی**، **اهداف** و **داشبورد امروز** متصل است.
- وقتی تمام شد، به **تالار دستاوردها** می‌رود (با مدال + کل ساعت صرف‌شده).

> مرزبندی: دوره‌ها **از فلوی «افزودن» عمومی ساخته نمی‌شوند** (مطابق `DESIGN_ADD_ITEM_FLOW.md`). فقط از همین صفحه. کنکور صفحه‌ی جدا دارد و به این صفحه ربطی ندارد.

---

## 🧱 وضعیت فعلی (آنچه هست)
- جدول `courses` (id, title, totalSessions, sessionDurationMinutes, activityType, zoneId, isArchived, energyRule, createdAt, updatedAt).
- جدول `course_sessions` (id, courseId, sessionNumber, plannedDate, completionStatus, actualDurationMinutes, note, createdAt, updatedAt).
- ویجت `education_management_sheet.dart` — یک Bottom Sheet ساده با CRUD دوره + تیک جلسات + قانون انرژی.
- **مشکل:** در `systems_hub_screen.dart` کاشی «دوره‌های آموزشی» هنوز «به‌زودی» نشان می‌دهد و به جایی وصل نیست.
- دیتابیس نسخه‌ی **۱۲**.

**تصمیم:** شیت موجود را به یک **صفحه‌ی تمام‌صفحه‌ی کامل** ارتقا می‌دهیم، جداول را گسترش می‌دهیم، و کاشی هاب را به آن وصل می‌کنیم (با شیت فعال‌سازی مثل بقیه‌ی ماژول‌ها).

---

## 🎨 هویت بصری
- رنگ پایه: **آبی یادگیری** `#3B82F6` → گرادیان به نیلی `#6366F1`.
- بک‌گراند گرم تیره/روشن طبق `RitmoTheme`. شیشه‌ای (glassmorphism) مطابق بقیه‌ی صفحات.
- هر دوره ایموجی + رنگ اختصاصی خودش (انتخابی کاربر).

---

## 📐 انواع دوره (Course Types) — قابل تنظیم
کاربر هنگام ساخت، **نوع** را انتخاب می‌کند و واحدها بر اساس آن نام‌گذاری می‌شوند:

| نوع | `courseType` | برچسب واحد (`unitLabel`) | نمونه |
|-----|--------------|--------------------------|-------|
| 🎬 دوره‌ی ویدیویی | `VIDEO` | «جلسه» | یودمی، فرادرس، یوتیوب |
| 📖 کتاب | `BOOK` | «فصل» | کتاب درسی، رمان آموزشی |
| 🎯 مهارت/تمرین | `SKILL` | «تمرین» | تایپ ۱۰‌انگشتی، طراحی |
| ✏️ دلخواه | `CUSTOM` | کاربر تعیین می‌کند | هر مسیر یادگیری |

> برچسب واحد در همه‌جای UI استفاده می‌شود: «۳ از ۱۲ **فصل**»، «**تمرین** بعدی».

---

## 🗂 ساختار صفحه‌ی اصلی (`courses_screen.dart`)

```
┌─────────────────────────────────────────────┐
│ ← دوره‌های آموزشی          [🤖]  [＋ دوره]    │
│                                               │
│  ┌─ کارت Hero (هفتگی) ────────────────────┐  │
│  │ این هفته: ████████░░ ۵ از ۷ جلسه       │  │
│  │ ⏱ ۴ ساعت و ۲۰ دقیقه مطالعه            │  │
│  │ 🔥 استریک ۶ روزه   ·   ۳ دوره‌ی فعال   │  │
│  └──────────────────────────────────────────┘ │
│                                               │
│  📅 جلسات امروز                              │  ← بخش ۱
│   ┌────────────────────────────────────────┐ │
│   │ 🎬 فلاتر · جلسه ۷  [▶ شروع] [✓ انجام]  │ │
│   │ 📖 کتاب تاریخ · فصل ۳  [▶] [✓]         │ │
│   └────────────────────────────────────────┘ │
│                                               │
│  📚 دوره‌های فعال                            │  ← بخش ۲
│   ┌────────────────────────────────────────┐ │
│   │ 🎬 آموزش فلاتر       ⏳ ۲ روز عقب       │ │
│   │ ███████░░░ ۷ از ۱۲ جلسه · هفته‌ای ۳   │ │
│   └────────────────────────────────────────┘ │
│   ... (تپ → صفحه‌ی جزئیات دوره)              │
│                                               │
│  🏆 تکمیل‌شده‌ها (۲)                  [نمایش] │  ← بخش ۳ (تاشده)
└─────────────────────────────────────────────┘
```

### بخش Hero (هفتگی) — `courses_weekly_hero.dart`
- نوار پیشرفت هفتگی: مجموع جلسات انجام‌شده‌ی این هفته / مجموع هدف هفتگی همه‌ی دوره‌ها.
- مجموع ساعت مطالعه‌ی این هفته (از `actualDurationMinutes`).
- استریک مطالعه (روزهای متوالی که حداقل یک واحد تیک خورده).
- تعداد دوره‌های فعال.

### بخش ۱ — جلسات امروز (`today_sessions_section.dart`)
- کوئری: `course_sessions` که `plannedDate == امروز` و `completionStatus == PENDING`.
- هر کارت: ایموجی دوره + عنوان + «واحد X» + دو دکمه:
  - **▶ شروع** → تایمر مطالعه (پایین) باز می‌شود.
  - **✓ انجام** → مودال تکمیل (مدت واقعی + یادداشت اختیاری) → ثبت.
- اگر امروز جلسه‌ای نیست: حالت خالی ملایم «امروز جلسه‌ای برنامه‌ریزی نشده 🌿».
- **شرط انرژی:** اگر انرژی فعلی «پایین» و دوره `energyRule == 'skip'` → کارت کم‌رنگ با برچسب «امروز رو استراحت کن». اگر `offerLight` → «یه جلسه‌ی کوتاه‌تر چطوره؟».

### بخش ۲ — دوره‌های فعال (`active_courses_section.dart`)
- کارت هر دوره: ایموجی/رنگ، عنوان، نوع، نوار پیشرفت، «X از Y واحد»، ریتم «هفته‌ای N»، و **badge وضعیت زمان‌بندی**:
  - 🟢 «به‌روز» · 🟡 «X روز عقب» · ⏸ «متوقف».
- تپ → `course_detail_screen.dart`.

### بخش ۳ — تکمیل‌شده‌ها / دستاوردها (`completed_courses_section.dart`)
- تاشده (پیش‌فرض بسته). دوره‌های `status == 'COMPLETED'`.
- هر کارت: 🏆 مدال + عنوان + تاریخ تکمیل + «کل: ۱۸ ساعت در ۶ هفته».
- پیام تشویقی: «تو این دوره رو تموم کردی 🎉».

---

## 🔍 صفحه‌ی جزئیات دوره (`course_detail_screen.dart`)
```
┌─────────────────────────────────────────────┐
│ ← 🎬 آموزش فلاتر                      [⚙ ویرایش]│
│  ███████░░░ ۷ از ۱۲ جلسه (۵۸٪)               │
│  ⏱ کل: ۹ ساعت · میانگین: ۳ جلسه/هفته         │
│  🏁 تخمین پایان: ۱۸ تیر (اگه با همین ریتم بری)│
│  🎯 متصل به هدف: «یادگیری برنامه‌نویسی»        │
│  ─────────────────────────────────────────── │
│  🎚 ریتم: هفته‌ای [۳] جلسه                    │
│      روزهای ترجیحی: ش · د · چ                 │
│      🔔 یادآوری: ۱۸:۰۰  [روشن]                │
│  ─────────────────────────────────────────── │
│  واحدها:                                      │
│   ✅ جلسه ۱  · ۴۵ دقیقه · ۵ تیر               │
│   ✅ جلسه ۲  · ۵۰ دقیقه                       │
│   ⬜ جلسه ۸  · برنامه: امروز  [▶] [✓]         │
│   ⬜ جلسه ۹  · برنامه: شنبه                   │
│   ... [＋ افزودن واحد]                         │
└─────────────────────────────────────────────┘
```
- ویرایش: عنوان، نوع، ایموجی/رنگ، قانون انرژی، هدف متصل، آرشیو/حذف.
- لیست واحدها: تیک، تاریخ برنامه‌ریزی‌شده، مدت واقعی، یادداشت، عنوان واحد اختیاری (نام فصل).
- دکمه‌ی **«بازچینش زمان‌بندی»**: با تغییر ریتم، تاریخ واحدهای باقی‌مانده دوباره پخش می‌شود.

---

## ⏱ تایمر مطالعه (`study_timer_sheet.dart`)
- وقتی «▶ شروع» زده شد: شیت تمام‌صفحه با تایمر بزرگ (شمارنده‌ی رو به جلو).
- دکمه‌های: توقف/ادامه · پایان.
- در پایان: مدت سپری‌شده به‌عنوان `actualDurationMinutes` پیش‌پر می‌شود → مودال تکمیل.
- ساده و بدون Pomodoro اجباری (کاربر اگر خواست فقط نگاه می‌کند به زمان).

---

## 🎚 موتور زمان‌بندی ریتمیک (`course_scheduler.dart`)
وقتی کاربر `weeklyTargetSessions` + `preferredDays` (+ `preferredTime`) را تنظیم می‌کند:
1. واحدهای `PENDING` به‌ترتیب `sessionNumber` گرفته می‌شوند.
2. از امروز به بعد، روی **روزهای ترجیحی** پخش می‌شوند تا سقف هفتگی پر شود، سپس هفته‌ی بعد و... .
3. `plannedDate` هر واحد ذخیره می‌شود.
4. اگر `reminderEnabled`، برای هر `plannedDate` در `preferredTime` یک یادآوری با `alarm_scheduler_service` ثبت می‌شود.
5. **محاسبه‌ی عقب‌افتادگی:** اگر واحدهایی با `plannedDate < امروز` و هنوز `PENDING` → «X روز عقب».

> دوره‌ها **به جدول `routines` اضافه نمی‌شوند** (مرز معماری حفظ می‌شود)؛ زمان‌بندی مستقل در `course_sessions.plannedDate` انجام می‌شود و داشبورد امروز جلسات را مستقیم از همین‌جا می‌خواند.

---

## 📊 موتور تحلیل (`CoursesEngine` — الگوی `CachedEngine`)
- **ورودی:** لیست دوره‌ها، جلسات، وضعیت انرژی فعلی، تاریخ امروز.
- **خروجی:**
  - `weeklyDoneSessions` / `weeklyTargetSessions`
  - `weeklyStudyMinutes`
  - `studyStreakDays`
  - `behindScheduleCourseIds` + تعداد روز عقب هر کدام
  - `todaySessions` (لیست جلسات امروز)
  - `estimatedEndDate` هر دوره (بر اساس ریتم فعلی و واحدهای باقی‌مانده)
- از `RitmoEngineBus` استفاده می‌شود (نه استاتیک)، طبق الگوی `insights_screen.dart`.

---

## 🔗 اتصال به سایر سیستم‌ها
1. **انرژی:** `energyRule` موجود (NONE/skip/offerLight/highEnergyOnly). کارت‌های جلسه‌ی امروز با انرژی فعلی تطبیق داده می‌شوند. منبع: `energy_logs` / `EnergyAnalyticsEngine`.
2. **اهداف (یک‌طرفه):** `courses.linkedGoalId → goals(id)`. در صفحه‌ی جزئیات، دوره به یک هدف کلان وصل می‌شود؛ درصد پیشرفت دوره فقط در UI هدف **نمایش/تغذیه** می‌شود. ⚠️ تکمیل جلسه یا حتی کل دوره **هرگز** `goal_step` را خودکار کامل نمی‌کند (یک قدمِ هدف ممکن است معنایی فراتر از این دوره داشته باشد).
3. **داشبورد امروز:** صفحه‌ی «امروز» یک بخش «جلسات مطالعه‌ی امروز» می‌گیرد که مستقیم از `course_sessions` (plannedDate=today) می‌خواند. (تسک یکپارچه‌سازی جداگانه؛ در این پرامپت فقط hook آماده می‌شود.)
4. **دستیار AI:** پایین — آخرین لایه، با الزام Preview→Edit→Save.

## 🧭 تصمیم‌های معماری قطعی
- **منبع Completion دوره‌ها = فقط `course_sessions.completionStatus`.** نه `routine_completions` (FK به `routines`)، نه `daily_rhythm` (تجمیع روتین). آمار دوره مستقل است.
- **AI هرگز مستقیم به DB نمی‌نویسد:** `Suggestion → Preview → Edit → Save`.
- **`weeklyTargetSessions` فعلاً ثابت؛** ستون `isAdaptive` از حالا در schema هست (بلااستفاده) برای تطبیق هوشمند آینده.
- **ترتیب اجرا:** PASS 0 (ممیزی، بدون کد) → زیرساخت → UI اصلی → AI آخر.

---

## 🤖 دستیار AI دوره‌ها
دکمه‌ی 🤖 در هدر. عملکردهای مجاز:
1. **«برنامه‌ی مطالعه برام بچین»** — با گرفتن تعداد واحد + مهلت (یا ریتم دلخواه) → پیشنهاد `weeklyTargetSessions` + روزهای ترجیحی. کاربر تأیید/ویرایش می‌کند (هرگز اعمال کور).
2. **«کدوم دوره عقب افتادم؟»** — خلاصه‌ی دوره‌های `behindSchedule` + پیشنهاد جبران ملایم.
3. **«این دوره رو به واحد تقسیم کن»** — مثلاً «کتاب ۱۲ فصلی» → ۱۲ واحد با عنوان (اگر کاربر اسم فصل‌ها را داد).
4. **«امروز چی بخونم؟»** — با توجه به انرژی + عقب‌افتادگی، یک جلسه پیشنهاد می‌دهد.

**ممنوعات:** تدریس محتوای علمی، تولید خلاصه‌ی درسی طولانی، قضاوت درباره‌ی سرعت یادگیری کاربر.

---

## ✅ تصمیم‌های قطعی (قفل‌شده)
1. **نوع دوره قابل انتخاب:** VIDEO / BOOK / SKILL / CUSTOM با برچسب واحد متناسب.
2. **زمان‌بندی ریتمیک:** «هفته‌ای N جلسه» + روزهای ترجیحی → پخش خودکار `plannedDate` + یادآوری. تاریخ دستی هم ممکن است (اختیاری در جزئیات).
3. **دوره‌ها از فلوی افزودن عمومی ساخته نمی‌شوند** — فقط از این صفحه. به `routines` اضافه نمی‌شوند.
4. **تب تکمیل‌شده‌ها + دستاورد:** دوره‌ی تمام‌شده به آرشیو با مدال و آمار کل می‌رود.
5. **تایمر مطالعه** اختیاری است؛ «انجام» بدون تایمر هم کار می‌کند (مودال مدت واقعی).
6. **لحن تشویقی، بدون قرمزِ سرزنش‌گر** برای عقب‌افتادگی (مطابق بقیه‌ی صفحات). «۲ روز عقبی، یه جلسه جبران کن 💪» نه «شکست خوردی».
7. **داده‌ی موجود حفظ:** دوره‌ها و جلسات قبلی (اگر هست) با مقادیر پیش‌فرض ستون‌های جدید سازگار بمانند.

---

## 🧩 تغییرات دیتابیس (خلاصه — جزئیات در پرامپت)
**گسترش `courses`:** `courseType`, `unitLabel`, `emoji`, `colorHex`, `provider`, `weeklyTargetSessions`, `preferredDays`, `preferredTime`, `reminderEnabled`, `linkedGoalId`, `status`, `completedAt`, `targetEndDate`.
**گسترش `course_sessions`:** `sessionTitle` (نام واحد اختیاری، مثل نام فصل).
نسخه‌ی دیتابیس: ۱۲ → **۱۳**.
