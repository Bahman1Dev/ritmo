# ✨ سند طراحی — سیستم «دستیار هوشمند» (AI Co-pilot) — ارتقای صفحه

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_ASSISTANT.md`.
> رنگِ هویت: فیروزه‌ای `#06B6D4` (همان رنگِ کاشیِ هاب). آیکن: `sparkles` (✨).

---

## ۱) وضعیت فعلی — موتورِ قوی، صفحه‌ی کم‌جان

- ✅ **زیرساختِ AI پخته است:** `AIGateway` (OpenRouter، کانفیگ از `app_settings`، retry/timeout)، `AIContextBuilder` (+`ConsentProfile`)، `AIPromptEngine`، `AIResponseProcessor`، `AICacheManager`، `AIRateLimiter`، `AIFallbackEngine`. علاوه بر این `breakDownGoal` و `parseQuickAdd` و چند دستیارِ درون‌صفحه‌ای (انرژی/خواب/دوره/کنکور/عبادت/سلامت).
- ✅ صفحه‌ی چت موجود است: `assistant_chat_screen.dart` با جدولِ `assistant_chats` (تاریخچه) و `assistant_suggestions` (چیپ‌ها)، گیتِ `module_assistant_enabled`، و کاشیِ هاب از قبل وصل.
- ⚠️ **ولی فقط یک چت‌باکسِ واکنشی است.** کاشیِ هاب وعده‌ی «بهترین اقدامِ بعدی» می‌دهد ولی صفحه هیچ بریفینگ/پیشنهادِ فعالی نمی‌دهد.
- ⚠️ **تنها صفحه‌ای که می‌تواند همه‌ی سیستم‌ها را هم‌زمان ببیند** (انرژی، خواب، اهداف، روتین، کنکور، دوره‌ها) ولی **هیچ سنتزِ چندسیستمی** نمی‌کند.
- ⚠️ **هیچ کاری نمی‌تواند انجام دهد** — فقط حرف می‌زند. نمی‌تواند روتین/هدف بسازد یا چیزی ثبت کند.
- 🐞 `ConsentProfile()` خالی پاس می‌شود (context واقعی کشیده نمی‌شود) و یک **متادیتای دیباگِ زشت** (`دسته داده… | سطح شواهد…`) به دُمِ هر پاسخ می‌چسبد.

**نتیجه:** زیرساخت آماده است؛ کاری که مانده **تبدیلِ چت‌بات به مغزِ اپ** است — کماندسنتر + سنتزِ چندسیستمی + اقدامِ عملی.

---

## ۲) سه تصمیمِ قطعی (تأییدشده)

1. **هم‌خلبانِ فعالِ کامل.** چت + **بریفینگِ روزانه** + **«بهترین اقدامِ بعدی»** به‌صورتِ خودکار (سنتزِ چندسیستمی) + توانِ پیشنهادِ اقدامِ عملی. مغزِ واقعیِ اپ، جوری که جای یک دستیارِ مستقل را بگیرد.
2. **کماندسنتر (بریفینگ اول).** صفحه با بریفینگِ امروز + کارت‌های «بهترین اقدامِ بعدی» (قابلِ‌اعمال) باز می‌شود؛ چت در تبِ دوم/پایین در دسترس. کاربر بلافاصله ارزش می‌بیند، بدونِ تایپ.
3. **اختیارِ اقدام در همه‌ی سیستم‌ها.** دستیار می‌تواند ساخت/ویرایش در روتین‌ها، اهداف، خواب، انرژی، کنکور، دوره‌ها را **پیشنهاد** دهد — ولی **هرگز نوشتنِ کور**: همیشه `Preview → Edit → Save`. AI payload را می‌سازد؛ کاربر می‌بیند/ویرایش می‌کند/ذخیره می‌کند.

---

## ۳) مغزِ مرکزی: سنتزِ چندسیستمی

این صفحه تنها جایی است که خروجیِ همه‌ی موتورها را با هم می‌بیند:
- **انرژی** (`EnergyAnalyticsEngine`: انرژیِ پویا، اوج، خستگی) و **حال** (`mood_engine`).
- **خواب** (`sleep_engine`: بدهیِ خواب، ثبات، دیشب).
- **اهداف** (`goals_engine`: گام‌های امروز، عقب‌افتاده، نزدیک‌ترین مهلت).
- **روتین‌ها** (`routine_completions`/تکمیل‌ها) و **کنکور/دوره‌ها** (آیتم‌های امروز).

از این‌ها یک **بریفینگِ صادق و کوتاه** می‌سازد («امروز انرژیت معمولاً عصرها می‌افته و ۳ گامِ عقب‌افتاده داری») و یک فهرستِ **رتبه‌بندی‌شده‌ی «بهترین اقدامِ بعدی»** (هر کدام با دلیل + در صورتِ امکان یک اقدامِ قابلِ‌اعمال). **هیچ نوشتنی؛ فقط خواندن و سنتز.**

---

## ۴) معماریِ داده

> گسترشِ حداقلی؛ بدونِ جدولِ سنگینِ جدید. داده‌ی فعلی تستی است.

`assistant_chats` گسترش می‌یابد (با `_safeAddColumn`, `DEFAULT`):

| ستون | نوع | معنی |
|---|---|---|
| `id` / `sender` / `message` / `createdAt` | — | موجود |
| `actionsJson` | TEXT | پیشنهادهای اقدامِ همراهِ پیام (JSON آرایه) — جدید |
| `meta` | TEXT | متادیتای داخلی (دسته‌ها/شواهد) — **دیگر به متنِ پیام چسبانده نمی‌شود** — جدید |

**تنظیمات (seed با `INSERT OR IGNORE`):**
- `module_assistant_enabled` (موجود)
- `assistant_briefing_enabled` = `true` (نمایشِ بریفینگ)
- `assistant_proactive_enabled` = `true` (پیشنهادهای فعال)

`assistant_suggestions` می‌ماند ولی چیپ‌ها **پویا** می‌شوند (از وضعیتِ فعلی).

---

## ۵) موتور (`assistant_engine.dart`, الگوی `CachedEngine`)

ورودی (همه فقط‌خواندنی): خروجی/دادهٔ انرژی، حال، خواب، اهداف، روتین‌ها، کنکور/دوره‌ها + `today`.
خروجی:
- `dailyBriefing` — متنِ کوتاهِ سنتزشده + چند آمارِ کلیدی.
- `nextActions` — فهرستِ رتبه‌بندی‌شده‌ی `NextAction` (عنوان + دلیلِ صادق + اقدامِ قابلِ‌اعمالِ اختیاری).
- `systemHighlights` — یک‌خطی برای هر سیستم (انرژی/خواب/اهداف...).
- `dynamicSuggestions` — چیپ‌های چت بر اساسِ وضعیت (گامِ عقب‌افتاده، خوابِ ثبت‌نشده، انرژیِ پایین...).

منطقِ خالصِ رتبه‌بندی در helperِ تست‌پذیر. **هیچ ارجاعِ مستقیمِ چرخه**؛ تعدیلِ غیرمستقیم فقط از `CycleConsentBridge`.

---

## ۶) اقدامِ عملی (هرگز نوشتنِ کور)

دستیار می‌تواند این اقدام‌ها را **پیشنهاد** دهد؛ هر کدام به یک شیتِ Preview→Edit→Save (ترجیحاً شیتِ موجودِ همان سیستم، پیش‌پُرشده) می‌رود:

| نوع اقدام | مقصد |
|---|---|
| `create_routine` | شیتِ ساختِ روتین (پیش‌پُر) |
| `create_goal` | `create_goal_sheet` (پیش‌پُر؛ حتی با خرد کردنِ AI) |
| `log_sleep` | شیتِ ثبتِ خوابِ دیشب |
| `log_energy_mood` | شیتِ ثبتِ سریعِ انرژی/حال |
| `add_konkur_item` / `create_course` | شیت‌های موجودِ کنکور/دوره |

**اصل:** AI فقط `payload` پیشنهادی تولید می‌کند → کاربر در پیش‌نمایش می‌بیند/ویرایش می‌کند → ذخیره از مسیرِ repositoryِ همان سیستم. AI هرگز مستقیم در DB نمی‌نویسد. اقدامِ خارج از فهرست = فقط دیپ‌لینک به صفحه‌ی مربوطه.

---

## ۷) ساختار صفحه (AssistantScreen — کماندسنتر)

**تبِ «امروز» (پیش‌فرض):**
- **بریفینگِ امروز** (کارتِ بالا) — سنتزِ صادقِ کوتاه + آمارِ کلیدی.
- **بهترین اقدامِ بعدی** — کارت‌های رتبه‌بندی‌شده، هرکدام: عنوان + دلیل + دکمه‌ی «اعمال» (→ پیش‌نمایش) یا «برو به...» (دیپ‌لینک).
- highlightهای کوتاهِ هر سیستم.
- حالتِ کم‌داده: «هنوز دارم اپت رو می‌شناسم — چند روز که ثبت کنی، دقیق‌تر راهنماییت می‌کنم ✨».

**تبِ «گفتگو»:**
- چتِ تمیز (بدونِ متادیتای دیباگ)، حباب‌های خوانا، نشانگرِ تایپ.
- چیپ‌های **پویا** از `dynamicSuggestions`.
- پاسخ‌ها می‌توانند **کارتِ اقدام** داشته باشند (اعمال → پیش‌نمایش).
- پاکسازیِ تاریخچه (موجود) حفظ شود.

دکمه‌ی سراسری: «اقدامِ سریع» (ثبت/ساخت) در دسترس.

---

## ۸) اصلاحاتِ زیرساختِ AI

- **`ConsentProfile` واقعی:** از تنظیمات/رضایت‌ها ساخته شود تا context واقعی کشیده شود (نه `ConsentProfile()` خالی).
- **حذفِ متادیتای دیباگ** از متنِ پیام؛ به ستونِ `meta` برود (لاگِ حریمِ خصوصی در `notification_history` حفظ شود).
- **پاسخِ هم‌خلبان:** قالبِ `{ reply, actions[] }` — جدا از `sendQuery`ِ فعلی (که نباید بشکند)؛ متدِ جدیدِ گیتوی + processorِ جدید. فیلترِ zero-leakِ چرخه روی خروجی.

---

## ۹) لحن و اصول

- هم‌خلبانِ آرام و بدونِ قضاوت؛ پیشنهاد، نه دستور. عقب‌افتادگی هرگز سرزنش نمی‌شود.
- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی.
- شفافیتِ علّی («چرا این پیشنهاد؟»).
- **هیچ ارجاعِ مستقیمی به چرخه** در بریفینگ/چت/اقدام؛ فقط از `CycleConsentBridge` و با لحنِ غیرمستقیم. برای کاربرِ مرد یا رضایتِ خاموش هیچ ردّی نباشد.
- AI هرگز مستقیم در DB نمی‌نویسد (Preview→Edit→Save).

---

## ۱۰) فعال‌سازی (هاب)

کاشیِ «دستیار هوشمند» از قبل با `module_assistant_enabled` وصل است؛ فقط مقصدش از چتِ ساده به `AssistantScreen`ِ کماندسنتر تغییر می‌کند.

---

## ۱۱) ترتیبِ ساخت (خلاصه)

A1 مهاجرت (گسترشِ `assistant_chats` + تنظیمات) → A2 مدل‌ها → A3 موتورِ سنتز (بریفینگ/اقدام/چیپِ پویا) → A4 اصلاحِ زیرساختِ AI (consent واقعی + متدِ هم‌خلبان `{reply,actions}` + حذفِ متادیتا) → A5 رجیستریِ اقدام (نگاشت به شیت‌های موجود) → A6 شیتِ پیش‌نمایشِ اقدام → A7 کماندسنتر (بریفینگ + بهترین اقدام) → A8 چتِ تمیزِ اقدام‌پذیر + چیپِ پویا → A9 مونتاژِ تب‌ها + هاب → A10 سنتزِ پویا/پیشنهادهای فعال → A11 قیدِ چرخه + consent + پرداخت → اعتبارسنجیِ نهایی.
