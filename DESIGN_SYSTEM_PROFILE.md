# 👤 سند طراحی — «پروفایل و تنظیمات کاربری» (Profile) — تکمیل و عمیق‌سازی

> برای **مالکِ محصول**. سندِ اجراییِ Gemini: `AGENT_PROMPT_PROFILE.md`.
> رنگِ هویت: آبی `#5B8AF5` (پایه‌ی فعلیِ صفحه). صفحه به‌صورتِ `DraggableScrollableSheet` از هاب/داشبورد باز می‌شود.

---

## ۱) وضعیت فعلی — پوسته‌ای کامل، مغزی نیمه‌خالی

صفحه (`profile_screen.dart`) ظاهرِ مرتب و چند بخش دارد، اما بخشِ بزرگی «نمایشی» است.

**واقعی و کار می‌کند:** عکسِ پروفایل (انتخاب/ذخیره/حذف)، اطلاعاتِ شخصی (نام/جنسیت/سن → `app_settings`)، تمِ روشن/تاریک (`themeRepository`)، زبان (`localeRepository`)، قلمروهای زمانی (`RealmManagementSheet`)، مناسبت‌های عبادتی، چرخه و هماهنگی، حذفِ تاریخچه‌ی اعلان، خروج، و محاسبه‌ی زنده‌ی «نبضِ زندگی».

**اما این‌ها فقط نمایشی/استاب‌اند:**
- ❌ **مدیریتِ دستگاه‌ها:** «۱ دستگاه متصل» هاردکد + یک ردیفِ ساختگی. هیچ دستگاهِ واقعی‌ای ثبت/خوانده نمی‌شود.
- ❌ **ارتقا به پریمیوم:** فهرستِ ویژگیِ ثابت + دکمه‌ای که فقط اسنک‌بارِ «اشتراکِ شبیه‌سازی فعال شد» نشان می‌دهد. **هیچ پرچمِ `is_premium`، هیچ قفلِ ویژگی.**
- ❌ **پشتیبان‌گیری:** شیت فقط اسنک‌بارِ جعلی می‌زند — درحالی‌که یک `BackupService` **واقعیِ کامل** (AES-GCM + PBKDF2) در `lib/core/services/backup_service.dart` وجود دارد و اصلاً صدا زده نمی‌شود!
- ❌ **رمز عبور / اثر انگشت:** شیت‌ها فقط متنِ توضیحی‌اند؛ دکمه‌ها هیچ کاری نمی‌کنند — درحالی‌که یک سیستمِ **واقعیِ PIN+بیومتریک** (`app_lock_password`/`cycle_biometric_enabled` با `local_auth`) در `cycle_lock_gate.dart` هست، ولی فقط برای چرخه استفاده می‌شود.
- ❌ **مدیریتِ ماژول‌ها:** فقط ۶ ماژول از ~۱۰ ماژول را نشان می‌دهد (کنکور، انرژی، خواب، عادات پیش‌رونده جا افتاده‌اند).
- ❌ **اعلان‌ها / بازخورد / راهنما:** فقط متنِ توضیحی، بدونِ کنترلِ واقعی.
- ❌ **وضعیتِ سریع (انرژی/بستر):** مقادیرِ هاردکد (`_currentEnergy=68`)؛ `_buildHeader`/`_buildQuickStatus` و شیتِ «ثبتِ انرژی» عملاً مرده‌اند.

**نتیجه:** زیرساختِ واقعیِ بک‌آپ و قفل **از قبل وجود دارد** ولی به UI وصل نیست؛ پریمیوم و دستگاه‌ها باید واقعی شوند؛ ماژول‌ها کامل شوند؛ کدِ مرده پاک شود.

---

## ۲) چهار تصمیمِ قطعی (تأییدشده)

1. **پریمیومِ واقعیِ درون‌برنامه.** پرچمِ واقعیِ `is_premium` + سرویسِ قفل/بازکردنِ ویژگی‌ها + فعال‌سازی با **کدِ لایسنس/پرومو** (اعتبارسنجیِ محلی). درگاهِ پرداختِ واقعی فعلاً نه — اما حالتِ پریمیوم در کلِ برنامه واقعی و اثرگذار است.
2. **پنلِ واقعیِ «این دستگاه».** ثبتِ واقعیِ دستگاهِ فعلی (شناسه‌ی پایدار، مدل/پلتفرمِ واقعی، تاریخِ اولین ورود) در یک جدولِ `devices`، با شفافیت که همگام‌سازیِ ابری فعلاً نیست.
3. **قفلِ کلِ برنامه + وصلِ تنظیمات.** یک `AppLockGate` در ریشه‌ی برنامه (قفل در استارت/بازگشت از پس‌زمینه) با همان PIN/بیومتریکِ موجود؛ و سِت‌شیت‌های پروفایل واقعاً PIN را تنظیم/تغییر/حذف و بیومتریک را روشن/خاموش کنند. قفلِ چرخه نشکند.
4. **افزودنِ پکیج‌ها:** `file_picker` + `share_plus` (پشتیبان واقعی)، `device_info_plus` + `package_info_plus` (دستگاه/نسخه).

---

## ۳) معماریِ داده (مهاجرت = نسخه‌ی فعلی + ۱)

> جدولِ موجودِ `app_settings` ستونِ کلید/مقدار دارد. بیشترِ کار با کلیدهای جدید است؛ فقط یک جدولِ سبکِ `devices` اضافه می‌شود.

**جدولِ جدید `devices`:**

| ستون | نوع | معنی |
|---|---|---|
| `id` | TEXT PK | شناسه‌ی پایدارِ دستگاه |
| `deviceName` | TEXT | نامِ نمایشی (مثلاً «Pixel 7 — Android 14») |
| `platform` | TEXT | android/ios/web/windows/... |
| `model` | TEXT | مدلِ واقعی |
| `firstSeenAt` | INTEGER | اولین ورود (epoch) |
| `lastActiveAt` | INTEGER | آخرین فعالیت (epoch) |
| `isCurrent` | INTEGER DEFAULT 1 | دستگاهِ فعلی |

**کلیدهای جدیدِ `app_settings` (seed با `INSERT OR IGNORE`):**
- `is_premium='false'`، `premium_plan=''`، `premium_activated_at=''`، `premium_license=''`.
- `app_lock_enabled='false'`، `app_biometric_enabled='false'` (قفلِ کلِ برنامه؛ جدا از `cycle_biometric_enabled` که دست‌نخورده می‌ماند).
- `notif_master_enabled='true'`، `notif_quiet_start='00:00'`، `notif_quiet_end='07:00'`، `notif_quiet_enabled='false'`.

> **رمزِ موجود plaintext است** (`cycle_lock_gate` آن را plaintext مقایسه می‌کند). برای نشکستنِ آن سازگار بمان؛ هش‌کردن در این فاز ممنوع.

---

## ۴) سرویس‌ها

- **`PremiumService`** (`lib/core/services/premium_service.dart`): `isPremium()`، `activateWithCode(code)` (اعتبارسنجیِ محلیِ کد → ست‌کردنِ `is_premium`/`premium_plan`/`premium_activated_at`)، `deactivate()`، و `isFeatureUnlocked(PremiumFeature f)`. فهرستِ واقعیِ ویژگی‌های پریمیوم تعریف شود (مثلاً بک‌آپِ ابریِ خودکار، تم‌های اضافی، تحلیل‌های پیشرفته). جاهایی که واقعاً قابلِ‌قفل‌اند، گیت بخورند؛ ادعای دروغ نکن.
- **`DeviceService`** (`lib/core/services/device_service.dart`): در استارتِ برنامه دستگاهِ فعلی را در `devices` ثبت/به‌روزرسانی کند (شناسه‌ی پایدار از `device_info_plus` + ذخیره‌ی محلی؛ `firstSeenAt` فقط یک‌بار). متدِ خواندنِ فهرستِ دستگاه‌ها.
- **`AppLockGate`** (`lib/core/security/app_lock_gate.dart`): آینه‌ی `CycleLockGate` اما برای کلِ برنامه؛ فقط وقتی `app_lock_enabled='true'`. قفل در `paused/inactive`؛ بیومتریک اگر `app_biometric_enabled='true'`.

---

## ۵) تغییراتِ صفحه (افزودن/واقعی‌کردن، نه بازنویسی)

- **بخشِ حساب:** ردیفِ پریمیوم → شیت/صفحه‌ی واقعی با وضعیتِ `is_premium` (نشانِ «طلایی» اگر فعال)، پلن‌ها، و ورودیِ کدِ لایسنس/پرومو. ردیفِ دستگاه‌ها → پنلِ واقعیِ این دستگاه + فهرستِ `devices` + یادداشتِ صادقانه‌ی نبودِ همگام‌سازی.
- **بخشِ سیستمِ زندگی:** «مدیریتِ ماژول‌های فعال» همه‌ی ماژول‌ها را نشان دهد (عبادت، دارو، چرخه[زنانه]، کنکور، دوره‌ها، اهداف، عاداتِ پیش‌رونده، دستیار، انرژی، خواب) با برچسب/آیکنِ درست؛ توگل‌ها واقعی persist + زمان‌بندیِ آلارم. کدِ مرده‌ی انرژی/بستر/وضعیتِ سریع پاک یا واقعی شود.
- **بخشِ برنامه:** «پشتیبان‌گیری» واقعی به `BackupService` وصل شود (export با عبارتِ‌عبور → `share_plus`/ذخیره؛ restore با `file_picker` → بازیابی، با هشدارِ جایگزینی). «اعلان‌ها» توگلِ اصلی + ساعتِ سکوتِ واقعی.
- **بخشِ امنیت:** «رمز عبور» واقعاً PIN را تنظیم/تغییر/حذف کند و `app_lock_enabled` را مدیریت کند. «اثر انگشت» واقعاً `app_biometric_enabled` را با بررسیِ در‌دسترس‌بودنِ `local_auth` روشن/خاموش کند. «حریم خصوصی» همان متنِ صادق بماند.
- **بخشِ پشتیبانی:** «درباره» نسخه‌ی واقعی از `package_info_plus`. «بازخورد» محلی ذخیره یا `mailto` با `url_launcher`. «راهنما» محتوای واقعی.

---

## ۶) اصول

- ارقامِ فارسی، RTL، Vazirmatn، تاریخِ شمسی. رنگ از `RitmoTheme`.
- **صداقت:** هیچ دکمه‌ای ادعای کاری که نمی‌کند نکند. اگر چیزی هنوز نیست (همگام‌سازیِ ابری، پرداختِ درگاهی)، صادقانه گفته شود.
- **محرمانگیِ چرخه:** قفل/ماژولِ چرخه فقط برای کاربرِ زن دیده شود؛ منطقِ `CyclePrivacyGuard`/`CycleLockGate` نشکند.
- داده‌ی محلی است؛ بک‌آپ رمزگذاری‌شده با عبارتِ‌عبورِ کاربر.

---

## ۷) ترتیبِ ساخت (خلاصه)

P1 پکیج‌ها + مهاجرت (`devices` + کلیدها) → P2 `PremiumService` → P3 صفحه‌ی پریمیومِ واقعی → P4 `DeviceService` + پنلِ این دستگاه → P5 `AppLockGate` + واقعی‌کردنِ سِت‌شیت‌های امنیت → P6 بک‌آپ/بازیابیِ واقعی → P7 تکمیلِ مدیریتِ ماژول‌ها + پاکسازیِ کدِ مرده → P8 تنظیماتِ اعلانِ واقعی → P9 درباره/بازخورد/راهنمای واقعی → P10 اعتبارسنجی و پرداخت.
