# 🎨 پرامپت بازطراحی صفحه‌ی اصلی (Now Dashboard) — برای Gemini 3.5 Flash

> فایل **خودبسنده**. کل آن را عیناً به Gemini بده. هدف: بازطراحی **پرمیومِ** صفحه‌ی اصلی با حفظ ساختار کلی، تمیزتر و شیک‌تر.
> فایل اصلی: `lib/features/today/presentation/now_dashboard_screen.dart` (ویجت‌ها از قبل در `lib/features/today/presentation/widgets/dashboard/` ماژولار شده‌اند).

---

## ⛔️ قوانین سخت (هرگز نقض نکن)
1. **هر بار فقط یک تسک.** بعد از هر تسک `flutter analyze` و `flutter test` را اجرا کن و گزارش بده. error/warning جدید نباید اضافه شود.
2. **فقط فایل‌های نام‌برده‌ی هر تسک را تغییر بده.**
3. **رنگ/اندازه/رادیوس را هاردکد نکن.** فقط از توکن‌های «سیستم طراحی» پایین یا از `RitmoTheme` استفاده کن.
4. **متن‌ها فارسی و راست‌چین (RTL).** ارقام فارسی در نمایش. فونت `Vazirmatn`.
5. **منطق داده را تغییر نده** — فقط لایه‌ی نمایش (UI) بازطراحی می‌شود. کوئری‌ها/متغیرهای state دست‌نخورده بمانند مگر تسک بخواهد.
6. **هیچ قرمزِ تنبیهی برای کارِ انجام‌نشده نگذار** (اصل «بدون سرزنش»). قرمز فقط برای هشدار ایمنی داروست.
7. **a11y الزامی:** حداقل لمسی ۴۸dp، اندازه‌ها `sp`، کنتراست WCAG AA، `Semantics`/`contentDescription` روی آیکون‌ها، احترام به Reduced Motion.
8. اگر چیزی مبهم بود، حدس نزن — بپرس.

## 🎨 سیستم طراحی (توکن‌ها — عیناً استفاده کن)
**تایپوگرافی (Vazirmatn، واحد sp):** عنوان بزرگ ۲۸ · عنوان کارت ۱۸ · بدنه ۱۵ · فرعی ۱۳. عدد بزرگ هیرو: ۴۴–۴۸.
**پالت (تاریک / روشن):**
| نقش | تاریک | روشن |
|---|---|---|
| پس‌زمینه | `#0F1117` | `#F7F8FA` |
| سطح/کارت | `#1A1D29` | `#FFFFFF` |
| متن اصلی | `#ECEEF3` | `#1A1D29` |
| متن فرعی | `#9AA0AE` | `#5B616E` |
| تأکید Primary | `#5B8AF5` | `#3B6FE0` |
| موفقیت/انجام | `#34D399` | `#10A37F` |
| گرادیان انرژی | `#6B9EFF → #9B89FF` | همان |
| هشدار ملایم | `#F5B95B` | `#D9881F` |
| قرمز (فقط دارو) | `#E5736B` | `#D85A50` |

**شکل/حرکت:** رادیوس کارت/دکمه `16` · چیپ `12` · شیت `24`. گذار `200ms` fade-through (با Reduced Motion → ۱۰۰ms یا حذف). شیشه‌ای (Blur) فقط روی کارت‌های کلیدی (نبض، خلاصه‌ی امروز).

## 🗺 ساختار جدید صفحه (ترتیب نهایی)
```
1. هدر (پاکسازی‌شده: سلام + تاریخ، آواتار، زنگوله، آیکون چت دستیار اگر فعال)
2. نوار pillهای زمینه (QuickActionsBar) — تمیزتر
3. ★ نوار افقی فشرده «سیستم‌های من» (جایگزین لیست حذف‌شده‌ی انتهای صفحه)
4. هشدارهای بحرانی (شرطی)
5. کارت چک‌این صبحگاهی (شرطی)
6. کارت پیشنهاد انعکاس (شرطی، بعد ۲۲:۰۰)
7. ★ کارت پرمیوم «نبض زندگی» (بازطراحی هیرو)
8. ★ کارت جدید «خلاصه‌ی امروز» (دقیقاً زیر نبض)
9. کارت زون و انرژی (ZoneCard)
10. بنر دستیار هوشمند (AssistantBanner)
11. لیست کامل «کارهای امروز»
   ❌ لیست زیرسیستم‌های انتهای صفحه حذف می‌شود
```

---

# 🗂 تسک‌ها (به ترتیب، یکی‌یکی)

## ▣ تسک R1 — بازطراحی پرمیوم کارت «نبض زندگی»
**فایل:** `lib/features/today/presentation/widgets/dashboard/pulse_card.dart`
**اقدام (فقط ظاهر؛ ورودی `rhythmScore` و `onNavigateToTab` ثابت):**
1. کارت شیشه‌ای بزرگ (Blur ملایم) با هاله‌ی گرادیانِ انرژی (`#6B9EFF→#9B89FF`) پشت `RitmoOrb`.
2. امتیاز نبض با تایپوگرافی درشت (۴۴–۴۸sp، رنگ موفقیت برای امتیاز بالا، تأکید برای متوسط). برچسب «نبض زندگی» زیرش (فرعی).
3. **نمودار میله‌ای ۷ روزه** با میله‌های گرادیانی و گردیِ گوشه؛ میله‌ی امروز برجسته (روشن‌تر/ضخیم‌تر).
4. **چیپ روند:** ↑/↓ نسبت به هفته‌ی قبل با رنگ مناسب (سبز/کهربایی، بدون قرمز).
5. کل کارت با لمس → `onNavigateToTab` به تب گزارش‌ها/بینش‌ها.
6. رادیوس ۱۶، سایه‌ی نرم، حاشیه‌ی داخلی ظریف. احترام به Reduced Motion (تپش Orb خاموش شود).
**تأیید:** کارت پرمیوم‌تر دیده شود؛ همان داده؛ analyze/test سبز.

## ▣ تسک R2 — کارت جدید «خلاصه‌ی امروز» (زیر نبض)
**فایل جدید:** `lib/features/today/presentation/widgets/dashboard/today_summary_card.dart`
**ورودی‌ها:** تعداد کل کارهای امروز، تعداد انجام‌شده، نام و ساعتِ **قدم بعدی**، callbackِ `onStartNext` (که شیت نیت را باز می‌کند).
**اقدام:**
1. **حلقه‌ی پیشرفت دایره‌ای** (انجام‌شده/کل) با گرادیان انرژی؛ وسطش درصد یا «۳/۵».
2. متن «۳ از ۵ کار انجام شد · ۲ مانده».
3. **برجسته‌سازی قدم بعدی:** نام + ساعت + دکمه‌ی شیشه‌ای کوچک «شروع» → `onStartNext`.
4. اگر همه انجام شده: حالت تبریک ملایم (سبز + تیک + «امروزت کامل شد») بدون اغراق.
5. کارت شیشه‌ای، رادیوس ۱۶، هماهنگ با کارت نبض.
**سپس** در `now_dashboard_screen.dart` این کارت را **دقیقاً بعد از `PulseCard`** قرار بده و داده‌های لازم را از stateِ موجود (همان داده‌های بخش «کارهای امروز») پاس بده. **منطق جدید نساز؛** از همان متغیرهای موجود استفاده کن.
**تأیید:** کارت زیر نبض دیده شود؛ قدم بعدی درست؛ دکمه شیت نیت را باز کند؛ analyze/test سبز.

## ▣ تسک R3 — حذف زیرسیستم‌های انتهای صفحه + نوار فشرده‌ی «سیستم‌های من»
**علت:** لیست `SubsystemsList` انتهای داشبورد با **تب اختصاصی مرکز سیستم‌ها** تکراری است (تداخل).
**فایل‌ها:** `now_dashboard_screen.dart` + فایل جدید `lib/features/today/presentation/widgets/dashboard/my_systems_strip.dart`
**اقدام:**
1. فراخوانی `SubsystemsList` در انتهای `build` داشبورد را **حذف کن** (خود فایل `subsystems_list.dart` را پاک نکن، فقط از داشبورد بردار).
2. ویجت جدید `MySystemsStrip` بساز: یک **ردیف افقی اسکرول‌شونده از چیپ‌های شیشه‌ای** فقط برای سیستم‌های **فعال** (آموزش، پروژه‌ها، سلامت، عبادت — بر اساس همان پرچم‌های `isCoursesActive/isGoalsActive/isMedicineActive/isWorshipActive`). هر چیپ: آیکون + نام کوتاه؛ لمس → همان callback/شیت قبلی (`onEducationTap` و...).
3. عنوان بخش: **«سیستم‌های من»** (نام جدید جایگزینِ «زیرسیستم‌ها/سیستم‌های سریع»).
4. این نوار را در **جایگاه ۳** (بعد از `QuickActionsBar`، قبل از هشدارها) قرار بده.
**تأیید:** انتهای داشبورد دیگر لیست بلند ندارد؛ نوار فشرده‌ی بالا فقط سیستم‌های فعال را نشان می‌دهد و شیت‌ها درست باز می‌شوند؛ analyze/test سبز.

## ▣ تسک R4 — پاکسازی هدر و pillهای زمینه
**فایل:** `now_dashboard_screen.dart` (متد `_buildHeader`) + `widgets/dashboard/quick_actions_bar.dart`
**اقدام:** بهبود سلسله‌مراتب بصری و فضای تنفس: تراز و اندازه‌ی آواتار/زنگوله، فاصله‌گذاری یکنواخت (مضارب ۴/۸)، یکدست‌کردن رنگ چیپ‌ها با توکن‌ها. اگر ماژول دستیار فعال است، آیکون چت در هدر (طبق سند طراحی). **بدون تغییر منطق.**
**تأیید:** هدر تمیزتر و متعادل‌تر؛ analyze/test سبز.

## ▣ تسک R5 — پرداخت نهایی پرمیوم (Polish سراسری صفحه)
**فایل:** `now_dashboard_screen.dart` و ویجت‌های `widgets/dashboard/*`
**اقدام:**
1. یکدست‌کردن فاصله‌های عمودی بین کارت‌ها (یک ثابت واحد، مثلاً ۱۶).
2. جایگزینی هر رنگ هاردکدِ باقیمانده با توکن `RitmoTheme`.
3. انیمیشن ورود ملایم کارت‌ها (fade/slide ۲۰۰ms) با احترام به Reduced Motion.
4. اطمینان از RTL کامل و ارقام فارسی در همه‌ی اعداد.
**تأیید:** `grep -rn "withOpacity" lib/features/today/presentation/widgets/dashboard/` خالی؛ analyze/test سبز.

## ▣ تسک R6 — صحت‌سنجی نهایی
**اقدام:** `flutter analyze` (بدون ایراد جدید) · `flutter test` (سبز) · `flutter run -d chrome` و بررسی چشمی کل صفحه در حالت تاریک و روشن: نبض پرمیوم، خلاصه‌ی امروز، نوار سیستم‌های من، حذف لیست انتها، RTL و ارقام فارسی.
**تأیید:** صفحه شیک، تمیز و بدون رگرسیون.

---

## 📤 قالب گزارش بعد از هر تسک
```
## تسک [R?]: [عنوان]
- فایل‌های تغییر‌یافته / ساخته‌شده: ...
- خلاصه‌ی تغییر بصری: ...
- flutter analyze: [قبل → بعد]
- flutter test: [N passed, M failed]
- وضعیت: ✅ / ⚠️ / ❌
- اسکرین‌شات یا توضیح ظاهر (در صورت امکان): ...
```
اگر تأیید شکست خورد، همان تسک را برگردان و منتظر دستور بمان.
```
نکته‌ی نام‌گذاری: نام «سیستم‌های من» پیشنهادی است؛ اگر مالک محصول نام دیگری خواست (مثلاً «مرکز سیستم‌ها»)، فقط رشته‌ی عنوان را عوض کن.
```
